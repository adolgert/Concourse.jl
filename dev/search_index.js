var documenterSearchIndex = {"docs":
[{"category":"section","location":"manual/statistics/#Statistical-analysis","page":"Statistical analysis","text":"A simulation run gives you a number. Research needs the number and an honest statement of its uncertainty. This page shows three standard output analyses — batch means, regenerative estimation, and common random numbers — each written as a fold over Concourse records or worlds, so the statistics compose with replay and splitting.\n\nThe running example is the M/M/1 queue at utilization rho = 05, whose long-run mean number in system is exactly rho(1-rho) = 1. Having the true answer lets every method be checked on the page.\n\nusing Concourse\nusing Statistics\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :counter; service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:counter))\nroute!(net, :counter, Always(:done))\nm = compile(net)\n\nrec = simulate(m, [1.0, 2.0], 20_000.0; seed = 42)\ntime_average(number_in_system, m, rec)","title":"Statistical analysis"},{"category":"section","location":"manual/statistics/#Why-one-run-lies","page":"Statistical analysis","text":"The time average above is close to 1, but what is its standard error? The observations inside one run are autocorrelated: when the queue is long now, it will still be long a moment from now. Treating the trajectory as independent samples understates the variance badly, so a naive confidence interval is far too narrow. Batch means and regenerative estimation are two disciplined ways to get an honest interval from a single long run.","title":"Why one run lies"},{"category":"section","location":"manual/statistics/#Batch-means","page":"Statistical analysis","text":"Cut the run into k equal time intervals, called batches. Compute the time average inside each batch. If batches are long compared to the correlation time of the queue, the batch means are nearly independent, and their sample standard deviation gives an honest standard error.\n\nThe fold below integrates a state function piecewise over the record — between firings the state is constant — and splits the integral at batch boundaries.\n\nfunction batch_means(g, m, rec, nbatch)\n    width = rec.horizon / nbatch\n    area = zeros(nbatch)\n    st = initial_state(m)\n    tprev = 0.0\n    times = vcat(rec.time, rec.horizon)\n    for i in eachindex(times)\n        v = g(st)\n        a, b = tprev, times[i]\n        while a < b                     # spread [a, b] over the batches it crosses\n            k = min(nbatch, Int(fld(a, width)) + 1)\n            edge = min(b, k * width)\n            area[k] += v * (edge - a)\n            a = edge\n        end\n        i <= length(rec.key) || break\n        ds = Concourse.replaydraws(rec.key[i], rec.draws[i], m.params)\n        st, _ = fire_changes(m, st, rec.key[i], rec.time[i], ds)\n        tprev = times[i]\n    end\n    area ./ width\nend\n\nb = batch_means(number_in_system, m, rec, 20)\nest = mean(b)\nse = std(b) / sqrt(length(b))\nround(est, digits = 3), round(se, digits = 3)\n\nThe interval covers the true value 1. One diagnostic is worth the habit: the lag-1 correlation of the batch means. If it is far from zero, the batches are too short — use fewer, longer batches.\n\nround(cor(b[1:end-1], b[2:end]), digits = 3)\n\nTwo practical notes. First, this run starts from an empty queue, so the early batches carry some warmup bias; at this utilization the effect is small, but for heavily loaded systems, discard an initial portion of the run before batching. Second, twenty batches is a conventional compromise: enough batches to estimate a variance, few enough that each stays long.","title":"Batch means"},{"category":"section","location":"manual/statistics/#Regenerative-estimation","page":"Statistical analysis","text":"An M/M/1 queue restarts itself. Each time an arrival finds the system empty, the future is a fresh, independent copy of the process — memorylessness wipes out the past. Times like these are called regeneration points, and the segments between them are independent, identically distributed cycles. The theory is developed in Shedler, Regeneration and Networks of Queues (1987), which treats exactly the generalized semi-Markov processes Concourse compiles to.\n\nThe estimator is a ratio. For cycle i, let Y_i be the integral of the statistic over the cycle and tau_i the cycle length. Then the long-run time average is EYEtau, estimated by hat r = sum Y_i  sum tau_i, with a standard error from the delta method: the variance of Z_i = Y_i - hat rtau_i.\n\nThe fold detects a regeneration whenever an :arrival clock fires while the system is empty:\n\nfunction regenerative_cycles(g, m, rec)\n    st = initial_state(m)\n    tprev = 0.0\n    Y = Float64[]; τ = Float64[]\n    y = 0.0; tcycle = 0.0; started = false\n    for i in eachindex(rec.key)\n        t = rec.time[i]\n        if rec.key[i][1] == :arrival && number_in_system(st) == 0\n            if started\n                push!(Y, y); push!(τ, t - tcycle)\n            end\n            started = true; y = 0.0; tcycle = t\n        end\n        started && (y += g(st) * (t - tprev))\n        ds = Concourse.replaydraws(rec.key[i], rec.draws[i], m.params)\n        st, _ = fire_changes(m, st, rec.key[i], rec.time[i], ds)\n        tprev = t\n    end\n    Y, τ\nend\n\nY, τ = regenerative_cycles(number_in_system, m, rec)\nn = length(Y)\nr̂ = sum(Y) / sum(τ)\nZ = Y .- r̂ .* τ\nse_regen = std(Z) / (mean(τ) * sqrt(n))\nn, round(r̂, digits = 3), round(se_regen, digits = 3)\n\nRoughly ten thousand cycles from the same record, and an estimate that again covers the true value. Two properties make the regenerative method attractive when it applies. There is no warmup problem — cycles are independent and identically distributed from the first one. And there is no batch-length tuning — the process itself chooses the cut points. Its limitation is just as plain: the system must actually regenerate often. A heavily loaded queue empties rarely, and a large network may effectively never return to its regeneration state.","title":"Regenerative estimation"},{"category":"section","location":"manual/statistics/#Common-random-numbers","page":"Statistical analysis","text":"Comparing two systems is a different problem from measuring one. The difference between two configurations is usually small compared to the noise of either, so simulating them independently wastes most of the budget. Common random numbers (CRN) is the cure: drive both systems with the same randomness, so the shared noise cancels in the difference.\n\nConcourse's randomness is keyed — every clock and every auxiliary draw has its own named stream derived from one seed — so two worlds built from the same seed reuse the same draws for corresponding clocks even when their parameters differ. Build two branchable worlds on a shared seed, run each to a horizon, and difference the results.\n\nimport ClockGradients\n\n# Fold a time average over a live world, driven by peek and commit.\nfunction world_time_average(g, w, T)\n    acc = 0.0\n    tprev = ClockGradients.branch_time(w)\n    t0 = tprev\n    while true\n        pk = ClockGradients.branch_peek(w)\n        (pk === nothing || pk[1] > T) && break\n        acc += g(w.state) * (pk[1] - tprev)\n        ClockGradients.branch_commit!(w, pk[2], pk[1])\n        tprev = pk[1]\n    end\n    acc += g(w.state) * (T - tprev)\n    acc / (T - t0)\nend\n\n# Mean number in system at μ = 2.0 versus μ = 2.2, R paired replications.\nR = 40; H = 500.0\navgN(θv, seed) = world_time_average(number_in_system,\n                                    branch_world(m, θv; seed), H)\n\npaired = [avgN([1.0, 2.0], r) - avgN([1.0, 2.2], r) for r in 1:R]\nindep  = [avgN([1.0, 2.0], r) - avgN([1.0, 2.2], 5000 + r) for r in 1:R]\n\n(crn = (round(mean(paired), digits = 3), round(std(paired) / sqrt(R), digits = 3)),\n independent = (round(mean(indep), digits = 3), round(std(indep) / sqrt(R), digits = 3)))\n\nThe true difference is 1 - frac1221 - 122 approx 0167. Both estimates cover it, but the paired standard error is many times smaller — a variance reduction of well over an order of magnitude for the same simulation budget. The same pairing works through simulate directly: two calls with equal seeds and different θ couple the same way, which is exactly how the finite-difference baselines in Gradient estimation are built.","title":"Common random numbers"},{"category":"section","location":"manual/statistics/#Splitting-and-statistics-compose","page":"Statistical analysis","text":"Everything above is a fold, and the folds run over records or over live worlds interchangeably. That is deliberate. The splitting idiom of Branching worlds — clone a world, rekey the clones with a shared seed, force different decisions — produces pairs of trajectories that any of these estimators can consume: batch the two continuations, difference their cycle statistics, or pair them as CRN replicates. The statistics do not know they are running on a counterfactual.","title":"Splitting and statistics compose"},{"category":"section","location":"queues/measuring/#Measuring-a-simulation","page":"Measuring a simulation","text":"Every page of this tutorial has compared a measured number against a formula, and each time the measurement quietly used the same three precautions: average over time, average over independent runs, and be suspicious of the start of a run. This page makes the three explicit, because on the problems where you need simulation — the ones with no formula — these habits are all that stands between you and a confident wrong answer.","title":"Measuring a simulation"},{"category":"section","location":"queues/measuring/#The-transient","page":"Measuring a simulation","text":"A simulation starts empty. The theory's L describes the steady state — the long-run behavior after the system has forgotten its start. Between the two lies the transient, and it can last a long time. Watch the running time-average of the number in system, bar N(t) = frac1tint_0^t N(s)ds, for three seeds of an M/M/1 queue at rho = 09, where theory says L = 9:\n\nusing Concourse, Statistics\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :desk; service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:desk))\nroute!(net, :desk, Always(:done))\nm = compile(net)\nθ = [0.9, 1.0]\n\nfunction running_average(rec)\n    ts = Float64[]; avgs = Float64[]\n    acc = 0.0; tprev = 0.0; n = 0\n    for (k, t) in zip(rec.key, rec.time)\n        acc += n * (t - tprev)\n        n += (k[1] == :arrival) ? 1 : -1\n        tprev = t\n        push!(ts, t); push!(avgs, t > 0 ? acc / t : 0.0)\n    end\n    ts, avgs\nend\n\nruns = [running_average(simulate(m, θ, 4000.0; seed = s)) for s in (11, 12, 13)]\nprintln(\"running average at t = 4000, three seeds: \",\n        join([string(round(a[end], digits = 2)) for (_, a) in runs], \", \"),\n        \"   (theory: 9.0)\")\n\nusing Luxor, QueueDiagrams, LaTeXStrings # hide\ntmax = 4000.0 # hide\nymax = 12.0 # hide\nx0, x1, y0, y1 = -270.0, 250.0, 120.0, -110.0 # hide\npx(t) = x0 + (x1 - x0) * t / tmax # hide\npy(v) = y0 + (y1 - y0) * min(v, ymax) / ymax # hide\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nsethue(\"gray60\"); setline(1) # hide\nLuxor.line(Point(x0, y0), Point(x1, y0), action = :stroke) # hide\nLuxor.line(Point(x0, y0), Point(x0, y1), action = :stroke) # hide\nsethue(\"gray40\"); fontsize(11) # hide\nfor t in (0, 1000, 2000, 3000, 4000) # hide\n    text(string(t), Point(px(t), y0 + 16), halign = :center) # hide\nend # hide\nfor v in (0, 3, 6, 9, 12) # hide\n    text(string(v), Point(x0 - 8, py(v) + 4), halign = :right) # hide\nend # hide\ntext(\"time\", Point((x0 + x1) / 2, y0 + 34), halign = :center) # hide\ntext(\"running average of N\", Point(x0, y1 - 12), halign = :left) # hide\nsetline(1.5); setdash(\"dotted\"); sethue(\"black\") # hide\nLuxor.line(Point(x0, py(9.0)), Point(x1, py(9.0)), action = :stroke) # hide\nsetdash(\"solid\"); fontsize(12) # hide\ntext(\"L = 9\", Point(x1 + 10, py(9.0) + 4), halign = :left) # hide\nsetline(2) # hide\nfor (shade, (ts, avgs)) in zip((\"#9db8dc\", \"#6b8ec9\", \"#3b6bb5\"), runs) # hide\n    sethue(shade) # hide\n    idx = 1:10:length(ts) # hide\n    poly([Point(px(ts[i]), py(avgs[i])) for i in idx], # hide\n         action = :stroke, close = false) # hide\nend # hide\nsethue(\"black\"); fontsize(12) # hide\ntext(\"three seeds, one model\", Point(x0 + 12, y1 + 10), halign = :left) # hide\nend 640 320 # hide\n\nTwo lessons in one picture. The curves start far below 9 — the queue was born empty and takes a long time to climb — and after 4000 time units the three seeds still disagree with each other and with the theory. The climb is the transient, a bias. The disagreement is variance. They are separate problems with separate cures.","title":"The transient"},{"category":"section","location":"queues/measuring/#Cure-one:-outrun-the-start,-or-discard-it","page":"Measuring a simulation","text":"How badly can the empty start hurt? Drop to rho = 08 (theory: L = 4), where the queue settles fast enough to measure the damage precisely. Average 32 replications at three horizons:\n\nθ = [0.8, 1.0]                                    # rho = 0.8, L = 4\nfor H in (50.0, 200.0, 2000.0)\n    naive = [time_average(number_in_system, m, simulate(m, θ, H; seed = 100 + r))\n             for r in 1:32]\n    println(\"horizon \", rpad(H, 8), \"estimate \", round(mean(naive), digits = 2),\n            \" ± \", round(std(naive) / sqrt(32), digits = 2))\nend\nprintln(\"theory: 4.0\")\n\nAt horizon 50 the estimate is not just noisy — it is wrong, more than four standard errors below the truth, and averaging more replications of the same short run would only pin down the wrong number more precisely. Bias is the error replication cannot fix. It fades as the horizon grows past the transient, and there is a sharper tool: discard an initial warmup window and average only what remains. replay makes that a pure post-processing step — no re-simulation, just a different fold over the same record:\n\nfunction windowed_average(g, m, rec, t0)\n    states = replay(m, rec)\n    times = vcat(rec.time, rec.horizon)\n    acc = 0.0; tprev = 0.0\n    for (i, t) in enumerate(times)\n        lo = max(tprev, t0)\n        t > lo && (acc += g(states[i]) * (t - lo))\n        tprev = t\n    end\n    acc / (rec.horizon - t0)\nend\n\nwindowed = [windowed_average(number_in_system, m,\n                             simulate(m, θ, 2000.0; seed = 100 + r), 500.0)\n            for r in 1:32]\nprintln(\"discarding t < 500: \", round(mean(windowed), digits = 2), \" ± \",\n        round(std(windowed) / sqrt(32), digits = 2), \"   (theory: 4.0)\")","title":"Cure one: outrun the start, or discard it"},{"category":"section","location":"queues/measuring/#Cure-two:-replicate,-and-let-the-spread-speak","page":"Measuring a simulation","text":"A single run gives one number and no idea how wrong it is. Thirty-two runs give an average and a standard error (SE), and the SE is what turns \"I got 3.91\" into a checkable claim. That is the convention used on every page of this tutorial and throughout Concourse's test suite: report mean ± SE over independent replications, and treat an exact value as confirmed when it lies within four SEs.\n\nThe rho = 09 chart shows why the convention matters. Those three seeds ran for 4000 time units each and ended at 7.74, 7.67, and 8.41 — every one of them below the true 9.0, and no one of them knowably so. A high-load queue drifts through long excursions, so a single trajectory — however long it feels — is one correlated sample, not a verdict. The spread across independent seeds is what tells you how much any of them can be trusted, and at rho = 09 it stays wide enough that even 16 replications leave a standard error near half a job. That slow, expensive convergence near saturation is not a defect of the simulator; it is a fact about the system, the same one the BCMP case study met at its last sweep point.","title":"Cure two: replicate, and let the spread speak"},{"category":"section","location":"queues/measuring/#Time-averages-and-job-averages","page":"Measuring a simulation","text":"One last distinction, because getting it wrong silently corrupts measurements. There are two natural ways to average, and they answer different questions:\n\nTime average — \"how many jobs are in the system, averaged over the clock?\" (time_average(number_in_system, ...); an operator's view of congestion).\nJob average — \"how long does a job spend, averaged over jobs?\" (the per-job sojourns of the disciplines page; a customer's view of delay).\n\nLittle's law L = lambda W is the exact bridge between them. What is not a valid average is the tempting third option: averaging the state over the events in the record. Events cluster where the system is busy, so the event average overweights congestion:\n\nrec = simulate(m, [1.0, 2.0], 2000.0; seed = 1)   # rho = 0.5, L = 1\nstates = replay(m, rec)\nevent_avg = mean(number_in_system(st) for st in states)\nprintln(\"time average:  \", round(time_average(number_in_system, m, rec), digits = 3),\n        \"   (theory: 1.0)\")\nprintln(\"event average: \", round(event_avg, digits = 3),\n        \"   (biased — for this queue the exact event-average is 1.5)\")\n\nBoth numbers are computed from the same record; only one of them is the answer to \"how full is this queue\". Averages must be weighted by time unless the question is genuinely about events.","title":"Time averages and job averages"},{"category":"section","location":"queues/measuring/#Where-to-go-next","page":"Measuring a simulation","text":"This page is the boundary of the tutorial. Everything here treated the record as a stream to fold statistics over; the Manual treats it as the central object it really is:\n\nThe record and replay — determinism, and why every measurement above was reproducible.\nBranching worlds — comparing systems by splitting a trajectory instead of re-running it.\nStatistical analysis — batch means, regeneration, and variance reduction beyond naive replication.\n\nThe formulas checked out. From here on, you can trust the simulator on the systems that have no formulas.","title":"Where to go next"},{"category":"section","location":"developer/event_loop/#The-event-loop","page":"The event loop","text":"This page translates notes/event_loop.tex, the normative middle-layer document. The model contract defines what a model is; this page describes how the interpreter runs one: four decisions D1–D4, the cascade worklist, the processor-sharing compilation, and the branchable world. Charter items F9–F14 extend the falsification charter.","title":"The event loop"},{"category":"section","location":"developer/event_loop/#The-four-decisions","page":"The event loop","text":"D1 (decided). fire emits enable/disable/re-enable deltas. The full recompute of the enabled set is kept as a debug-mode consistency check, not measured against.\nD2 (proposed, then tested). The cascade worklist must settle to a unique, deterministic fixed point; contention is resolved by a declared policy, and the rest of the cascade is claimed to be order-free.\nD3 (decided). The interpreter owns the marked record. CompetingClocks samples, and only samples.\nD4 (decided). Sampler choice belongs to CompetingClocks' SamplerBuilder. No sampler is load-bearing.","title":"The four decisions"},{"category":"section","location":"developer/event_loop/#D1:-fire-emits-clock-deltas","page":"The event loop","text":"An enabled-set diff — compare enabled(state) before and after a firing and enable/disable the difference — looks simpler, but it is semantically blind to re-declarations. When processor sharing re-evaluates every resident clock on an arrival, the enabled set before and after is identical: the diff sees nothing, while the sampler must receive a reenable!. Deltas carry information the diff cannot express. Deltas were chosen for correctness of the order of operations, not for speed.\n\nThe contract's fire is pure and theta-free, so deltas carry only keys and operations — (:enable | :disable | :reenable, key). The interpreter derives each distribution from clock_distribution at the post-firing state and each anchor from the state's te table.\n\nAfter every firing, debug mode asserts two things:\n\nMembership — the new enabled set equals the old one plus the emitted enables minus the emitted disables. The recompute is the specification; deltas are the implementation.\nAnchors — the sampler's per-clock enabling times equal the state's te table. This is the A1 invariant, and it is what checks re-declarations, which membership cannot see.","title":"D1: fire emits clock deltas"},{"category":"section","location":"developer/event_loop/#Deltas-are-derived,-not-emitted","page":"The event loop","text":"Running charter item F2 (add reneging touching only the family registry) falsified D1 as first implemented. Patience clocks are born when a job enters a buffer and die when it leaves. With deltas hand-emitted inside settle! and the family fire functions, adding the family would have meant threading emission code through every state-transition site — interpreter surgery, which is exactly what F2 forbids.\n\nThe repair keeps the decision and changes the mechanism. The cascade knows which stations it touched. After the cascade, for each touched station and each family, the family's enabled keys are diffed between the old and new states (both exist, because fire is pure). Clock lifecycle follows from membership alone; explicit :reenable deltas remain for the processor-sharing path, which membership cannot see. The derived diff runs only over the touched stations' clocks, so the cost argument for deltas still stands. And there is a correctness dividend: a clock enabled and disabled within one firing nets out of the diff and never reaches the sampler — the GSMP has no zero-duration clocks.\n\nThis is the falsification charter working as designed: the claim failed against the first implementation, and what survived is a better mechanism under the same decision. After the repair, adding :patience changed only the registry and the surface fields.","title":"Deltas are derived, not emitted"},{"category":"section","location":"developer/event_loop/#D2:-the-cascade-worklist","page":"The event loop","text":"Within one firing, instantaneous consequences chain. A deposit triggers settle! at the destination. A departure from a full buffer lets a blocked upstream server transfer in, which frees that server, which admits from its buffer, which may unblock stations further upstream. The cascade must reach a fixed point before the sampler sees any delta, and the fixed point must be unique and deterministic, because fire is the replayed, differentiated object. (The generalized stochastic Petri net literature has this exact problem as immediate transitions and vanishing markings.)\n\nThe design splits contention from propagation:\n\nContention is model semantics. Order matters in exactly one place: two consumers competing for one freed resource — two blocked upstream servers and one freed buffer slot. Who wins is an observable modeling choice, so it is a declared policy on the station: FCFSUnblock() (longest-blocked first, the default), PriorityUnblock(by), or RandomUnblock() (a recorded choice, inside the A4 randomness census).\nPropagation is order-free. With contention delegated to policy, the claim is that the remaining cascade relation is confluent: any fair processing order reaches the same fixed point. Termination comes from check C3 (acyclic blocking chains) — or, when compile was passed allow_blocking_cycles, from runtime detection: a realized cycle of full holding stations raises BlockingDeadlock the moment its closing block-edge forms, instead of cascading forever. Confluence plus termination give a unique normal form.\n\nThe implementation still fixes a canonical order — a first-in-first-out worklist seeded with the destination, then the origin — so behavior is deterministic even if the confluence claim were false. A debug-build fuel counter turns nontermination into an error rather than a hang. The claim is tested as F9: run the same trajectories with FIFO and LIFO worklists and require equal states at every index. It holds, on acyclic and on cyclic blocking topologies alike — on a cycle, both orders either reach the horizon with identical records or raise the identical BlockingDeadlock.","title":"D2: the cascade worklist"},{"category":"section","location":"developer/event_loop/#D3:-the-interpreter-owns-the-record","page":"The event loop","text":"The marked record — (key, time, auxiliary draws) per firing — is the primary observable of the library. Its auxiliary-draw column exists because the interpreter supplies the draw source (amendment A4); the sampler never sees those draws, so a sampler-side watcher could never capture them. CompetingClocks' watchers remain what they are — instrumentation of sampling — and none of the record path attaches to them.\n\nOne replay detail follows: during estimation, fire must read the current firing's recorded draws, and the contract does not pass an index. The state therefore carries a firing counter, incremented by every fire — a pure counter in a pure state.","title":"D3: the interpreter owns the record"},{"category":"section","location":"developer/event_loop/#D4:-the-builder-chooses-the-sampler","page":"The event loop","text":"Concourse tells SamplerBuilder the clock-key type and lets CompetingClocks choose a sampler for the distributions in play. The design consequence is a claim, not a configuration: every sampler valid for the model's distributions must produce statistically identical results, because the interpreter speaks only the blessed SamplingContext surface and no model code reaches around it. Estimator-driven overrides (the carry coupling for IPA) are passed through by the estimation entry points, not chosen by the model. This is charter item F10, tested by running the charter oracles under multiple sampler methods.\n\nWith D1–D4 fixed, the interpreter loop is small: ask the sampler for the winner, fire it, run fire_changes (the cascade happens inside), append to the record, apply the deltas to the sampler. Everything model-specific is inside fire_changes; everything stochastic is inside the sampling context and the draw source; everything observable is the record. The loop itself should never grow.","title":"D4: the builder chooses the sampler"},{"category":"section","location":"developer/event_loop/#The-processor-sharing-compilation","page":"The event loop","text":"Processor sharing (PS) serves every job at a station simultaneously, each at a fraction of the server's speed. Every arrival and departure changes the fraction, so the law of every resident clock changes while the clocks stay enabled. Implementing PS (charter item F8) surfaced one normative rule that governs everything else.\n\nThe declaration rule. A model's entire specification for an enabled clock is the pair (F t_e) — a distribution and an enabling time — re-declarable only at event times. Its meaning is the conditional law: given the history up to now, the firing time is distributed as t_e + X with X sim F, conditioned on not having fired yet. Nothing else crosses the interface — not a speed, not an age, not how the sampler realizes the draw. \"Speed\" is a modeling-side device used to construct the declared F, and it compiles away. Two declarations with the same conditional law are the same model. In particular, the anchored-at-now form and the fixed-anchor form of a PS re-declaration are the same conditional law, and a test pins that identity numerically. The choice between them is not semantics but which sufficient statistics the state records — and that choice is forced by the replay bookkeeping, which holds t_e fixed while a clock stays enabled.\n\nConcretely:\n\nThe state carries (bank, anchor) per clock — the internal age at the last re-declaration and that re-declaration's wall time. This is an addendum to amendment A1. The sampler side is CompetingClocks' age-keeping reenable!; the interpreter's existing :reenable branch needed no change.\nA re-evaluation hook. The derived-delta pass gained a per-family hook run after the membership diff at each touched station. The :service method detects a resident-count change at a PS station, banks each survivor's internal age at the old speed, moves its anchor, and emits (:reenable, k). A :rescale memory mode joins :fresh and :resume.\nA bespoke distribution. Composing library truncation and affine wrappers builds the right law but poisons automatic differentiation: stored bound constants produce NaN partial derivatives. SharedRemaining (in src/semantics.jl) implements the conditional-remaining law directly, and is the object the sampler draws from, the likelihood differentiates, and the replay bookkeeping compares for segment detection.\n\nThe oracles: M/M/1-PS matches rho(1-rho); M/G/1-PS with Gamma service is insensitive to the service distribution's shape, while the same law under FCFS matches its distinct Pollaczek–Khinchine mean — which proves the discipline actually shares; replay reproduces bank and anchor exactly; and the score gradient matches finite differences through genuine mid-flight segment chains. All in test/test_ps.jl.","title":"The processor-sharing compilation"},{"category":"section","location":"developer/event_loop/#The-branchable-world","page":"The event loop","text":"The clone-based estimators (branching, and smoothed perturbation analysis — SPA) need to copy a running simulation and force alternative events in the copy. ConcourseWorld — the model, theta, the state, the sampling context, and the clock — implements ClockGradients' ten-verb branchable protocol: branch_peek, branch_commit!, branch_force!, branch_clone, branch_rekey!, branch_time, branch_enabled_ages, branch_clock_distribution, branch_state, and branch_schedule. It is the second implementation beside the ChronoSim adapter — the same two-clients test the model contract passed.\n\nThe verbs are thin. Peek reads the context's cached reservation. Commit and force share one update path. A coupled clone is an empty clone plus a clock copy plus a time restore. Rekey re-seeds and then jitters, because re-seeding alone would replay cached putative times. Ages come from the state's own te table — the A1 invariant makes the sampler agree.\n\nAuxiliary draws in branch worlds (the Q1 decision — see Design questions): the world owns a live keyed stream family beside the sampler, so models with marks, probabilistic routing, and random selection can branch too. The stream key is the stable identity the draw belongs to. A mark draw is keyed by the source's own clock and consumed once per arrival, so the k-th value is the k-th arrival of that source in any same-seed world, whatever else fired in between. Clones copy the generators' states; rekeys re-seed the auxiliary family along with the clocks; and two same-seed clones agree on the marks of corresponding jobs even after a forced firing makes their event orders diverge. Mark laws that read theta are refused at construction: that derivative term is future work, not a default.","title":"The branchable world"},{"category":"section","location":"developer/event_loop/#Charter-F9-F14","page":"The event loop","text":"Continuing the numbering from the model contract's charter:\n\nItem Claim Status Test\nF9 Cascade confluence: with contention resolved by UnblockPolicy, the fixed point is order-independent (FIFO vs LIFO worklists, random blocking networks; on cycles, identical records or identical deadlocks). Tested, holding test/test_blocking.jl, test/test_blocking_cycles.jl\nF10 Sampler agnosticism: no charter result depends on the sampler; oracles pass under multiple builder outcomes. Tested, holding test/test_samplers.jl\nF11 Delta fidelity: emitted deltas agree with the recompute oracle — membership and anchor assertions over long mixed runs, including preemption and PS. Tested, holding debug oracle, asserted in every debug-mode run\nF12 Protocol portability: check_branchable passes in full on ConcourseWorld, for frozen-law models and for PS. Tested, holding test/test_branch.jl\nF13 Clone estimation over re-declared clocks: branching_gradient matches finite differences on the PS terminal count. Tested, holding test/test_branch.jl\nF14 Pathwise estimation over re-declared clocks: frozen-order IPA on PS records gives the correct derivative. Refuted, under both couplings test/test_ps.jl (pinned with fixed seeds)\n\nF14 deserves the emphasis. IPA (infinitesimal perturbation analysis, the pathwise estimator) was believed ready for multi-segment records; the experiment says no. The refutation, the degenerate-segment control that localizes the bias to the record's segment representation, and the diagnosis are on the Design questions page; the algebra that explains it is summarized on The SPA estimator page. On PS records the valid estimators today are the score function and branching.","title":"Charter F9-F14"},{"category":"section","location":"queues/richer_stations/#Richer-stations","page":"Richer stations","text":"The stations so far take one job in, put one job out, and never refuse work. Three mechanisms break that mold, and each one steps outside what product-form theory can bless: fork–join (one job becomes several that must reunite), blocking (a full buffer backs up into the station upstream), and reneging (waiting jobs give up and leave). Each gets a diagram, a short simulation, and the strongest check available — an exact formula where one exists, an exact accounting identity where none does.","title":"Richer stations"},{"category":"section","location":"queues/richer_stations/#Fork–join","page":"Richer stations","text":"A fork splits an arriving job into one sibling per branch; a join waits until all siblings have finished and releases a single merged job. This is every \"scatter–gather\" in computing: a file striped across two disks, a query fanned out to replicas, a build with parallel steps. The join is the expensive part — the merged job is only as fast as its slowest sibling.\n\nusing Luxor, QueueDiagrams, LaTeXStrings # hide\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nsethue(\"black\") # hide\ncircle(Point(-150, 0), 4, action = :fill)   # fork # hide\ncircle(Point(150, 0), 4, action = :fill)    # join # hide\nfontsize(12) # hide\ntext(\"fork\", Point(-150, 22), halign = :center) # hide\ntext(\"join\", Point(150, 22), halign = :center) # hide\nb1 = buffer(Point(-50, -45), 90, 30; slots = 3, jobs = [0]) # hide\ns1 = server(Point(20, -45), 15; inservice = 0, label = L\"\\mu\") # hide\nb2 = buffer(Point(-50, 45), 90, 30; slots = 3, jobs = [0, 0]) # hide\ns2 = server(Point(20, 45), 15; inservice = 0, label = L\"\\mu\") # hide\nflow(Point(-270, 0), Point(-156, 0); label = L\"\\lambda\") # hide\nflow(Point(-146, -3), b1.entry) # hide\nflow(Point(-146, 3), b2.entry) # hide\nflow(b1.exit, s1.entry) # hide\nflow(b2.exit, s2.entry) # hide\nflow(s1.exit, Point(146, -3)) # hide\nflow(s2.exit, Point(146, 3)) # hide\nflow(Point(156, 0), Point(260, 0)) # hide\nend 600 190 # hide\n\nIn Concourse, fork! and join! are stations with no clocks of their own — splitting and merging are instantaneous; only the branch stations take time:\n\nusing Concourse, Statistics\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nfork!(net, :split; branches = (:disk_a, :disk_b))\nstation!(net, :disk_a; service = Law(:Exponential, scale = inv(Param(:mu))))\nstation!(net, :disk_b; service = Law(:Exponential, scale = inv(Param(:mu))))\njoin!(net, :merge; parts = 2)\nsink!(net, :done)\nroute!(net, :arrive, Always(:split))\nroute!(net, :disk_a, Always(:merge))\nroute!(net, :disk_b, Always(:merge))\nroute!(net, :merge, Always(:done))\nfj = compile(net)\nnothing # hide\n\nFork–join queues are notoriously hard: for more than two branches no exact mean is known. But for exactly two exponential branches, Flatto and Hahn derived one:\n\nET = frac12 - rho8 cdot frac1mu - lambda\nqquad rho = lambdamu\n\nand by Little's law the mean number of fork groups in the system is lambda ET. A group is still \"in the system\" while any sibling is, so the measurement counts distinct groups in the state:\n\nfunction replicate(f, m, θ, horizon, nreps; seed0 = 1000)\n    vals = [f(simulate(m, θ, horizon; seed = seed0 + r)) for r in 1:nreps]\n    mean(vals), std(vals) / sqrt(nreps)\nend\n\ngroups_in_system(st) = Float64(length(Set(values(st.group))))\n\nθ = [1.0, 2.0]\nρ = θ[1] / θ[2]\nexact = θ[1] * (12 - ρ) / 8 / (θ[2] - θ[1])\nest, se = replicate(r -> time_average(groups_in_system, fj, r), fj, θ, 4000.0, 16)\nprintln(\"measured groups in system: \", round(est, digits = 3), \" ± \",\n        round(se, digits = 3), \"   Flatto–Hahn: \", round(exact, digits = 4))\n\nFor comparison, a single M/M/1 at this load holds rho(1-rho) = 1 job on average — synchronization pushes the two-branch system well above that, exactly as far as the formula says.","title":"Fork–join"},{"category":"section","location":"queues/richer_stations/#Racing-and-cancellation","page":"Richer stations","text":"A join that waits for all siblings pays for the slowest one. The other classic use of a fork is the opposite bet: send the same request to n replicas, keep the first answer, and throw the rest away. Two keywords of join! declare the race — need, how many siblings must arrive before the merge, and cancel, what happens to the others:\n\njoin!(net, :merge; parts = n, need = k, cancel = :on_completion)\n\nThe two cancellation policies differ in where the race is decided.\n\ncancel = :on_completion decides it at the finish line. The moment the need-th sibling of a group reaches the join, the group merges and every other sibling is canceled wherever it sits — waiting in a buffer, in service, or held blocked — freeing its server or waiting-room slot on the spot, with the freed capacity refilled by the ordinary dispatch cascade in the same instant.\ncancel = :on_start decides it at the starting line. The moment the need-th sibling of a group enters service, siblings still waiting in buffers are canceled. A sibling already in service is never interrupted: it runs to completion, and the join merges the first need finishers and silently absorbs any later one, like a sink — the late finisher's service stays visible in the record; only the join's output ignores it.\n\nThe distinction has teeth even with memoryless service, where intuition says starting and finishing should not matter. Consider (n 1): fork to n single-server replicas, need = 1. Under :on_completion all n servers work on the head group until the first finishes — the service the group experiences is the minimum of n draws, and with exponential service the system is exactly M/M/1 at the pooled rate nmu. Under :on_start the fork deposits all n siblings into their buffers before any dispatch happens, so the first service entry is already the need-th and cancels the rest while they wait: exactly one sibling per group ever serves, and the n replicas behave as one FCFS line with n servers — M/M/n, not a faster M/M/1. Simulation against both closed forms:\n\nfunction race(n, cancel; service = Law(:Exponential, scale = inv(Param(:mu))))\n    net = QueueNetwork(param_names = (:lambda, :mu))\n    source!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\n    branches = [Symbol(:replica_, i) for i in 1:n]\n    fork!(net, :scatter; branches)\n    for b in branches\n        station!(net, b; service)\n        route!(net, b, Always(:first))\n    end\n    join!(net, :first; parts = n, need = 1, cancel)\n    sink!(net, :done)\n    route!(net, :arrive, Always(:scatter))\n    route!(net, :first, Always(:done))\n    compile(net)\nend\n\nθ = [1.0, 1.0]\nmc = race(3, :on_completion)\nms = race(3, :on_start)\nLc, sec = replicate(r -> time_average(groups_in_system, mc, r), mc, θ, 2000.0, 16)\nLs, ses = replicate(r -> time_average(groups_in_system, ms, r), ms, θ, 2000.0, 16)\n\n# Exact answers by Little's law, L = λW.\nWc = 1 / (3 * θ[2] - θ[1])                       # M/M/1 at rate 3μ\na = θ[1] / θ[2]                                  # Erlang C for M/M/3\ntail = a^3 / (factorial(3) * (1 - a / 3))\nC = tail / (sum(a^k / factorial(k) for k in 0:2) + tail)\nWs = 1 / θ[2] + C / (3 * θ[2] - θ[1])\nprintln(\"on_completion: \", round(Lc, digits = 3), \" ± \", round(sec, digits = 3),\n        \"   M/M/1 at 3μ: \", round(θ[1] * Wc, digits = 3))\nprintln(\"on_start:      \", round(Ls, digits = 3), \" ± \", round(ses, digits = 3),\n        \"   M/M/3:       \", round(θ[1] * Ws, digits = 3))\n\nThe :on_completion race is Joshi, Soljanin & Wornell's (n 1) fork-join with cancellation, and their Lemma 1 holds for general service: the system is M/G/1 with service X_(1n), the minimum of n draws, so the Pollaczek–Khinchine formula prices the whole race. A shifted-exponential law — a floor Delta plus an exponential tail, the standard model of a replica with fixed overhead — makes the minimum Delta + mathrmExp(nmu), and P–K needs only its two moments:\n\nusing Distributions: Exponential\nΔ, μs = 0.2, 2.0\noverhead_exp = Opaque((θ, mk, te) -> Δ + Exponential(1 / μs))\nmg = race(3, :on_completion; service = overhead_exp)\n\nλ = 1.5\nLg, seg = replicate(r -> time_average(groups_in_system, mg, r), mg, [λ, 0.0], 2000.0, 16)\n\nEX  = Δ + 1 / (3μs)                              # E[X₍₁:₃₎]\nEX2 = Δ^2 + 2Δ / (3μs) + 2 / (3μs)^2             # E[X₍₁:₃₎²]\nW   = EX + λ * EX2 / (2 * (1 - λ * EX))          # Pollaczek–Khinchine\nprintln(\"measured groups in system: \", round(Lg, digits = 3), \" ± \",\n        round(seg, digits = 3), \"   P–K: \", round(λ * W, digits = 3))\n\nThe test suite runs this oracle for hyperexponential service too, and checks the price of redundancy from the record's service spans: every replica works on the head group for exactly X_(1n), so the server time spent per merged job is EC = nEX_(1n), canceled work included.\n\nA few semantics worth knowing before racing a larger network:\n\nBlocked siblings cancel cleanly. A sibling canceled while held blocked — finished at its station but waiting for room downstream — frees both the server it was holding hostage and its slot in the blocked queue, and the cascade admits the next job immediately. Cancellation never wedges a blocking chain.\nNested forks cancel recursively. A canceled sibling that has itself forked cancels its live descendants, all the way down. In the other direction, a nested join's merged job inherits the group of its fork parent, so inner races compose with outer ones.\nThe record shows the canceled work. Cancellation is deterministic given the state: it consumes no draws and adds no event kind, so replay reproduces every race from the same record (I1 and I4). A canceled service span is still visible — fold fire_changes over the record and watch the clock's st.te entry open and then vanish without a firing of its own; that fold is how the EC test above counts abandoned work.\n\nThree compile-time checks keep a race coherent. A join with need < parts must declare a cancellation policy, or the leftover siblings would be stranded (check C7, caught at declaration):\n\ntry\n    join!(QueueNetwork(param_names = (:lambda,)), :first; parts = 3, need = 2)\ncatch err\n    println(err.msg)\nend\n\ncompile then checks that a fork's siblings can reach at most one canceling join, so which join decides a group's race is unambiguous (check C8), and that every station strictly between a tracked fork and its canceling join receives sibling traffic only — no routes and no renege_to edges from outside — so a job found on a branch is always somebody's sibling (check C9).\n\nOne estimator caveat, kept in the estimator-validity table: which sibling wins a race is decided by event order, the canonical discontinuity that pathwise IPA cannot see. The score estimator remains valid on racing records; Gradient estimation has the details.","title":"Racing and cancellation"},{"category":"section","location":"queues/richer_stations/#Blocking","page":"Richer stations","text":"Buffers are finite in real systems. When a station's waiting room is full, Concourse offers two overflow behaviors: :drop (the arriving job is lost — a call center with a busy signal) and :block, where a finished job at the upstream station cannot move forward, and holds its server hostage until a slot frees downstream. Blocking is how congestion travels backwards through a production line.\n\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nb1 = buffer(Point(-160, 0), 110, 38; slots = 4, jobs = [0, 0, 0]) # hide\ns1 = server(Point(-70, 0), 20; inservice = 0, label = L\"\\mu_1\") # hide\nb2 = buffer(Point(60, 0), 70, 38; slots = 1, jobs = [0]) # hide\ns2 = server(Point(140, 0), 20; inservice = 0, label = L\"\\mu_2\") # hide\nsethue(\"black\"); fontsize(12) # hide\ntext(\"capacity 1\", Point(60, -30), halign = :center) # hide\ntext(\"blocked\", Point(-70, -34), halign = :center) # hide\nflow(Point(-300, 0), b1.entry; label = L\"\\lambda\") # hide\nflow(b1.exit, s1.entry) # hide\nflow(s1.exit, b2.entry) # hide\nflow(b2.exit, s2.entry) # hide\nflow(s2.exit, Point(240, 0)) # hide\nend 620 150 # hide\n\nA tandem line where the second station's buffer holds one waiting job (capacity = 1, overflow = :block), fed harder than the slow second server can drain (lambda = 2, mu_1 = 3, mu_2 = 07) so blocking is constant. When several upstream servers wait for the same freed slot, who goes first is model semantics, declared per station by an unblock policy — FCFSUnblock (longest-blocked first) is the default and currently the only policy:\n\nnet = QueueNetwork(param_names = (:lambda, :mu1, :mu2))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :first;  service = Law(:Exponential, scale = inv(Param(:mu1))))\nstation!(net, :second; service = Law(:Exponential, scale = inv(Param(:mu2))),\n         capacity = 1, overflow = :block, unblock = FCFSUnblock())\nsink!(net, :done)\nroute!(net, :arrive, Always(:first))\nroute!(net, :first,  Always(:second))\nroute!(net, :second, Always(:done))\nbt = compile(net)\n\nθ = [2.0, 3.0, 0.7]\nrec, states = simulate(bt, θ, 500.0; seed = 3, keep_states = true)\nfirst_st = bt.names[:first]\nblocked(st) = isempty(st.hold[first_st]) ? 0.0 : 1.0\nprintln(\"fraction of time station 1 is blocked: \",\n        round(time_average(blocked, bt, rec), digits = 3))\n\nNo closed form is claimed here — finite-buffer tandems have no simple one — but an exact accounting identity must still hold, blocking or not: every arrival is either still inside or has departed. (Note the line at station 1 grows without bound at these rates; this system is deliberately unstable, which is what sustained blocking looks like.)\n\narrivals    = count(k -> k[1] == :arrival, rec.key)\nsecond_st   = bt.names[:second]\ncompletions = count(k -> k[1] == :service && k[2] == second_st, rec.key)\nprintln(arrivals, \" arrivals − \", completions, \" completions = \",\n        arrivals - completions, \"; in system at the end: \",\n        number_in_system(states[end]))\n\nOne structural fact worth knowing: a cycle of stations that can block each other is a deadlock waiting to happen. By default compile rejects such networks (check C3) rather than let a simulation hang — but real systems do have loops (rework sent back upstream, a token ring), and for those the next section shows how to run one anyway and what happens the moment it wedges.","title":"Blocking"},{"category":"section","location":"queues/richer_stations/#Cycles","page":"Richer stations","text":"Under transfer blocking, a deadlock is a cycle of stations, each with a full buffer, each holding a finished job routed to the next station on the cycle. Nothing can ever move again: the only slots that could receive a held job are occupied by jobs whose own destinations are full, all the way around the ring, and no future event frees space because each held job's destination is already chosen. Passing allow_blocking_cycles = true to compile skips check C3 and lets a cyclic topology run; the moment the closing block-edge of a cycle forms, the simulation raises BlockingDeadlock instead of hanging. The exception carries the cycle's station names in routing order, the wall time, the held job ids, and the partial record — every firing up to and including the one that deadlocked, with the horizon set to the deadlock time.\n\nHere is a two-station loop fed hard enough to wedge: jobs go :a → :b, and :b sends three quarters of its output back to :a. Both buffers hold one job, so once each station is full and holding a job bound for the other, the ring is closed:\n\nloop = QueueNetwork(param_names = (:lambda, :mu_a, :mu_b))\nsource!(loop, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(loop, :a; service = Law(:Exponential, scale = inv(Param(:mu_a))),\n         capacity = 1, overflow = :block)\nstation!(loop, :b; service = Law(:Exponential, scale = inv(Param(:mu_b))),\n         capacity = 1, overflow = :block)\nsink!(loop, :done)\nroute!(loop, :arrive, Always(:a))\nroute!(loop, :a, Always(:b))\nroute!(loop, :b, Probabilistic(:a => 0.75, :done => 0.25))\nml = compile(loop; allow_blocking_cycles = true)\n\nerr = try\n    simulate(ml, [4.0, 1.0, 1.0], 1000.0; seed = 1)\ncatch e\n    e\nend\nprintln(sprint(showerror, err))\n\nA deadlock is a property of the state, not of the random draws, so the attached record is replayable evidence: replay reproduces the trajectory and re-raises the identical error at the same firing.\n\nrec = err.record\nprintln(\"record stops at the deadlock: horizon = \", rec.horizon,\n        \", last firing at t = \", rec.time[end])\nerr2 = try\n    replay(ml, rec)\ncatch e\n    e\nend\nprintln(\"replay re-raises the same deadlock: \",\n        err2.cycle == err.cycle && err2.time == err.time && err2.jobs == err.jobs)\n\nThe cascade's order-independence claim (F9 in the event-loop charter) was argued for acyclic blocking and has been re-examined under cycles: it holds. On a cyclic topology under load, FIFO and LIFO worklists either both reach the horizon with identical records or both raise the identical BlockingDeadlock — freed waiting room admits the longest-blocked transfer from a per-destination queue (FCFSUnblock), so cascade order can change when a settle runs but never which blocked transfer wins the room.\n\nOne dynamics fact the tests make vivid: deadlock hazard accrues on every blocking episode, regardless of utilization. A cycle in which both edges actually block carries a positive wedging probability per episode, so it wedges eventually — light load only stretches the expected time. A run that should reach its horizon needs the closing edge structurally out of reach, not merely improbable per unit time: in the two-station loop above, a wide buffer at :a keeps the :b → :a edge from ever forming, and a small CTMC computation in the test suite puts the wedging probability below 10^-5 over the whole horizon.\n\nWhat Concourse does not offer is deadlock resolution — policies like the simultaneous exchange, where every job on a wedged cycle rotates one station forward at once. Resolution is a modeling decision with real degrees of freedom (who moves, in what order, what the service clocks do) and no canonical answer, so for now the library reports the deadlock precisely and stops rather than pick a resolution silently. (Relatedly, if a blocking network ever dies with the settle-cascade fuel error instead, recompile with allow_blocking_cycles = true to get the precise BlockingDeadlock diagnosis.)","title":"Cycles"},{"category":"section","location":"queues/richer_stations/#Reneging","page":"Richer stations","text":"Customers hang up; packets time out; shoppers abandon carts. Reneging gives each waiting job a patience clock: if service has not started when the clock fires, the job leaves the line and goes to a declared destination. A job whose service has begun never reneges.\n\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nb = buffer(Point(-40, 0), 160, 44; slots = 5, jobs = [0, 0, 0]) # hide\ns = server(Point(90, 0), 26; inservice = 0, label = L\"\\mu\") # hide\nflow(Point(-260, 0), b.entry; label = L\"\\lambda\") # hide\nflow(b.exit, s.entry) # hide\nflow(s.exit, Point(200, 0)) # hide\nflow(Point(-70, 24), Point(-70, 80)) # hide\nsethue(\"black\"); fontsize(15) # hide\ntext(L\"\\gamma\", Point(-88, 56), halign = :center, valign = :middle) # hide\nfontsize(12) # hide\ntext(\"gives up\", Point(-40, 96), halign = :center) # hide\nend 560 200 # hide\n\nThe model M/M/1+M adds exponential patience (rate gamma, gamma) to M/M/1, declared with a patience law and a renege_to destination. Take lambda = mu = 1: without abandonment this queue sits exactly on the edge of instability, but impatience drains long lines, so it is stable:\n\nnet = QueueNetwork(param_names = (:lambda, :mu, :gamma))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :counter; service = Law(:Exponential, scale = inv(Param(:mu))),\n         patience = Law(:Exponential, scale = inv(Param(:gamma))),\n         renege_to = :lost)\nsink!(net, :done)\nsink!(net, :lost)\nroute!(net, :arrive, Always(:counter))\nroute!(net, :counter, Always(:done))\nra = compile(net)\nnothing # hide\n\nThe exact stationary distribution (the Erlang-A model) comes from a birth–death chain: arrivals at rate lambda; departures from state n at rate mu + (n-1)gamma, the server plus n-1 impatient waiters. Ten lines to compute, two predictions to check — the mean number in system L, and the abandonment rate, which must equal gamma times the mean number waiting L_q:\n\nfunction erlang_a(λ, μ, γ; N = 400)\n    w = ones(N + 1)\n    for n in 1:N\n        w[n + 1] = w[n] * λ / (μ + (n - 1) * γ)\n    end\n    π = w ./ sum(w)\n    L  = sum(n * π[n + 1] for n in 0:N)\n    Lq = sum(max(n - 1, 0) * π[n + 1] for n in 0:N)\n    L, Lq\nend\n\nθ = [1.0, 1.0, 0.5]\nL_exact, Lq_exact = erlang_a(θ...)\n\nrecs = [simulate(ra, θ, 2000.0; seed = 1000 + r) for r in 1:16]\nLs    = [time_average(number_in_system, ra, r) for r in recs]\nrates = [count(k -> k[1] == :patience, r.key) / r.horizon for r in recs]\n\nprintln(\"mean in system:   \", round(mean(Ls), digits = 3), \" ± \",\n        round(std(Ls) / sqrt(16), digits = 3), \"   Erlang-A: \",\n        round(L_exact, digits = 3))\nprintln(\"abandonment rate: \", round(mean(rates), digits = 3), \" ± \",\n        round(std(rates) / sqrt(16), digits = 3), \"   gamma*Lq: \",\n        round(θ[3] * Lq_exact, digits = 3))\n\nAbandonments appear in the record as a third kind of event key, (:patience, station, job), next to :arrival and :service — the same record machinery from page one, one clock family richer.\n\nEvery check in this tutorial has quietly leaned on replications, seeds, and time averages. The last page makes that machinery explicit — and shows the traps.","title":"Reneging"},{"category":"section","location":"manual/checking/#Checking-your-model","page":"Checking your model","text":"A queueing model can be wrong before it runs, and a simulator can drift from its model while it runs. Concourse checks both. The compiled model is plain data — every law is an inspectable expression, not a closure — so static checks are total functions of the model value, and the runtime checks compare the simulator against the model's own definition.","title":"Checking your model"},{"category":"section","location":"manual/checking/#Checks-at-compile-time","page":"Checking your model","text":"compile(net) refuses broken networks with an error that names the problem. Structural mistakes come first: a route to a station that does not exist, a station with no route out, a law that reads an unknown parameter.\n\nusing Concourse\n\nnet = QueueNetwork(param_names = (:mu,))\nstation!(net, :a; service = Law(:Exponential, scale = inv(Param(:mu))))\nroute!(net, :a, Always(:nowhere))\ntry\n    compile(net)\ncatch err\n    println(err.msg)\nend\n\nMark use is a dataflow check over the routing graph: a law may only read marks that every source reaching it produces.\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :a; service = Law(:Exponential, scale = Mark(:size) * inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:a))\nroute!(net, :a, Always(:done))\ntry\n    compile(net)\ncatch err\n    println(err.msg)\nend\n\nDeadlock from blocking is detected statically. A cycle of stations whose buffers can all fill and block each other would freeze the simulation; the compiler rejects the cycle instead. The same loop with :drop overflow cannot deadlock and compiles fine.\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :a; service = Law(:Exponential, scale = inv(Param(:mu))),\n         capacity = 1, overflow = :block)\nstation!(net, :b; service = Law(:Exponential, scale = inv(Param(:mu))),\n         capacity = 1, overflow = :block)\nsink!(net, :done)\nroute!(net, :arrive, Always(:a))\nroute!(net, :a, Probabilistic(:b => 0.5, :done => 0.5))\nroute!(net, :b, Probabilistic(:a => 0.5, :done => 0.5))\ntry\n    compile(net)\ncatch err\n    println(err.msg)\nend","title":"Checks at compile time"},{"category":"section","location":"manual/checking/#Stability,-per-parameter-value","page":"Checking your model","text":"A network can be well formed and still saturate at the parameters you chose. Concourse.stability (not exported) solves the traffic equations and reports each station's utilization rho, warning when a station is at or beyond capacity.\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :counter; service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:counter))\nroute!(net, :counter, Always(:done))\nm = compile(net)\n\nConcourse.stability(m, [1.0, 2.0])\n\nConcourse.stability(m, [3.0, 2.0])   # warns: ρ = 1.5\n\nAn unstable queue is not an error — you may be studying overload on purpose — so this check warns instead of throwing.","title":"Stability, per parameter value"},{"category":"section","location":"manual/checking/#Checks-at-run-time","page":"Checking your model","text":"","title":"Checks at run time"},{"category":"section","location":"manual/checking/#Replay-as-an-oracle","page":"Checking your model","text":"The strongest routine check costs one keyword. simulate(...; keep_states = true) returns the live state trajectory next to the record, and states_equal compares each live state against the replay fold, field by field — buffers, jobs, clock enabling times, banked ages, everything.\n\nrec, live = simulate(m, [1.0, 2.0], 500.0; seed = 7, keep_states = true)\nall(states_equal(a, b) for (a, b) in zip(live, replay(m, rec)))\n\nIf a model change ever breaks the determinism of replay — a draw that bypassed the recording discipline, state hidden outside QueueState — this comparison fails at the first divergent firing.","title":"Replay as an oracle"},{"category":"section","location":"manual/checking/#The-debug-oracle","page":"Checking your model","text":"The event loop keeps the sampler's enabled set in sync with the model's by applying small deltas: enable this clock, disable that one. The debug = true keyword turns on the membership oracle, which recomputes the model's full enabled set after every firing and errors on the first drift between model and sampler, naming the clocks that disagree.\n\nrec = simulate(m, [1.0, 2.0], 500.0; seed = 7, debug = true)\nlength(rec)   # ran clean: model and sampler agreed after every firing\n\nThe oracle roughly doubles the cost of a run, which is cheap insurance everywhere except your largest production sweeps. The test suite runs with debug = true as a matter of course, and new station types or disciplines should be developed with it on: the recompute is the specification, and the deltas are the implementation being checked against it.","title":"The debug oracle"},{"category":"section","location":"manual/checking/#Certifying-a-branchable-world","page":"Checking your model","text":"For work with branching worlds, one more check applies: ClockGradients.check_branchable exercises the world protocol — peek repeatability, clone coupling, rekey independence — against a world factory and reports pass or fail with diagnostics. Run it once for any new model you intend to branch.","title":"Certifying a branchable world"},{"category":"section","location":"manual/checking/#The-habit","page":"Checking your model","text":"The checks are layered so each catches what the previous cannot: compile catches structure, stability catches parameters, replay catches recording discipline, and the debug oracle catches event-loop drift. Together they are the working answer to the question every simulation paper gets asked — \"why should I believe your simulator?\" — and they are cheap enough to leave on while you build.","title":"The habit"},{"category":"section","location":"queues/arrivals/#Arrivals-and-the-waiting-line","page":"Arrivals and the waiting line","text":"The first page fed a server one job at a time. Real work does not arrive politely. Jobs arrive at random, sometimes in bursts, and when a job finds the server busy it has to wait. This page adds the waiting line — the buffer — and the single number that decides whether the line stays short or grows forever.","title":"Arrivals and the waiting line"},{"category":"section","location":"queues/arrivals/#Poisson-arrivals","page":"Arrivals and the waiting line","text":"The standard model of \"jobs arrive at random\" is the Poisson process. It has one parameter, the arrival rate lambda (lambda): the average number of arrivals per unit of time. Equivalent description: the gaps between arrivals are independent draws from an exponential distribution with mean 1lambda. That is exactly what\n\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\n\ndeclares: interarrival times are exponential, so the source is a Poisson process.\n\nThe exponential distribution is memoryless: however long you have already waited for the next arrival, the remaining wait has the same distribution as the original one. Buses that arrive like this are infuriating, but the property makes the mathematics of the pages that follow tractable, which is why it is the default assumption everywhere in queueing.","title":"Poisson arrivals"},{"category":"section","location":"queues/arrivals/#The-buffer","page":"Arrivals and the waiting line","text":"A station now has two parts: the buffer, where jobs wait, and the server. Arriving jobs join the buffer; whenever the server frees up, it takes the next job according to the discipline (still first come, first served — FCFS):\n\nusing Luxor, QueueDiagrams, LaTeXStrings # hide\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nb = buffer(Point(-40, 0), 160, 44; slots = 5, jobs = [0, 0, 0])\ndiscipline(Point(-40, 0), 160, 44, \"FCFS\")\ns = server(Point(90, 0), 26; inservice = 0, label = L\"\\mu\")\nflow(Point(-260, 0), b.entry; label = L\"\\lambda\")\nflow(b.exit, s.entry)\nflow(s.exit, Point(200, 0))\nend 560 160 # hide","title":"The buffer"},{"category":"section","location":"queues/arrivals/#Utilization-and-stability","page":"Arrivals and the waiting line","text":"Work arrives at rate lambda jobs per minute, and each job needs 1mu minutes of service on average. So the fraction of its time the server must spend busy just to keep up is\n\nrho = fraclambdamu\n\nrho (rho) is called the utilization. It is the single most important number about a queue.\n\nIf rho  1, the server can keep up. The line grows and shrinks but keeps returning to empty. The queue is stable.\nIf rho  1, work arrives faster than it can be served. No discipline, no cleverness helps: the line grows without bound. The queue is unstable.\n\nLet us watch both happen. The model is the same one-station network as before; we only change theta. First lambda = 08 mu = 1 (so rho = 08), then lambda = 11 mu = 1 (so rho = 11). The number in the system rises by one at each arrival and falls by one at each service completion, so a single pass over the record gives the whole trajectory:\n\nusing Concourse\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :desk; discipline = FCFS(), servers = 1,\n         service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:desk))\nroute!(net, :desk, Always(:done))\nm = compile(net)\n\nfunction number_over_time(rec)\n    ts = Float64[0.0]; ns = Int[0]; n = 0\n    for (k, t) in zip(rec.key, rec.time)\n        n += (k[1] == :arrival) ? 1 : -1\n        push!(ts, t); push!(ns, n)\n    end\n    push!(ts, rec.horizon); push!(ns, n)\n    ts, ns\nend\n\nt_stable,   n_stable   = number_over_time(simulate(m, [0.8, 1.0], 400.0; seed = 7))\nt_unstable, n_unstable = number_over_time(simulate(m, [1.1, 1.0], 400.0; seed = 7))\nprintln(\"final counts: rho = 0.8 ends at \", n_stable[end],\n        \" jobs; rho = 1.1 ends at \", n_unstable[end], \" jobs\")\n\nymax = 10.0 * ceil(max(maximum(n_stable), maximum(n_unstable)) / 10) # hide\nx0, x1, y0, y1 = -270.0, 240.0, 120.0, -110.0 # hide\npx(t) = x0 + (x1 - x0) * t / 400.0 # hide\npy(n) = y0 + (y1 - y0) * n / ymax # hide\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nsethue(\"gray60\"); setline(1) # hide\nLuxor.line(Point(x0, y0), Point(x1, y0), action = :stroke) # hide\nLuxor.line(Point(x0, y0), Point(x0, y1), action = :stroke) # hide\nsethue(\"gray40\"); fontsize(11) # hide\nfor t in (0, 100, 200, 300, 400) # hide\n    text(string(t), Point(px(t), y0 + 16), halign = :center) # hide\nend # hide\nfor n in (0, ymax / 2, ymax) # hide\n    text(string(round(Int, n)), Point(x0 - 8, py(n) + 4), halign = :right) # hide\nend # hide\ntext(\"time\", Point((x0 + x1) / 2, y0 + 34), halign = :center) # hide\ntext(\"number in system\", Point(x0, y1 - 12), halign = :left) # hide\nsetline(2) # hide\nsethue(CLASS_COLORS[2]) # hide\npoly([Point(px(t), py(n)) for (t, n) in zip(t_unstable, n_unstable)], # hide\n     action = :stroke, close = false) # hide\nsethue(CLASS_COLORS[1]) # hide\npoly([Point(px(t), py(n)) for (t, n) in zip(t_stable, n_stable)], # hide\n     action = :stroke, close = false) # hide\nsethue(CLASS_COLORS[2]) # hide\ncircle(Point(x1 + 10, py(n_unstable[end])), 4, action = :fill) # hide\nsethue(CLASS_COLORS[1]) # hide\ncircle(Point(x1 + 10, py(n_stable[end])), 4, action = :fill) # hide\nsethue(\"black\"); fontsize(12) # hide\ntext(\"ρ = 1.1\", Point(x1 + 18, py(n_unstable[end]) + 4), halign = :left) # hide\ntext(\"ρ = 0.8\", Point(x1 + 18, py(n_stable[end]) + 4), halign = :left) # hide\nend 640 320 # hide\n\nThe stable line keeps coming back to zero. The unstable line has a random wobble on top of a straight climb: on average it gains lambda - mu = 01 jobs per minute, and after 400 minutes it is roughly 40 jobs deep and still climbing.","title":"Utilization and stability"},{"category":"section","location":"queues/arrivals/#Measuring-the-utilization","page":"Arrivals and the waiting line","text":"For a stable queue, rho is not just a threshold — it is a prediction: the long-run fraction of time the server is busy equals rho exactly. The simulation can check that. time_average integrates any function of the state over the run; here the function is \"is the desk's server occupied\". We average eight independent runs and report the spread of the estimate (a standard error):\n\nusing Statistics\ndesk = m.names[:desk]\nbusy(st) = isempty(st.srv[desk]) ? 0.0 : 1.0\nvals = [time_average(busy, m, simulate(m, [0.8, 1.0], 2000.0; seed = 100 + r))\n        for r in 1:8]\nprintln(\"measured busy fraction: \", round(mean(vals), digits = 3),\n        \" ± \", round(std(vals) / sqrt(8), digits = 3))\nprintln(\"theory: rho = 0.8\")\n\nAt rho = 11 no such number exists to measure: a time average over a growing line depends on how long you watch. Stability first, statistics second — a theme the measurement page returns to.\n\nHow long is the line, on average, when rho  1? That answer needs one more ingredient — the service discipline — and then the M/M/1 page gives the famous formula.\n\nNext: who gets served first.","title":"Measuring the utilization"},{"category":"section","location":"reference/gradients_worlds/#Gradients-and-worlds","page":"Gradients and worlds","text":"This layer binds Concourse to ClockGradients.jl for derivative estimation. A ReplayModel implements the ClockGradients model contract over the record of the interpreter, which admits the score and IPA (infinitesimal perturbation analysis) estimators. A ConcourseWorld implements the branchable-world protocol, which admits the clone-based estimators: weak-derivative branching and SPA (smoothed perturbation analysis). Both are thin wrappers over the contract functions of IR and contract.","title":"Gradients and worlds"},{"category":"section","location":"reference/gradients_worlds/#The-model-contract","page":"Gradients and worlds","text":"","title":"The model contract"},{"category":"section","location":"reference/gradients_worlds/#The-branchable-world","page":"Gradients and worlds","text":"","title":"The branchable world"},{"category":"type","location":"reference/gradients_worlds/#Concourse.ReplayModel","page":"Gradients and worlds","text":"ReplayModel\n\nConcourse's implementation of the ClockGradients.jl model contract: a QueueGSMP bound to a source of auxiliary draws, so the contract's θ-free fire can reproduce mark and routing draws indexed by the state's own firing counter. Build one with replay_model (bound to a record) or live_model (no record, for draw-free models). Pass it to ClockGradients.gradient_record, score_gradient, ipa_gradient, and spa_gradient.\n\n\n\n\n\n","title":"Concourse.ReplayModel"},{"category":"function","location":"reference/gradients_worlds/#Concourse.replay_model","page":"Gradients and worlds","text":"replay_model(m::QueueGSMP, rec::MarkedRecord) -> ReplayModel\n\nBind the model to the auxiliary draws of one record, for gradient estimation over that record. Replaying the draws keeps fire deterministic and free of θ, which the ClockGradients.jl estimators require.\n\nExample\n\nrec = simulate(m, θ, horizon; seed = 1)\nrm = replay_model(m, rec)\ngrec = ClockGradients.gradient_record(rm, rec, θ)\nClockGradients.score_gradient(rm, θ, grec)\n\n\n\n\n\n","title":"Concourse.replay_model"},{"category":"function","location":"reference/gradients_worlds/#Concourse.live_model","page":"Gradients and worlds","text":"live_model(m::QueueGSMP) -> ReplayModel\n\nThe off-record binding, for estimators that continue past any record, such as ClockGradients.spa_gradient (SPA = smoothed perturbation analysis). fire runs with an empty draw source, so this binding works only for models that consume no auxiliary randomness — no marks, no Probabilistic routing, no SIRO, and no populate! mark laws (the init binding is empty too, so a drawn initial mark fails loudly at seeding). A model that asks for a draw fails loudly instead of replaying stale values.\n\n\n\n\n\n","title":"Concourse.live_model"},{"category":"type","location":"reference/gradients_worlds/#Concourse.ConcourseWorld","page":"Gradients and worlds","text":"ConcourseWorld\n\nA branchable simulation world: the compiled model, its parameter values, the current state, a CompetingClocks.jl sampling context, and keyed streams for auxiliary draws. It implements ClockGradients.jl's branchable-world protocol (branch_peek, branch_commit!, branch_force!, branch_clone, branch_rekey!, and the rest), which admits the clone-based estimators such as ClockGradients.branching_gradient. Build one with branch_world.\n\nA clone carries the auxiliary generators' states, so an unrekeyed clone's future is bit-for-bit the original's. After branch_rekey!, same-seed clones still agree on corresponding jobs' draws, because each draw is keyed by the clock it belongs to, never by a position in a global call sequence.\n\n\n\n\n\n","title":"Concourse.ConcourseWorld"},{"category":"function","location":"reference/gradients_worlds/#Concourse.branch_world","page":"Gradients and worlds","text":"branch_world(m, θ; seed, method=NextReactionMethod()) -> ConcourseWorld\n\nAn initialized, ready-to-peek branchable world for model m at parameter vector θ. seed seeds both the sampler's clock streams and the world's auxiliary streams. The default sampler method caches its next reservation, which the protocol's peek-repeatability obligation requires; a redraw-at-next method would fail ClockGradients.check_branchable honestly.\n\nMarks, Probabilistic routing, and SIRO are admitted because their draws are θ-free. A mark law that reads a parameter — a source's, or a station's deposit-time remark — is refused with an ArgumentError: its own derivative would add a term to every estimator, and that term is not implemented. A service law that reads station occupancy (InService/InBuffer) is refused too: pathwise branching is not guaranteed unbiased when event reordering changes an occupancy-dependent law — use the score estimator for those models. A model containing a round station (Rounds) or a ShortestQueue route is refused in v1 for the same reordering reason — a reordered occupancy flips the routing decision discontinuously — and score gradients over their records remain valid (for round stations, θ in the arrival, mark, or remark laws feeding them; a ShortestQueue decision bears no likelihood at all).\n\nA populate! population follows the same rules: a θ-reading initial mark law is refused like a source's, and because a branchable world seeds from the zero-argument initial_state, any drawn initial mark fails loudly at seeding — only draw-free populations branch.\n\nExample\n\nres = ClockGradients.branching_gradient(\n    () -> branch_world(m, θ; seed = 81), θ,\n    st -> Float64(number_in_system(st));\n    nreps = 1200, horizon = 5.0, seed = 17, branch_rng_seed = 18)\n\n\n\n\n\n","title":"Concourse.branch_world"},{"category":"section","location":"developer/spa/#The-SPA-estimator","page":"The SPA estimator","text":"Smoothed perturbation analysis (SPA) is one of the four derivative estimators Concourse supports through ClockGradients.jl. This page summarizes the derivation note notes/spa_derivation.tex (also built as a PDF in notes/), which rebuilds SPA from first principles and then works out what changes when clocks are re-declared mid-flight. The full derivation stays in the note; read it there if you need the mathematics line by line. This page gives the shape.","title":"The SPA estimator"},{"category":"section","location":"developer/spa/#What-SPA-estimates","page":"The SPA estimator","text":"We want partial_thetamathbbEL: the derivative of an expected performance measure — a time integral such as int_0^T N(t)dt, a terminal reading, a first-passage time — with respect to the parameter vector theta.\n\nFix the underlying random numbers and move theta a little. Every holding time slides smoothly, so every firing time slides too. As long as the order of firings does not change, the path deforms continuously, and differentiating along it is what the pathwise estimator — infinitesimal perturbation analysis (IPA) — computes. But at certain values of theta, two firing times tie. Crossing a tie swaps the order of two events, and a functional that reads anything order-dependent jumps there. The set of paths near a tie has probability of order deltatheta, and each contributes a jump of order one; the product is a first-order term that IPA cannot see:\n\nfracddthetamathbbEL\n= underbracemathbbEleftfracpartial Lpartialthetaright_textIPA times slide\n+ underbrace(textprobability flux across ties) times (textjump size)_textthe boundary term IPA drops \n\nSPA estimates the second term and adds it to the first.","title":"What SPA estimates"},{"category":"section","location":"developer/spa/#The-shape-of-the-derivation","page":"The SPA estimator","text":"Conditioning smooths. The trick (Gong and Ho; developed at book length by Fu and Hu) is not to differentiate L itself but a conditional expectation of L. Condition on everything except one distinguished piece of randomness; the inner expectation integrates over that piece, and integration turns jump indicators into continuous probabilities, which differentiate.\n\nThe candidate and the hazard weight. Apply this at each firing. The winner fired at time t_k; consider one other enabled clock — a candidate — with age xi at that moment. Conditioned on everything else, all we know about the candidate is that it survived to age xi, so its conditional law is its declared law conditioned on survival. That is why the boundary weight is a hazard, h_e(xi) = f_e(xi)(1 - F_e(xi)), and not a marginal density. An exact two-clock-race check pins this in the implementation: the hazard weight integrates to the true derivative of the win probability; the marginal density integrates to a wrong value.\n\nThe velocity. The boundary is a tie between two moving times, so the probability flux is proportional to the relative velocity of the age and the candidate's sample, taken at positive part: (dxidtheta - dX_edtheta)^+. The first piece comes from the pathwise replay's time derivatives; the second comes from the candidate's law directly. This dependence matters below: the boundary term is built on top of the pathwise replay. If the replay's time derivatives are wrong, no boundary correction can rescue the sum.\n\nThe jump, by coupled clones. What multiplies the flux is the difference between the functional on the path where the pair fired in swapped order and on the nominal path. The implementation branches two clones from the pre-commit world, gives them one shared rekey seed, forces the pair in opposite orders, and runs both to the horizon. The shared seed makes the difference quiet: the continuation randomness cancels and only the effect of the order remains. Two shortcuts reduce cost. The commuting gate: if both orders re-coalesce to the same state, the jump is exactly zero and no clones are needed — cloning there would return pure noise. The horizon term: a clock scheduled just past the observation window crosses into it as theta moves; that boundary is a constant, so the jump is read directly off the fired state, no clones.\n\nThe estimator is the IPA term plus these boundary contributions summed over epochs and candidates. The note's section 5 maps every symbol of the derivation onto the implementation in ClockGradients/src/spa.jl.","title":"The shape of the derivation"},{"category":"section","location":"developer/spa/#What-re-declared-clocks-change","page":"The SPA estimator","text":"Processor sharing re-declares a clock's distribution at event times while the clock stays enabled; each replacement starts a new segment. SPA refuses records containing multi-segment clocks, and the derivation asked what it would take to lift that refusal.\n\nWhat generalizes cleanly. The weight: the conditioning argument never used the number of segments, so the boundary weight for a re-declared candidate is the hazard of the latest segment's declared law — a mechanical bookkeeping change. The clone construction: whenever the two forced orders re-coalesce to the same state, the survivors' re-declared laws coincide in both arms, so the commuting gate survives.\n\nWhere it actually breaks: the velocity, not the weight. The Question 3 experiments (see Design questions) showed the pathwise replay itself is biased on multi-segment records. The derivation explains why. Differentiating the carry chain — the map that carries a would-be firing age across a re-declaration by matching conditional survival — produces a term in which the sensitivity of the boundary time enters the clock's future weighted by the hazard jump across the re-declaration:\n\ndotalpha = frach^mathrmnew(a) - h(a)h^mathrmnew(alpha)dota\n+ (textterms in  dotalpha text and  partial_theta log S)\n\nThree consequences, each matching an experiment. If a re-declaration does not change the conditional law, the coefficient is zero and the chain step must be transparent. If it changes the speed — processor sharing — the boundary time's derivative genuinely feeds the clock's future, and dropping it biases the replay. And the current implementation cannot express the term: segment laws are rebuilt from states folded at the recorded floating-point times, so their parameters are frozen constants, and the evaluation at the boundary lands at the new law's support start, where it is pinned to zero however the boundary moves. Half of a cancelling pair is killed, which is why even the degenerate case — segments that all declare the same law — fails with large per-path errors.","title":"What re-declared clocks change"},{"category":"section","location":"developer/spa/#The-frozen-order-IPA-bias,-diagnosed","page":"The SPA estimator","text":"This is the diagnosis of charter item F14 (the refutation pinned in test/test_ps.jl). On M/M/1-PS both IPA couplings are biased in different directions while finite differences and the dual machinery both behave. The degenerate-segment control — a 64-server PS station whose dynamics are identical to FCFS but whose records carry chains — shows the bias lives entirely in the record's segment representation: same law, segment-free records pass, chained records fail. A one-line repair (taking the formula branch at the support boundary) was tried and refuted: bit-identical gradients, because round-off lands the evaluation on either side of the boundary and the frozen anchors stay frozen either way. A refuted repair is as informative as a successful one; it rules out the cheap fix before anyone tries it again.\n\nThe two-job worked example in the note makes the lesson concrete. For a plain PS station and an occupancy functional, the jump at a completion–completion tie is exactly zero — the entire derivative must come through the smooth pathwise term, including the way each completion depends on the other job's completion through the speed change. That is precisely the channel the replay is missing. For processor sharing, the hard part of SPA is not an exotic boundary weight; it is that the smooth part must first be made correct.","title":"The frozen-order IPA bias, diagnosed"},{"category":"section","location":"developer/spa/#The-falsification-ladder","page":"The SPA estimator","text":"The derivation dictates the order of implementation work, each step gated by a test that can refute it:\n\nSegment-aware replay. Give the replay the missing channel: rebuild segment laws with dual-valued boundary parameters (fold at dual times, or record segment boundary indices — the Question 4 decision). Gate: the degenerate-segment test flips from refutation to agreement, path by path, not just on average.\nPathwise on PS. Gate: the M/M/1-PS carry column matches finite differences; then a Weibull-base PS variant, which exercises the terms the exponential case partially hides.\nSPA weight for re-declared candidates. Implement the latest-segment hazard, replacing the whole-record refusal only after step 2 holds. Gates on both a boundary-free functional (must agree with repaired IPA) and an order-carried one (must match finite differences where plain IPA visibly does not).\nJump construction audit. An adversarial model where the two forced orders do not re-coalesce, checked against finite differences.\n\nUntil step 2 passes, both SPA's multi-segment refusal and the matching refusal for IPA on chained records are the user-facing truth. Refusals are whole-record, by decision: a record either is within the derivation or is not, and the user hears a clear no rather than a quietly wrong number.","title":"The falsification ladder"},{"category":"section","location":"developer/architecture/#Architecture","page":"Architecture","text":"Concourse is built in three layers. A user writes a queueing network in a small surface language. The library compiles that network into an intermediate representation (IR): a generalized semi-Markov process (GSMP) written down as plain data, with a small generic interpreter. The interpreter drives CompetingClocks.jl, which samples the clocks. This page translates the design note notes/queue_layers.tex, which set out the layering.\n\nnote: Precedence\nnotes/queue_layers.tex was the first design document. Two later notes, notes/model_definition.tex and notes/event_loop.tex, amend it. Where the documents disagree, the later two win. The pages The model contract and The event loop translate those amendments. The end of this page lists what changed.","title":"Architecture"},{"category":"section","location":"developer/architecture/#Why-three-layers","page":"Architecture","text":"Each layer has a one-sentence contract.\n\nLayer 1 builds a value. The surface calls (source!, station!, route!, ...) construct a QueueNetwork. Nothing executes.\nLayer 2 is pure functions over data. compile(net) produces a QueueGSMP that satisfies the ClockGradients.jl model contract. The model is a value you can print, diff, hash, and analyze.\nLayer 3 is driven by the interpreter. No model rule ever calls the sampler. The interpreter tells CompetingClocks which clocks to enable and disable; the sampler only answers \"which clock fires next, and when.\"\n\nThe payoff of this shape: the static checker and the derivative estimators attach to the IR — the same data structure the simulator runs. There is no second model description to keep in sync.\n\nFour decisions were taken as given when the layering was designed:\n\nClock speeds are compiled away. The GSMP literature gives each clock a state-dependent speed. Concourse instead expresses a speed change as a re-declaration of the clock's firing-time distribution (a left-truncated, rescaled law). The sampler never sees a speed. See the declaration rule.\nNo one-input-queue rule. An earlier project (Quarton.jl) required every server to read exactly one queue, which priced out fork–join and multiserver jobs. The IR allows synchronization; the surface exposes it as fork!/join!.\nRecord, then analyze. The primary observable of a run is the record: the ordered sequence of (clock key, firing time, auxiliary draws). Every queueing statistic is a fold over that record. No statistics accumulate inside tokens or queues.\nFull-featured target. Time-varying hazards, static model checking, and derivative estimation are goals, not extensions.","title":"Why three layers"},{"category":"section","location":"developer/architecture/#Layer-1:-the-surface-language","page":"Architecture","text":"Six constructors build a network. Each adds an entry to a QueueNetwork value and has no other effect.\n\nConstructor Meaning\nsource! jobs enter here (interarrival law, mark sampler)\nstation! jobs are served here (discipline, servers, service law, capacity)\nsink! jobs leave here\nfork! one job becomes siblings\njoin! siblings merge when all have arrived\nroute! where departures from a station go\n\nA law maps the parameter vector theta and the enabling time to a probability distribution. Because the law receives the wall-clock time, time-varying hazards (a nonhomogeneous arrival process, a shift schedule) need no extra machinery. A mark is a named value attached to a job at creation — its size or its class. Disciplines and routing kernels read marks.","title":"Layer 1: the surface language"},{"category":"section","location":"developer/architecture/#A-discipline-is-four-functions","page":"Architecture","text":"A service discipline is not a queue subtype. It is a value with four small pure functions, and the buffer is always the same ordered vector:\n\ninsert — where an arriving job is filed in the buffer,\nselect — which waiting job a free server admits,\npreempt — which job in service, if any, is evicted,\nmemory — what an evicted clock remembers: :fresh (start over), :resume (keep the accrued age), or :replay (repeat the identical draw).\n\nFirst-come-first-served (FCFS), last-come-first-served (LCFS), random order (SIRO), priority with and without preemption, and shortest remaining processing time (SRPT) are all instances of this one struct. Processor sharing has no buffer — every job is in service — and is handled by the compilation described in The event loop.","title":"A discipline is four functions"},{"category":"section","location":"developer/architecture/#Routing-kernels","page":"Architecture","text":"A routing kernel is a function from (job, state) to a destination station, attached to the origin station. Always, Probabilistic, ByMark, JoinShortestQueue, LeastWorkLeft, and RoundRobin cover the standard policies. Stateful kernels declare their state (round robin's cursor lives in the IR state), so record replay reproduces them.","title":"Routing kernels"},{"category":"section","location":"developer/architecture/#Layer-2:-the-intermediate-representation","page":"Architecture","text":"Compilation turns the surface value into a QueueGSMP: a vector of compiled stations plus a name table. The dynamic state, QueueState, is places: a buffer vector and an in-service vector per station, the job table, pending join groups, routing cursors — and, per the A1 amendment, the per-clock enabling times and banked ages (see The model contract).\n\nA clock key is one concrete tuple, (family, station, jobid). The initial families are :arrival (a source's next-arrival clock) and :service (one clock per job in service). The family set is open — reneging added :patience — which is the A2 amendment.\n\nThe entire dynamic semantics is three functions:\n\nfire — what a firing means. An arrival creates a job, draws its marks, and routes it. A service completion removes the job from service and routes it. fire is pure: it maps a state to a new state.\ndeposit! — what arriving at a station means. Sinks remove the job. Forks create siblings. Joins hold siblings until all parts are present. Ordinary stations file the job into the buffer, or drop or block it when the buffer is full.\nsettle! — normalization after a change. First preemption: may the best waiting job evict a job in service? Then dispatch: fill free servers in discipline order.\n\nEvery surface feature lands in one identifiable branch of these functions. A service law becomes the distribution of a :service clock. A discipline's insert and select become the filing position and the dispatch step of settle!. A routing kernel becomes the destination argument of deposit!. Within one firing, consequences cascade in a fixed, deterministic order — the cascade worklist of The event loop.","title":"Layer 2: the intermediate representation"},{"category":"section","location":"developer/architecture/#Randomness-accounting","page":"Architecture","text":"A GSMP puts all randomness in clocks. Queueing adds two draws that are not clocks: job marks (a size drawn at arrival) and random routing (a weighted coin flip). The rule, made strict by amendment A4, is:\n\nEvery random draw is either a clock, a recorded mark with a density, or a recorded choice with a probability mass. Nothing else may touch a random number generator.\n\nThe record therefore stores auxiliary draws next to the firings that consumed them. Replay never draws again; it reads the recorded values back. That is what keeps replay exact, and it is what lets the likelihood — and the score estimator built on it — account for every source of randomness.","title":"Randomness accounting"},{"category":"section","location":"developer/architecture/#The-record-and-measurement","page":"Architecture","text":"The primary output of a run is the marked record: one entry per firing, holding the clock key, the firing time, and the auxiliary draws consumed. Because fire is pure and deterministic given the recorded draws, the state at any index is a fold over the record. Waiting times, response times, number in system, utilization, and passage times are all folds. Streaming observers are possible for long runs, but they are an execution-policy choice, not a modeling concept — the same fold, run early.","title":"The record and measurement"},{"category":"section","location":"developer/architecture/#Model-checking","page":"Architecture","text":"Because the IR is data, checks are total functions of it, not lint heuristics over user callbacks:\n\nC1 structure — every routing target exists; stations reach a sink; forks and joins match.\nC2 mark typing — a law may only read marks that every source reaching it produces (a dataflow check over the routing graph).\nC3 capacity — blocking chains must be acyclic; a cycle of full blocking buffers is deadlock, detected statically.\nC4 stability — solve the traffic equations, estimate utilization, warn at saturation.\nC5 oracle detection — networks satisfying the BCMP conditions get a product-form stationary distribution emitted as a test oracle; small all-exponential networks get an exact answer by state enumeration. This is how the test suite gets closed-form answers for free.\nC6 randomness census — list every mark and routing draw with its theta-dependence, and report which derivative estimators are valid.\n\nThese checks run on test/test_checks.jl.","title":"Model checking"},{"category":"section","location":"developer/architecture/#Layer-3:-CompetingClocks","page":"Architecture","text":"The interpreter speaks only the SamplingContext surface: enable!, disable!, reenable!, next, fire!. The mapping is direct: a clock appearing in the enabled set is an enable!; a clock leaving it is a disable!; :resume memory is an enable! with a left-shifted enabling time; a processor-sharing speed change is a reenable! with a truncated, rescaled law. Reproducibility comes from keyed random streams derived from one master seed. Sampler choice belongs to CompetingClocks' builder — no sampler is load-bearing (decision D4 in The event loop).\n\nNon-exponential and time-varying laws are first-class throughout. That is the reason CompetingClocks exists, so supporting them costs the surface language nothing.","title":"Layer 3: CompetingClocks"},{"category":"section","location":"developer/architecture/#What-later-notes-changed","page":"Architecture","text":"Amendments in notes/model_definition.tex and decisions in notes/event_loop.tex supersede this note on four points:\n\nClock bookkeeping moved into the state (A1). This note left per-clock enabling times and ages in the sampler. That broke fire's purity for age-reading disciplines and made replay ambiguous for time-varying laws.\n\"Two clock families suffice\" was refuted (A2). Reneging is a third family; breakdowns would be a fourth. The family set became a registry.\nFunction-valued fields became combinator values (A3), so the IR is actually inspectable data, not closures.\nThe enabled-set diff was replaced by clock deltas (D1). A diff is blind to re-declarations, which processor sharing requires. The diff survives as a debug oracle.\n\nConcourse also dropped the planned dependency on ChronoSim.jl. Every seam this note borrowed from ChronoSim exists in CompetingClocks.jl and ClockGradients.jl, and Concourse became the second independent implementation of the ClockGradients model contract — the test that the contract is an abstraction, not a description of its only client.","title":"What later notes changed"},{"category":"section","location":"queues/marks/#Marks:-classes-of-jobs","page":"Marks: classes of jobs","text":"So far every job has been anonymous. Real workloads are mixtures: short requests and long ones, interactive users and batch jobs, readers and writers. Concourse gives jobs identity through marks: named numbers stamped on a job when it is born, carried with it everywhere it goes.\n\nA mark is declared at the source with a MarkLaw — one probability law per mark name. Marks then have three uses, two of which earlier pages already sneaked in:\n\nOrdering keys. The disciplines page sorted buffers by Mark(:size) for priority and SRPT.\nClass-dependent service. A service law can read a mark: Law(:Dirac, value = Mark(:size)) (\"service equals the stamped size\") or Law(:Exponential, scale = Mark(:mean_s)) (\"exponential with a per-job mean\").\nRouting. The ByMark kernel sends a job one way or another depending on a mark — this page's experiment.","title":"Marks: classes of jobs"},{"category":"section","location":"queues/marks/#Routing-by-size","page":"Marks: classes of jobs","text":"A classic design question: given one stream of jobs with wildly mixed sizes and two servers, should the servers share a single line? One good answer is SITA — size-interval task assignment: dedicate one server to short jobs and the other to long jobs, so a short job never gets stuck behind a monster. In the diagram, blue jobs (short) go up, orange jobs (long) go down.\n\nusing Luxor, QueueDiagrams, LaTeXStrings # hide\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nsethue(\"black\"); fontsize(13) # hide\ntext(\"size < 2\", Point(-90, -52), halign = :center) # hide\ntext(\"size ≥ 2\", Point(-90, 78), halign = :center) # hide\nb1 = buffer(Point(20, -40), 100, 34; slots = 4, jobs = [1, 1]) # hide\ns1 = server(Point(100, -40), 18; inservice = 1, label = L\"\\mu_{short}\") # hide\nb2 = buffer(Point(20, 60), 100, 34; slots = 4, jobs = [2]) # hide\ns2 = server(Point(100, 60), 18; inservice = 2, label = L\"\\mu_{long}\") # hide\nflow(Point(-240, 10), Point(-140, 10); label = L\"\\lambda\") # hide\nflow(Point(-140, 10), b1.entry) # hide\nflow(Point(-140, 10), b2.entry) # hide\nflow(b1.exit, s1.entry) # hide\nflow(b2.exit, s2.entry) # hide\nflow(s1.exit, Point(200, -40)) # hide\nflow(s2.exit, Point(200, 60)) # hide\nend 560 230 # hide\n\nThe model: each arrival is stamped with an exponential size mark (mean 1), and ByMark compares the mark against the cutoff 2.0 — below it, the job goes to :short; at or above it, to :long.\n\nusing Concourse, Statistics\n\nnet = QueueNetwork(param_names = (:lambda, :mu_short, :mu_long))\nsource!(net, :arrive;\n        interarrival = Law(:Exponential, scale = inv(Param(:lambda))),\n        mark = MarkLaw(size = Law(:Exponential, scale = Const(1.0))))\nstation!(net, :short; service = Law(:Exponential, scale = inv(Param(:mu_short))))\nstation!(net, :long;  service = Law(:Exponential, scale = inv(Param(:mu_long))))\nsink!(net, :done)\nroute!(net, :arrive, ByMark(Mark(:size), [2.0], [:short, :long]))\nroute!(net, :short, Always(:done))\nroute!(net, :long,  Always(:done))\nm = compile(net)\nθ = [1.0, 2.0, 1.0]        # lambda, mu_short, mu_long\nnothing # hide","title":"Routing by size"},{"category":"section","location":"queues/marks/#Marks-live-in-the-record","page":"Marks: classes of jobs","text":"The first page said the record is the complete output of a simulation. Marks keep that promise: every mark draw is written into the record next to the event that consumed it. Print the first few events:\n\nrec = simulate(m, θ, 8.0; seed = 5)\nfor i in 1:min(7, length(rec))\n    println(rpad(round(rec.time[i], digits = 3), 8), rpad(rec.key[i], 22),\n            isempty(rec.draws[i]) ? \"(no draws)\" : rec.draws[i])\nend\n\nEach arrival carries its :size draw; service completions consumed no draws. Because the stamped sizes are in the record, replaying a run reproduces every routing decision exactly — replay never re-rolls the dice. (This is the foundation the Manual builds on.)","title":"Marks live in the record"},{"category":"section","location":"queues/marks/#The-check:-thinning","page":"Marks: classes of jobs","text":"Splitting a Poisson stream by an independent per-job test leaves each branch Poisson (the same thinning argument as the probabilistic split). A size mark below the cutoff happens with probability p = P(textsize  2) = 1 - e^-2 approx 0865, so:\n\n:short is an M/M/1 queue with lambda_s = plambda and mu_s = 2,\n:long is an M/M/1 queue with lambda_l = (1-p)lambda and mu_l = 1,\n\nand each branch must obey its own L = rho(1-rho) — computed here against measured per-station occupancy:\n\nfunction replicate(f, m, θ, horizon, nreps; seed0 = 1000)\n    vals = [f(simulate(m, θ, horizon; seed = seed0 + r)) for r in 1:nreps]\n    mean(vals), std(vals) / sqrt(nreps)\nend\n\nat(m, name) = (q = m.names[name]; st -> Float64(length(st.buf[q]) + length(st.srv[q])))\n\np = 1 - exp(-2.0)\nfor (name, λx, μx) in [(:short, p * θ[1], θ[2]), (:long, (1 - p) * θ[1], θ[3])]\n    ρ = λx / μx\n    est, se = replicate(r -> time_average(at(m, name), m, r), m, θ, 2000.0, 16)\n    println(rpad(name, 7), \"measured L = \", round(est, digits = 3), \" ± \",\n            round(se, digits = 3), \"   theory \", round(ρ / (1 - ρ), digits = 3))\nend\n\nBoth stations match their closed forms. Note what was checked: a derived property (thinning) of a composite mechanism (mark draw, ByMark kernel, two stations), against formulas that never mention marks at all.","title":"The check: thinning"},{"category":"section","location":"queues/marks/#Redrawing-marks-en-route","page":"Marks: classes of jobs","text":"A mark is stamped at birth, but some models want a fresh value at each visit: a request that arrives at its next station with a new size, a re-entrant flow whose every pass draws its own work. The remark keyword of station! declares laws that are redrawn when a job is deposited into the station from outside — same names replace the job's marks, new names extend them:\n\nstation!(net, :hop2;\n         service = Law(:Dirac, value = Mark(:size)),\n         remark = (size = Law(:Exponential, scale = Const(0.25)),))\n\nThe redraw happens before anything at the station reads marks, so an ordered discipline files the job by its new values and the service law sees them. Three conventions:\n\nPre-redraw reads. Every remark law is evaluated against the job's marks before the redraw, and only then do the drawn values merge — so remark = (a = Law(:Dirac, value = Mark(:b)), b = Law(:Dirac, value = Mark(:a))) swaps a and b.\nOnce per deposit from outside. Moves within the station — a preempted job returning to the buffer, a blocked transfer finally admitted — redraw nothing; the blocked transfer drew at the deposit that blocked it.\nRecorded like any mark. Remark draws ride the depositing firing's draw list in the record, so replay reproduces them, and the compile-time census knows where they exist: a law may read a remark-only mark at the remark station or downstream of it, never upstream, and a remark law may not read station state (check C11).\n\nTwo exponential hops where the second redraws size, as above, is the sharpest self-check: each hop is an M/M/1 on its own size moments, and the tandem matches both closed forms only if the redraw really replaced the mark at the second hop.","title":"Redrawing marks en route"},{"category":"section","location":"queues/marks/#Where-this-leads","page":"Marks: classes of jobs","text":"Marks are the whole multi-class apparatus in one primitive. Stamp a class mark and per-class service scales, let laws read the marks, and you have a multi-class queueing network; add processor sharing from the M/G/1 page and probabilistic routing from the networks page, and you have everything the celebrated BCMP theorem needs. The BCMP case study in the Manual assembles exactly those pieces into a model of a high-performance computing cluster and checks the whole network — every station, both classes — against the product-form theory.","title":"Where this leads"},{"category":"section","location":"queues/kendall_mm1/#Kendall-notation-and-M/M/1","page":"Kendall notation and M/M/1","text":"Queueing models are named with a shorthand invented by David Kendall:\n\nA  S  c\n\nA — the arrival process,\nS — the service-time distribution,\nc — the number of servers.\n\nThe letters in the first two slots form a small alphabet. M stands for Markovian, or memoryless: a Poisson arrival stream, or exponential service times. D is deterministic: a fixed constant. G is general: any distribution. So:\n\nname meaning\nM/M/1 Poisson arrivals, exponential service, one server\nM/D/1 Poisson arrivals, constant service, one server\nM/G/1 Poisson arrivals, any service distribution, one server\nM/M/c Poisson arrivals, exponential service, c servers\n\nThis page is about the first row: M/M/1, the simplest queue there is, and the one with the cleanest theory. It is exactly the model the arrivals page built: a Poisson source, one FCFS (first come, first served) station, exponential service.\n\nusing Luxor, QueueDiagrams, LaTeXStrings # hide\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nb = buffer(Point(-40, 0), 160, 44; slots = 5, jobs = [0, 0]) # hide\ndiscipline(Point(-40, 0), 160, 44, \"M/M/1, FCFS\") # hide\ns = server(Point(90, 0), 26; inservice = 0, label = L\"\\mu\") # hide\nflow(Point(-260, 0), b.entry; label = L\"\\lambda\") # hide\nflow(b.exit, s.entry) # hide\nflow(s.exit, Point(200, 0)) # hide\nend 560 160 # hide\n\nWhy memorylessness matters: at any instant, the time to the next arrival and the remaining service of the job in progress are both exponential, regardless of history. The past tells you nothing, so the number of jobs in the system is all the state there is. That collapse is what makes every formula on this page exact.","title":"Kendall notation and M/M/1"},{"category":"section","location":"queues/kendall_mm1/#The-formulas","page":"Kendall notation and M/M/1","text":"For a stable M/M/1 queue (rho = lambdamu  1), the long-run averages are:\n\nL = fracrho1-rho\nqquad\nW = frac1mu - lambda\n\nwhere L is the mean number of jobs in the system (waiting or in service) and W is the mean time a job spends in the system. Note what happens as rho to 1: the denominator collapses and L explodes. A server at 50% load holds 1 job on average; at 90% load, 9 jobs; at 99% load, 99 jobs. This hockey stick is the central fact of capacity planning.\n\nThe two formulas are tied together by Little's law,\n\nL = lambda W\n\nwhich holds for any stable system, any discipline, any distributions — one of the very few free theorems in the subject.","title":"The formulas"},{"category":"section","location":"queues/kendall_mm1/#Simulating-it","page":"Kendall notation and M/M/1","text":"Build M/M/1 with lambda = 1, mu = 2, so rho = 05 and the theory says L = 1 and W = 1.\n\nusing Concourse\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :counter; discipline = FCFS(), servers = 1,\n         service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:counter))\nroute!(net, :counter, Always(:done))\nm = compile(net)\nθ = [1.0, 2.0]\nnothing # hide\n\nA single run gives one noisy estimate, so the convention throughout Concourse (and its test suite) is: run several independent replications, average them, and report the standard error (SE) of that average. An estimate is accepted when it lands within four standard errors of the exact value.\n\nusing Statistics\n\nfunction replicate(f, m, θ, horizon, nreps; seed0 = 1000)\n    vals = [f(simulate(m, θ, horizon; seed = seed0 + r)) for r in 1:nreps]\n    mean(vals), std(vals) / sqrt(nreps)\nend\n\nest, se = replicate(r -> time_average(number_in_system, m, r), m, θ, 2000.0, 16)\nprintln(\"measured L: \", round(est, digits = 3), \" ± \", round(se, digits = 3))\nprintln(\"theory:     rho/(1-rho) = 1.0\")\nprintln(\"|z| = \", round(abs(est - 1.0) / se, digits = 2), \"  (accept if ≤ 4)\")","title":"Simulating it"},{"category":"section","location":"queues/kendall_mm1/#Checking-Little's-law","page":"Kendall notation and M/M/1","text":"Little's law relates two measurements of different kinds: L is a time average of a state (how many are present), while W is a job average of a duration (how long each one stayed). The record supports both. One long run:\n\nrec = simulate(m, θ, 4000.0; seed = 42)\n\n# L: time-average number in system\nL = time_average(number_in_system, m, rec)\n\n# W: mean time in system, per job (arrival to departure, ids in arrival order)\narr = Float64[]; dep = Dict{Int,Float64}()\nfor (k, t) in zip(rec.key, rec.time)\n    k[1] == :arrival && push!(arr, t)\n    k[1] == :service && (dep[Int(k[3])] = t)\nend\nW = mean(dep[j] - arr[j] for j in eachindex(arr) if haskey(dep, j))\n\n# lambda: measured arrival rate\nλ̂ = length(arr) / rec.horizon\n\nprintln(\"L        = \", round(L, digits = 3))\nprintln(\"lambda*W = \", round(λ̂ * W, digits = 3))\nprintln(\"theory:  L = 1.0, W = 1.0, lambda = 1.0\")\n\nThe two sides agree to within the run's own noise, and both sit near the theory. Two completely different ways of reading the same record, one identity connecting them — this cross-check costs three lines and catches whole classes of measurement bugs, so use it freely.\n\nM relies on memorylessness twice. The next page drops it on the service side — M/G/1 — and the service distribution, not just its mean, starts to matter.","title":"Checking Little's law"},{"category":"section","location":"manual/gradients/#Gradient-estimation","page":"Gradient estimation","text":"Suppose the expected number in system over a morning is J(theta), where theta holds the arrival and service rates. Research questions are often about partial Jpartialtheta: how much would the queue shrink if service were ten percent faster? Derivatives drive optimization, sensitivity analysis, and calibration against data.\n\nThe naive approach — run at two nearby parameter values and subtract — is noisy, because the difference between the runs is small compared to the noise of each. Concourse supports four estimators that compute the derivative from simulation output directly, through ClockGradients.jl.\n\nOne design rule makes this possible: θ enters a simulation in exactly one place, when the model builds a clock's firing-time distribution. Nothing else reads θ. There is one door through which a parameter can affect anything, so an estimator only has to watch that door.","title":"Gradient estimation"},{"category":"section","location":"manual/gradients/#Binding-a-model-to-the-contract","page":"Gradient estimation","text":"ClockGradients works against a model contract, not against Concourse specifically. A ReplayModel binds a compiled QueueGSMP to that contract. There are two bindings:\n\nreplay_model(m, rec) binds one record's auxiliary draws to the model, so replaying the trajectory reproduces every mark and routing draw. Use it for the record-based estimators, score and IPA.\nlive_model(m) is the off-record binding, used by the clone-based estimators that run past any record. A model whose firings consume auxiliary draws fails loudly under this binding rather than replaying stale values.\n\nClockGradients.gradient_record(rm, rec, θ) ingests a MarkedRecord into the gradient machinery, rebuilding the clock histories the estimators differentiate.","title":"Binding a model to the contract"},{"category":"section","location":"manual/gradients/#The-four-estimators","page":"Gradient estimation","text":"Score function. Replay the record and compute how much more or less likely the same trajectory would have been at a slightly different θ. Multiply that sensitivity by the observed outcome and average over runs. This works for almost any model and any statistic. Its weakness is noise: the estimates have high variance.\n\nPathwise, also called IPA (infinitesimal perturbation analysis). Keep the order of firings fixed, and ask how each firing time would shift if θ moved a little. Slide the times, recompute the statistic, and read off the slope. This has low noise. Its weakness is blindness: if changing θ would change which clock wins a race, IPA does not see that effect, and for statistics that jump when the order changes, IPA is simply wrong.\n\nBranching. Run the simulation live. At a firing, copy the whole running world and force a different clock to win in the copy. Compare the copy's outcome with the original's. This directly measures the effect of order changes, which is exactly what IPA misses. Its weakness is cost: every branch point spawns clones.\n\nSmoothed perturbation analysis, SPA. A compromise. Take the IPA estimate, then add a correction for the near-ties: moments where two clocks almost fired together, so a small change in θ could swap their order. The correction weighs each near-tie by how likely the swap was and by how much the statistic would jump if it happened, estimated with one coupled clone pair. It needs far fewer clones than branching.","title":"The four estimators"},{"category":"section","location":"manual/gradients/#Score-and-IPA-on-a-record","page":"Gradient estimation","text":"The test statistic is the integrated occupancy int_0^H N(t)dt over H = 50 for an M/M/1 queue, and the check is against common-random-number finite differences. Both estimators read the same records.\n\nusing Concourse\nusing Statistics\nimport ClockGradients\nusing ClockGradients: IntegratedOccupancy\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :counter; service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:counter))\nroute!(net, :counter, Always(:done))\nm = compile(net)\n\nθ = [1.0, 2.0]\nH = 50.0\nR = 200\nintN(rec) = rec.horizon * time_average(number_in_system, m, rec)\nfn = IntegratedOccupancy(number_in_system)\n\nfs = zeros(R); S = zeros(2, R); G = zeros(2, R)\nfor r in 1:R\n    rec = simulate(m, θ, H; seed = 100 + r)\n    rm = replay_model(m, rec)\n    grec = ClockGradients.gradient_record(rm, rec, θ)\n    fs[r] = intN(rec)\n    S[:, r] = ClockGradients.score_gradient(rm, θ, grec)\n    G[:, r] = ClockGradients.ipa_gradient(rm, θ, grec, fn)\nend\n\n# Score estimator: covariance of the outcome with the score.\nfbar = mean(fs)\nscore_est = [mean((fs .- fbar) .* S[j, :]) for j in 1:2]\nscore_se  = [std((fs .- fbar) .* S[j, :]) / sqrt(R) for j in 1:2]\n# IPA estimator: a plain mean over replications.\nipa_est = [mean(G[j, :]) for j in 1:2]\nipa_se  = [std(G[j, :]) / sqrt(R) for j in 1:2]\nnothing # hide\n\nThe finite-difference baseline runs pairs of simulations at theta_j pm delta with shared seeds:\n\nδ = 0.05\nfd_est = zeros(2); fd_se = zeros(2)\nfor j in 1:2\n    θp = copy(θ); θp[j] += δ\n    θm = copy(θ); θm[j] -= δ\n    diffs = [(intN(simulate(m, θp, H; seed = 100 + r)) -\n              intN(simulate(m, θm, H; seed = 100 + r))) / (2δ) for r in 1:R]\n    fd_est[j] = mean(diffs); fd_se[j] = std(diffs) / sqrt(R)\nend\n\nusing Printf\nfor (name, est, se) in ((\"score\", score_est, score_se),\n                        (\"IPA  \", ipa_est, ipa_se),\n                        (\"FD   \", fd_est, fd_se))\n    @printf(\"%s  ∂λ = %7.1f ± %4.1f   ∂μ = %7.1f ± %4.1f\\n\",\n            name, est[1], se[1], est[2], se[2])\nend\n\nAll three agree within statistical error (the repository convention is four standard errors). Notice the standard errors: the score estimator's are several times larger than IPA's on the same 200 records. That is the trade in practice — score is general, IPA is quiet.","title":"Score and IPA on a record"},{"category":"section","location":"manual/gradients/#Branching,-on-a-live-world","page":"Gradient estimation","text":"The branching estimator drives a branchable world rather than a record. Pass it a world factory, the functional as a function of the terminal state, and a replication budget.\n\nT = 5.0\nres = ClockGradients.branching_gradient(() -> branch_world(m, θ; seed = 71), θ,\n                                        st -> Float64(number_in_system(st));\n                                        nreps = 600, horizon = T,\n                                        seed = 11, branch_rng_seed = 12)\nround.(res.estimate, digits = 2), round.(res.stderr, digits = 2), round(res.clones_per_rep, digits = 1)\n\nThe finite-difference check on the same terminal statistic, N(T) at T = 5:\n\nterminalN(θv, seed) = begin\n    rec = simulate(m, θv, T; seed)\n    isempty(rec.key) ? 0.0 : Float64(number_in_system(replay(m, rec)[end]))\nend\nfdt_est = zeros(2); fdt_se = zeros(2)\nfor j in 1:2\n    θp = copy(θ); θp[j] += 0.1\n    θm = copy(θ); θm[j] -= 0.1\n    diffs = [(terminalN(θp, 7000 + r) - terminalN(θm, 7000 + r)) / 0.2 for r in 1:3000]\n    fdt_est[j] = mean(diffs); fdt_se[j] = std(diffs) / sqrt(3000)\nend\nround.(fdt_est, digits = 2), round.(fdt_se, digits = 2)","title":"Branching, on a live world"},{"category":"section","location":"manual/gradients/#SPA,-on-a-live-world-plus-a-pure-model","page":"Gradient estimation","text":"spa_gradient takes both a world factory and the pure model binding — the world simulates, and the pure live_model twin replays clone jumps at near-ties.\n\nspa = ClockGradients.spa_gradient(() -> branch_world(m, θ; seed = 77),\n                                  live_model(m), θ,\n                                  IntegratedOccupancy(number_in_system);\n                                  nreps = 600, horizon = T, seed = 15)\nround.(spa.estimate, digits = 2), round.(spa.stderr, digits = 2)\n\nAnd its finite-difference check on the integrated occupancy over 0 5:\n\nfdo_est = zeros(2); fdo_se = zeros(2)\nfor j in 1:2\n    θp = copy(θ); θp[j] += 0.1\n    θm = copy(θ); θm[j] -= 0.1\n    diffs = [(intN(simulate(m, θp, T; seed = 7000 + r)) -\n              intN(simulate(m, θm, T; seed = 7000 + r))) / 0.2 for r in 1:3000]\n    fdo_est[j] = mean(diffs); fdo_se[j] = std(diffs) / sqrt(3000)\nend\nround.(fdo_est, digits = 2), round.(fdo_se, digits = 2)\n\nSPA's validity conditions are real requirements, not fine print. It needs clock families whose replay carries derivative information (Exponential, Weibull, LogNormal), a fire that is pure, and no mid-flight re-declaration of an enabled clock's distribution. A record that contains re-declared clocks — which is what processor sharing produces — makes spa_gradient throw rather than return a biased number.","title":"SPA, on a live world plus a pure model"},{"category":"section","location":"manual/gradients/#An-honest-caveat:-IPA-fails-on-processor-sharing","page":"Gradient estimation","text":"Under processor sharing (PS), every job at a station is served at once, each at a fraction of the server's speed. Each arrival or departure changes that fraction, so the model re-declares the law of every resident service clock. Each re-declaration starts a new segment in the clock's history.\n\nThe obvious hope was that IPA would extend to these multi-segment records. It does not. The experiment that settled it (test file test/test_ps.jl, translated in Design questions and outcomes) found frozen-order IPA over PS records biased under both couplings — both ways of realizing a re-declaration, carrying the retained draw or redrawing conditioned on age. A sharpened version of the experiment ran a 64-server PS station at a load where it never actually slows anyone down, so its law equals plain first-come-first-served (FCFS) — and IPA was unbiased on the segment-free FCFS records and biased on the multi-segment PS records of the same dynamics. The bias lives in the record's segment representation, not in the queue.\n\nThe demonstration is cheap enough to run here. IPA versus finite differences for partialpartialmu of the integrated occupancy of an M/M/1-PS queue:\n\nnetps = QueueNetwork(param_names = (:lambda, :mu))\nsource!(netps, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(netps, :cpu; discipline = ProcessorSharing(), servers = 1,\n         service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(netps, :done)\nroute!(netps, :arrive, Always(:cpu))\nroute!(netps, :cpu, Always(:done))\nmps = compile(netps)\n\nRps = 150\nintNps(rec) = rec.horizon * time_average(number_in_system, mps, rec)\nfps = zeros(Rps); Sps = zeros(2, Rps); Gps = zeros(2, Rps)\nfor r in 1:Rps\n    rec = simulate(mps, θ, H; seed = 700 + r)\n    rm = replay_model(mps, rec)\n    grec = ClockGradients.gradient_record(rm, rec, θ)\n    fps[r] = intNps(rec)\n    Sps[:, r] = ClockGradients.score_gradient(rm, θ, grec)\n    Gps[:, r] = ClockGradients.ipa_gradient(rm, θ, grec, fn)\nend\nfpsbar = mean(fps)\nps_score = mean((fps .- fpsbar) .* Sps[2, :])\nps_score_se = std((fps .- fpsbar) .* Sps[2, :]) / sqrt(Rps)\nps_ipa = mean(Gps[2, :]); ps_ipa_se = std(Gps[2, :]) / sqrt(Rps)\n\nθp = copy(θ); θp[2] += δ\nθm = copy(θ); θm[2] -= δ\ndiffs = [(intNps(simulate(mps, θp, H; seed = 700 + r)) -\n          intNps(simulate(mps, θm, H; seed = 700 + r))) / (2δ) for r in 1:Rps]\nps_fd = mean(diffs); ps_fd_se = std(diffs) / sqrt(Rps)\n\n@printf(\"IPA    ∂μ = %6.1f ± %4.1f\\n\", ps_ipa, ps_ipa_se)\n@printf(\"score  ∂μ = %6.1f ± %4.1f\\n\", ps_score, ps_score_se)\n@printf(\"FD     ∂μ = %6.1f ± %4.1f\\n\", ps_fd, ps_fd_se)\n@printf(\"IPA is %.0f combined standard errors from FD\\n\",\n        abs(ps_ipa - ps_fd) / sqrt(ps_ipa_se^2 + ps_fd_se^2))\n\nThe IPA estimate is confidently, precisely wrong — a small standard error around a badly biased value. The score estimator on the same records agrees with finite differences. On processor-sharing records, the valid estimators today are score and branching. SPA refuses the records; IPA returns a biased answer if you force it, so do not.","title":"An honest caveat: IPA fails on processor sharing"},{"category":"section","location":"manual/gradients/#A-second-caveat:-IPA-is-blind-to-who-wins-a-race","page":"Gradient estimation","text":"Sibling cancellation — racing forks, join! with need < parts and a cancel policy — is the purest form of the order-dependence IPA cannot see. Which sibling wins the race is the event order: perturb theta enough to swap two finish times and a different sibling merges, different work is canceled, and any statistic that notices the winner's identity — per-branch throughput, canceled-work totals, a latency read off which replica answered — jumps by a finite amount. Unlike processor sharing, the records themselves are harmless: cancellation re-declares no clocks, so IPA on a racing record is not structurally biased, and it remains fine for statistics genuinely insensitive to the identity switch. That insensitivity is documented, not certified — branch_world does not refuse racing models, and the claim is yours to make per statistic. The score estimator is valid on racing records; SPA's swap correction is exactly shaped for the winner-identity jump, and racing records do not trip spa_gradient's multi-segment refusal (segment-free, unlike PS). The estimator-validity table records the row.","title":"A second caveat: IPA is blind to who wins a race"},{"category":"section","location":"manual/gradients/#Which-estimator-when","page":"Gradient estimation","text":"Estimator Needs Variance Order changes PS records\nScore records high correct valid\nIPA records low blind biased — do not use\nBranching branchable world moderate correct valid\nSPA world + pure model low corrected refused (throws)\n\nStart with IPA when your statistic is a smooth path functional and the model has no re-declared clocks. Use score as the general fallback and as a cross-check. Reach for branching or SPA when the statistic responds to event order — counts, thresholds, first passages — and read the SPA page for when SPA's conditions hold.","title":"Which estimator when"},{"category":"section","location":"manual/bcmp_hpc/#A-BCMP-model-of-a-high-performance-computing-cluster","page":"A BCMP model of a high-performance computing cluster","text":"This page walks through examples/bcmp_hpc.jl: a multi-class queueing network model of an HPC cluster, checked against the exact theory of BCMP networks, with two experiments that show what the theory promises and where its promises stop.","title":"A BCMP model of a high-performance computing cluster"},{"category":"section","location":"manual/bcmp_hpc/#What-a-BCMP-network-is","page":"A BCMP model of a high-performance computing cluster","text":"In 1975, Baskett, Chandy, Muntz, and Palacios proved a remarkable theorem. Take a network of queues. Let jobs belong to classes, move between stations according to probabilities, and demand different amounts of service by class. Then, if every station is one of four types, the stationary distribution of the whole network factorizes: the network behaves, in the long run, as if its stations were independent queues, each with a simple closed-form distribution. This is called a product-form solution, and the four station types are:\n\nFCFS with exponential service. One or more servers, first come first served. The price of this type: every class must have the same exponential service distribution. First come first served makes waiting jobs interchangeable, so the theory only survives if their remaining-work distributions are interchangeable too — which forces the memoryless law.\nProcessor sharing. Everyone at the station is served at once, each at an equal fraction of the server's speed. Service laws may be general and class-dependent.\nInfinite server (a delay station). Every arrival gets its own server; nobody waits. Laws general and class-dependent.\nLCFS preemptive-resume. Last come first served, with preemption. Laws general and class-dependent.\n\nThe asymmetry between type 1 and the others is the interesting part. Types 2–4 have a property called insensitivity: their occupancy distribution depends on the service law only through its mean. You can replace an exponential service with a Gamma, a deterministic value, or a heavy-tailed law of the same mean, and the station's long-run occupancy does not move. Type 1 has no such property, and the second experiment below shows both facts side by side.","title":"What a BCMP network is"},{"category":"section","location":"manual/bcmp_hpc/#The-cluster","page":"A BCMP model of a high-performance computing cluster","text":"using Luxor, QueueDiagrams, LaTeXStrings # hide\n@drawsvg begin # hide\n    background(\"white\") # hide\n    fontface(\"Helvetica\") # hide\n    yrow = -50 # hide\n    login = sharedserver(Point(-300, yrow), 30; jobs = [1, 2], label = \"login (PS)\") # hide\n    bc = Point(-175, yrow) # hide\n    b = buffer(bc, 100, 34; slots = 4, jobs = [2, 1]) # hide\n    discipline(bc, 100, 34, \"sched (FCFS)\") # hide\n    sv = server(Point(-95, yrow), 17; inservice = 1) # hide\n    compute = sharedserver(Point(55, yrow), 48; # hide\n                           jobs = [1, 2, 1, 2, 2, 1, 2, 1], label = \"compute (64 nodes)\") # hide\n    fs = sharedserver(Point(55, 105), 26; jobs = [2, 1], label = \"fs (PS)\") # hide\n    flow(Point(-430, yrow - 22), Point(login.entry.x - 4, yrow - 8); label = L\"\\lambda_{debug}\") # hide\n    flow(Point(-430, yrow + 22), Point(login.entry.x - 4, yrow + 8); label = L\"\\lambda_{prod}\") # hide\n    flow(login.exit, b.entry) # hide\n    flow(b.exit, sv.entry) # hide\n    flow(sv.exit, compute.entry) # hide\n    flow(compute.exit, Point(250, yrow); label = \"0.25\") # hide\n    fontsize(13); text(\"done\", Point(262, yrow + 4)) # hide\n    flow(Point(80, yrow + 44), Point(80, 82)) # hide\n    flow(Point(30, 82), Point(30, yrow + 44)) # hide\n    fontsize(13) # hide\n    text(\"0.75\", Point(95, 45), halign = :left) # hide\n    text(\"checkpoint\", Point(16, 45), halign = :right) # hide\nend 900 300 # hide\n\nThe network: two job classes (debug in blue, production in orange) pass through a processor-sharing login node, a first-come-first-served scheduler, a 64-node compute pool, and a processor-sharing filesystem that jobs cycle through while checkpointing.\n\nTwo classes of work arrive at a cluster:\n\nclass arrivals login compute burst checkpoint write\ndebug 2.0 /min 0.05 min 1.0 min 0.04 min\nproduction 0.5 /min 0.10 min 6.0 min 0.20 min\n\nEvery job passes through four resources:\n\nlogin — the login node, where users prepare a submission. Modeled as processor sharing (interactive shells share the CPU), with a class-dependent Gamma service law. BCMP type 2.\nsched — the scheduler, one server, FCFS, exponential service with the same mean for both classes (0.15 min). BCMP type 1, and the class-independence is required, not a simplification we chose.\ncompute — 64 nodes, one job per node. At our loads a job essentially never waits, so this is a delay station. BCMP type 3.\nfs — the parallel filesystem, modeled as processor sharing. BCMP type 2.\n\nAfter each compute burst, a job checkpoints to the filesystem with probability p = 0.75 and then computes again; with probability 0.25 it leaves. So a job makes a geometric number of compute visits — 4 on average, with 3 filesystem visits — and the network has a cycle in it, which the theory handles without complaint.\n\nClass-dependent service travels as marks: each source stamps its jobs with its class's service scales, and the station laws read the marks. This is the same recorded-randomness machinery the rest of Concourse uses — every mark and every routing coin flip is drawn from a keyed stream and written into the record, so any run replays exactly.\n\nsource!(net, :prod_in;\n        interarrival = Law(:Exponential, scale = inv(Param(:lambda_prod))),\n        mark = MarkLaw(class = Law(:Dirac, value = Const(1.0)),\n                       login_s = Law(:Dirac, value = Const(0.10)),\n                       comp_s  = Law(:Dirac, value = Const(6.0)),\n                       io_s    = Law(:Dirac, value = Const(0.20))))\nstation!(net, :login; discipline = ProcessorSharing(), servers = 1,\n         service = Law(:Gamma, shape = Const(2.0),\n                       scale = Const(0.5) * Mark(:login_s)))\nstation!(net, :sched; discipline = FCFS(), servers = 1,\n         service = Law(:Exponential, scale = Const(0.15)))\nstation!(net, :compute; discipline = FCFS(), servers = 64,\n         service = Law(:Exponential, scale = Mark(:comp_s)))\nstation!(net, :fs; discipline = ProcessorSharing(), servers = 1,\n         service = Law(:Exponential, scale = Mark(:io_s)))\nroute!(net, :compute, Probabilistic(:fs => 0.75, :done => 0.25))\nroute!(net, :fs, Always(:compute))","title":"The cluster"},{"category":"section","location":"manual/bcmp_hpc/#The-closed-forms","page":"A BCMP model of a high-performance computing cluster","text":"Product form makes the predictions elementary. First solve the traffic equations — each station's total arrival rate, counting revisits. The checkpoint loop turns one external arrival into a geometric number of internal visits:\n\n@drawsvg begin # hide\n    background(\"white\") # hide\n    fontface(\"Helvetica\") # hide\n    stations = [(\"login\", 1), (\"sched\", 1), (\"compute\", 4), (\"fs\", 3)] # hide\n    for (i, (name, visits)) in enumerate(stations) # hide\n        x = -240 + (i - 1) * 160 # hide\n        server(Point(x, 0), 24) # hide\n        fontsize(14) # hide\n        sethue(\"black\") # hide\n        text(name, Point(x, -38), halign = :center) # hide\n        text(\"visits: $visits\", Point(x, -56), halign = :center) # hide\n        i < 4 && flow(Point(x + 26, 0), Point(x + 134, 0)) # hide\n    end # hide\n    feedback(Point(240, 26), Point(80, 30); drop = 85) # hide\n    fontsize(14); sethue(\"black\") # hide\n    text(\"p = 0.75\", Point(160, 128), halign = :center) # hide\nend 700 260 # hide\n\nWith per-class external rates λ_c and those mean visit counts (login 1, sched 1, compute 4, fs 3), each station's load is\n\nρ = Σ_c λ_c · (visits) · (mean service of class c).\n\nThen each station's mean occupancy is a one-liner:\n\nFCFS and PS: L = ρ / (1 − ρ)\ndelay: L = ρ (the occupancy is Poisson).\n\nPer-class response times follow by summing sojourns: a PS visit costs E[S_c] / (1 − ρ), an FCFS visit E[S] / (1 − ρ), a delay visit E[S_c], and Little's law N_c = λ_c · T_c cross-checks the class decomposition against a completely different measurement.","title":"The closed forms"},{"category":"section","location":"manual/bcmp_hpc/#Experiment-1:-the-simulation-against-the-theory","page":"A BCMP model of a high-performance computing cluster","text":"Sixteen replications of 2000 simulated minutes. Every station and both class totals agree with the product form within the repository's four-standard-error convention (the test/test_bcmp.jl charter test pins this claim, with the debug-mode membership oracle on):\n\nquantity simulated product form |z|\nlogin 0.177 ± 0.001 0.176 0.2\nsched 0.604 ± 0.006 0.600 0.6\ncompute 19.981 ± 0.142 20.000 0.1\nfs 1.182 ± 0.015 1.174 0.5\ndebug jobs in system 9.099 ± 0.033 9.119 0.6\nproduction jobs in system 12.645 ± 0.101 12.831 1.8\n\nThe last two rows are the per-class Little's-law check: the measured class populations against λc · Tc with T_c computed from per-visit sojourns (debug 4.56 min, production 25.66 min). Two completely different routes to the same numbers.","title":"Experiment 1: the simulation against the theory"},{"category":"section","location":"manual/bcmp_hpc/#Experiment-2:-where-the-cluster-saturates","page":"A BCMP model of a high-performance computing cluster","text":"Hold debug arrivals fixed and raise the production rate from 0.1 to 1.2 jobs per minute.\n\n(Image: bottleneck sweep)\n\nThe compute pool holds by far the most jobs — about 20 at the base point — but it is a delay station: its occupancy grows linearly, and with 64 nodes it is nowhere near trouble. The danger is the quiet curve below it: the filesystem's load crosses 0.9 as λ_prod approaches 1.2, and its occupancy turns up the ρ/(1−ρ) wall. The model says the cluster's first failure mode is filesystem contention from checkpoint traffic, not compute capacity — the kind of conclusion HPC operators pay for, read directly off two closed-form curves that the simulation reproduces.\n\n(The last simulated point sits below its curve with a wide error bar. That is what near-saturation looks like in a finite window: at ρ = 0.96 the queue's relaxation time exceeds the 1000-minute horizon, so the time-average has not yet climbed to its stationary value. The theory curve is the infinite-horizon limit.)","title":"Experiment 2: where the cluster saturates"},{"category":"section","location":"manual/bcmp_hpc/#Experiment-3:-insensitivity,-and-the-FCFS-exception","page":"A BCMP model of a high-performance computing cluster","text":"Fix all means and change only the service distribution — coefficient of variation 2, 1, and 1/4 — first at the processor-sharing filesystem, then at the FCFS scheduler.\n\n(Image: insensitivity)\n\nThe filesystem's occupancy does not move (1.17, 1.14, 1.15 — all within noise of the product-form 1.174): that is insensitivity, the defining privilege of BCMP types 2–4, and the dashed product-form line runs through all three bars. The scheduler's occupancy moves with the variance (0.70, 0.60, 0.51 across CV² = 2, 1, 1/4) — more variable service, longer queue, exactly as Pollaczek–Khinchine intuition says — and with a non-exponential FCFS station the network as a whole has left the BCMP class. The two panels are the theorem's boundary, drawn by simulation.","title":"Experiment 3: insensitivity, and the FCFS exception"},{"category":"section","location":"manual/bcmp_hpc/#What-this-demonstrates-about-Concourse","page":"A BCMP model of a high-performance computing cluster","text":"Multi-class BCMP structure — classes, class-dependent laws, probabilistic routing, cyclic routes — assembles from the existing surface language (marks + kernels) with no new machinery.\nProcessor sharing, the discipline behind types 2's insensitivity, is the same PS implementation the charter tests against M/G/1-PS theory.\nEvery run is exactly replayable (marks and coin flips are recorded), and every demonstration run executes with the delta-fidelity oracle checking the sampler against the model at each firing.\nThe measurements are replay-based: occupancies are time averages read off replayed states, not counters bolted into the simulator.","title":"What this demonstrates about Concourse"},{"category":"section","location":"manual/bcmp_hpc/#Running-it","page":"A BCMP model of a high-performance computing cluster","text":"julia --project=examples examples/bcmp_hpc.jl\n\nRuntime is a few minutes; figures land in docs/figures/.","title":"Running it"},{"category":"section","location":"developer/model_contract/#The-model-contract","page":"The model contract","text":"This page translates notes/model_definition.tex, the normative statement of what a Concourse model is. It amends the three-layer design described in Architecture; where the two disagree, this one wins.\n\nThe note makes four amendments, labeled A1–A4, and states each so that a test could refute it. The refutation tests are collected in the falsification charter at the end of this page.","title":"The model contract"},{"category":"section","location":"developer/model_contract/#A1:-clock-bookkeeping-is-model-state","page":"The model contract","text":"A generalized semi-Markov process (GSMP) has Markov state (s textclock readings). The original design put s in QueueState and left the clock readings — enabling times and accrued ages — inside the sampler. That split fails twice:\n\nIt breaks fire's purity for age-reading disciplines. Shortest remaining processing time (SRPT) compares remaining sizes, which are size marks minus ages. :resume re-enabling needs the banked age. If ages live only in the sampler, the model must query the sampler mid-firing, which inverts the layering.\nIt makes replay ambiguous for time-varying laws. The score estimator's replay evaluates each clock's distribution at states later than that clock's enabling. A time-varying law must be anchored at the clock's enabling time. Only a per-clock enabling-time table inside the state gives the same answer no matter which state the replay asks from.\n\nSo QueueState carries two dictionaries: te, the enabling time of every enabled clock, and bank, the accrued age of every disabled :resume clock. A clock entering the enabled set at time t gets tek = t - bankk; a :resume clock leaving without firing banks t - tek; a :fresh clock's bank is discarded. A clock's age at any state is readable from the state alone — no model code ever calls the sampler's age query.\n\nTwo consequences. First, state equality and record replay now compare the clock bookkeeping too, so a divergence between the model's ages and the sampler's is detectable rather than silent: the runtime invariant is that st.te equals the sampler's enabling times after every firing. Second, the state stays plain data that copies, hashes, and diffs.\n\nContract delta CD-1. fire must stamp the state's time and the new te entries, so it needs the firing time. The ClockGradients.jl contract passed only fire(model, state, key); the amendment adds a four-argument form fire(model, state, key, t) with the three-argument form as a fallback, so existing time-free models are untouched. The alternative — keeping fire time-free and expressing every law in age coordinates — was rejected because it silently forbids wall-clock-inhomogeneous laws, which are a stated goal.","title":"A1: clock bookkeeping is model state"},{"category":"section","location":"developer/model_contract/#A2:-the-clock-family-set-is-open","page":"The model contract","text":"The original design claimed two clock families (:arrival, :service) suffice. The claim holds for the initial feature set and fails one step outside it: reneging — a patience clock on a job that is waiting, not in service — is bread-and-butter queueing and is a third family. Server breakdowns would be a fourth. The claim was demoted from an architectural assumption to a statement about the initial feature set, and the architecture was changed to make adding a family cheap.\n\nA clock family is a registry entry: who is enabled (enumerate), what law each clock carries (law), and what firing means (fire). The enabled function is the concatenation of each family's enumeration in registry order, which keeps the deterministic ordering the contract requires. The interpreter, the record fold, the checker, and the estimators never mention a family by name. What stays closed: a family's fire still works through the shared deposit!/settle! vocabulary, so disciplines and routing compose with new families instead of each family reinventing station semantics.\n\nThe acid test is charter item F2: adding :patience must touch the registry and the surface constructor and nothing else. Running that test falsified the first delta-emission mechanism and forced a repair — see the D1 refinement.","title":"A2: the clock-family set is open"},{"category":"section","location":"developer/model_contract/#A3:-every-function-valued-field-is-a-combinator-value","page":"The model contract","text":"The IR's advertised virtue is that it is data — printable, diffable, checkable. Plain-function fields for laws, marks, and kernels quietly forfeit that: closures cannot be hashed, compared, or inspected, and the checks that justify the IR need to know what a law reads and whether it depends on theta.\n\nThe fix is one small expression algebra. Param(:mu) reads a parameter by name, Mark(:size) reads a job mark, Enab() reads the enabling time, Const(2.0) is a constant, and arithmetic is lifted over these. A law is a distribution family applied to such expressions:\n\nservice = Law(Exponential, scale = Mark(:size) * inv(Param(:mu_short)))\n\nPredicates like reads_θ, reads_marks, and reads_time are derived by walking the tree, not declared. Mark typing (check C2) becomes a dataflow check over values, and the randomness census (C6) and the estimator validity table are computed, not asserted.\n\nThere is one escape hatch, Opaque, which wraps an arbitrary function but must declare its reads. The checker trusts the declaration, marks every conclusion resting on it as unverified, and degrades the estimator tiers it cannot certify. A closure with no declaration is a construction-time error. Charter item F3 measures whether the algebra is rich enough that the hatch stays shut for the standard models.","title":"A3: every function-valued field is a combinator value"},{"category":"section","location":"developer/model_contract/#A4:-no-hidden-randomness","page":"The model contract","text":"Two kinds of randomness are not clocks: marks drawn at arrival, and random choices (probabilistic routing, random-order selection). A4 makes the rule \"every draw is a clock, a recorded mark, or a recorded choice\" a signature-level fact: any function that may draw receives an explicit draw source as an argument. In live simulation the draw source wraps keyed random streams, keyed by (clock, purpose) rather than by position in a global call sequence, and every drawn value is appended to the record. In replay, the draw source is the recorded sequence, and drawing returns the recorded values in consumption order.\n\nThis is exactly the conditioning that makes fire deterministic given the record, and it is what the likelihood's mark and routing terms require.","title":"A4: no hidden randomness"},{"category":"section","location":"developer/model_contract/#The-amended-contract-surface","page":"The model contract","text":"Concourse implements the ClockGradients model contract. The working surface is five functions over (QueueGSMP, QueueState):\n\nFunction What it does\ninitial_state(m) empty places; te and bank empty; time zero\nenabled(m, st) the enabled clock keys, in deterministic registry order\nclock_distribution(m, θ, k, st) the clock's declared law, anchored from st.te\nfire_changes(m, st, k, t) the new state plus the clock deltas the firing caused\nstates_equal(a, b) value equality, including the clock bookkeeping\n\nfire_changes is the delta-emitting form of fire chosen by decision D1 (see The event loop); both carry the CD-1 time argument. Replay determinism is conditional on the recorded draws (A4), and states_equal comparing te and bank is what makes divergences visible (A1). Concourse is the second independent implementation of this contract, beside the ChronoSim.jl extension.","title":"The amended contract surface"},{"category":"section","location":"developer/model_contract/#The-falsification-charter","page":"The model contract","text":"Every design claim above is paired with a concrete test that would refute it. A failed test is a design discovery, not a bug to paper over. Oracles use the 4-standard-error convention against closed forms (M/M/1, the SITA farm as two independent M/G/1 queues, Erlang-A for reneging). The charter continues with F9–F14 in The event loop.\n\nItem Claim Status Test\nF1 A1 suffices: no model code needs sampler introspection. SRPT and preempt-resume match oracles with ages read from state alone. Tested, holding test/test_srpt.jl\nF2 A2 openness: a new clock family costs one registry entry. Reneging (M/M/1+M vs Erlang-A) touched only the registry and surface fields — after the D1 repair it forced. Tested, holding test/test_reneging.jl\nF3 A3 coverage: the expression algebra covers the standard models with zero Opaque escapes. Tested, holding test/test_splits.jl\nF4 A4 determinism: folding the marked record reproduces the live trajectory at every index, including te and bank. Tested, holding test/test_replay.jl\nF5 CD-1 sufficiency: the four-argument fire is the only ClockGradients change queueing needs. Score and IPA on a time-varying-arrival M(t)/M/1 match finite differences. Tested, holding test/test_gradients.jl\nF6 Cascade totality: under acyclic blocking, cascades terminate deterministically. Tested, holding test/test_blocking.jl\nF7 The initial family set {:arrival, :service} suffices for the five phase-1 models. Fork–join sharpened this: forks and joins are clock-free station kinds, not a new family. Tested, holding test/test_forkjoin.jl\nF8 The speed-free compilation of processor sharing equals an explicit-speed reference. M/M/1-PS matches rho(1-rho); M/G/1-PS shows insensitivity. Tested, holding test/test_ps.jl\n\nReading the notation: IPA is infinitesimal perturbation analysis, the pathwise derivative estimator; the estimators are described in the Manual's gradients page. F2's structural half was a genuine falsification-and-repair, and F14 (in the event-loop charter) is an outright refutation — both are recorded, because recording what did not work is what the charter is for.","title":"The falsification charter"},{"category":"section","location":"manual/branching/#Branching-worlds-and-trajectory-splitting","page":"Branching worlds and trajectory splitting","text":"A record replays the past. A branchable world runs the future. It is a live simulation you can pause, copy, steer, and re-randomize. Trajectory splitting — \"what would have happened if a different event had won this race?\" — and the clone-based derivative estimators are both built on it.\n\nbranch_world wraps a compiled model in a ConcourseWorld, which implements the branchable-world protocol of ClockGradients.jl. The protocol is ten small functions, called verbs:\n\nVerb Meaning\nbranch_peek look at the next firing (time, clock) without committing it\nbranch_commit! let the peeked winner fire\nbranch_force! make a different clock fire at that time\nbranch_clone copy the whole running world\nbranch_rekey! give a world fresh randomness from a seed\nbranch_time the world's current time\nbranch_enabled_ages the enabled clocks and their ages\nbranch_clock_distribution a clock's current firing-time law\nbranch_state the current QueueState\nbranch_schedule the pending firing times, in order\n\nThe verbs live in ClockGradients, so call them qualified: ClockGradients.branch_peek(w) and so on.","title":"Branching worlds and trajectory splitting"},{"category":"section","location":"manual/branching/#Driving-a-world-by-hand","page":"Branching worlds and trajectory splitting","text":"The example model routes each arrival by a random size mark: small jobs go to one station, large jobs to another. Marks and random routing matter here, because they are exactly the draws a clone must handle carefully.\n\nusing Concourse\nimport ClockGradients\n\nnet = QueueNetwork(param_names = (:lambda, :mu_short, :mu_long))\nsource!(net, :arrive;\n        interarrival = Law(:Exponential, scale = inv(Param(:lambda))),\n        mark = MarkLaw(size = Law(:Exponential, scale = Const(1.0))))\nstation!(net, :short; service = Law(:Exponential, scale = inv(Param(:mu_short))))\nstation!(net, :long;  service = Law(:Exponential, scale = inv(Param(:mu_long))))\nsink!(net, :done)\nroute!(net, :arrive, ByMark(Mark(:size), [2.0], [:short, :long]))\nroute!(net, :short, Always(:done))\nroute!(net, :long, Always(:done))\nm = compile(net)\nθ = [1.2, 2.0, 2.0]\n\nw = branch_world(m, θ; seed = 5)\n\n# Advance a world n firings; return the keys that fired and the marks of\n# every job seen along the way (jobs vanish at the sink, so collect early).\nfunction advance!(w, n)\n    keys = Concourse.ClockKey[]\n    seen = Dict{Int64,NamedTuple}()\n    for _ in 1:n\n        pk = ClockGradients.branch_peek(w)\n        pk === nothing && break\n        ClockGradients.branch_commit!(w, pk[2], pk[1])\n        push!(keys, pk[2])\n        merge!(seen, w.state.jobs)\n    end\n    keys, seen\nend\n\nadvance!(w, 6)\nClockGradients.branch_time(w), number_in_system(w.state)","title":"Driving a world by hand"},{"category":"section","location":"manual/branching/#A-clone-continues-identically","page":"Branching worlds and trajectory splitting","text":"branch_clone copies everything: the state, the pending clocks with their reserved firing times, and the internal states of the auxiliary random streams. With no further action, the clone's future is the original's future, firing for firing — including the sizes of jobs that have not yet arrived.\n\nc = ClockGradients.branch_clone(w)\nkw, jw = advance!(w, 20)\nkc, jc = advance!(c, 20)\nkw == kc, states_equal(w.state, c.state), jw == jc","title":"A clone continues identically"},{"category":"section","location":"manual/branching/#Rekeying-decouples-a-clone","page":"Branching worlds and trajectory splitting","text":"branch_rekey! re-seeds a world's randomness from a given seed: the clock streams and the auxiliary streams both. After a rekey, the clone's future is a fresh, independent draw.\n\nw2 = branch_world(m, θ; seed = 5)\nadvance!(w2, 6)\nc2 = ClockGradients.branch_clone(w2)\nClockGradients.branch_rekey!(c2, 0xDECAF)\nk_orig, _ = advance!(w2, 20)\nk_new, _ = advance!(c2, 20)\nk_orig != k_new\n\nTwo clones rekeyed with the same seed share their randomness with each other while differing from the original. That controlled sharing is what makes the difference between two clones quiet: the common randomness cancels, and only the effect you injected remains.","title":"Rekeying decouples a clone"},{"category":"section","location":"manual/branching/#Splitting-a-trajectory","page":"Branching worlds and trajectory splitting","text":"Here is the full splitting idiom. Clone twice, rekey both clones with the same seed, then commit the peeked winner in one clone and force the runner-up in the other. From that moment the two trajectories disagree about event order. The difference between their outcomes measures the effect of that single decision, with everything else held common.\n\nw3 = branch_world(m, θ; seed = 5)\n# Advance until the coming race has at least two contenders.\nwhile length(ClockGradients.branch_enabled_ages(w3)) < 2\n    advance!(w3, 1)\nend\na = ClockGradients.branch_clone(w3)\nb = ClockGradients.branch_clone(w3)\nClockGradients.branch_rekey!(a, 0xA11CE)\nClockGradients.branch_rekey!(b, 0xA11CE)\n\n(tstar, winner) = ClockGradients.branch_peek(a)\nrunnerup = first(k for (k, _) in ClockGradients.branch_enabled_ages(b) if k != winner)\nClockGradients.branch_commit!(a, winner, tstar)\nClockGradients.branch_force!(b, runnerup, tstar)\n\nka, ja = advance!(a, 40)\nkb, jb = advance!(b, 40)\nka != kb   # the event orders genuinely diverged\n\nEven though the orders diverged, corresponding jobs still drew the same marks in both trajectories:\n\ncommon = intersect(keys(ja), keys(jb))\nlength(common) > 5, all(ja[j] == jb[j] for j in common)\n\nThat alignment is not luck. It is the auxiliary-draw discipline.","title":"Splitting a trajectory"},{"category":"section","location":"manual/branching/#The-rules-for-auxiliary-draws","page":"Branching worlds and trajectory splitting","text":"A clone runs forward, past the end of any record. Its future arrivals need new sizes; its future routing needs new coin flips. The rules that govern those draws are part of the world's contract:\n\nThe world owns its auxiliary streams. They sit beside the sampler's clock streams, keyed and separate, so adding a mark to a model never perturbs its firing times.\nA stream key is a stable identity, not a position. A mark or routing draw is keyed by the clock it belongs to — a source's arrival clock, a job's service clock — never by its position in a global call sequence. The k-th value of a source's mark stream is the k-th arrival of that source, in any same-seed world, whatever else fired in between. That is why the split above kept corresponding jobs' marks matched after the orders diverged.\nClones copy generator states. A coupled clone reproduces the original's future draws exactly, including draws for jobs not yet born.\nRekeys re-seed both families. branch_rekey! re-seeds the clock streams and the auxiliary streams. Re-seeding only the clocks would leave a clone replaying the original's future sizes and coin flips.\nParameter-dependent mark laws are refused. A mark law that reads θ would add its own derivative term to every estimator. Until that term is implemented, branch_world rejects such models at construction, loudly. Routing weights and random-order (SIRO) selection are θ-free by construction, so they are always admitted.\n\nThe refusal in action:\n\nnet2 = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net2, :arrive;\n        interarrival = Law(:Exponential, scale = inv(Param(:lambda))),\n        mark = MarkLaw(size = Law(:Exponential, scale = inv(Param(:mu)))))\nstation!(net2, :cpu; service = Law(:Dirac, value = Mark(:size)))\nsink!(net2, :done)\nroute!(net2, :arrive, Always(:cpu))\nroute!(net2, :cpu, Always(:done))\nmbad = compile(net2)\ntry\n    branch_world(mbad, [1.0, 2.0]; seed = 1)\ncatch err\n    println(err.msg)\nend","title":"The rules for auxiliary draws"},{"category":"section","location":"manual/branching/#Certifying-a-world","page":"Branching worlds and trajectory splitting","text":"ClockGradients ships a conformance checker that exercises the protocol obligations — peek repeatability, clone coupling, rekey independence, and the rest — against a world factory. It is the same certification the test suite runs.\n\nreport = ClockGradients.check_branchable(() -> branch_world(m, θ; seed = 44), θ;\n                                         nsteps = 20, seed = 0xFACE)\nreport.pass\n\nOne practical note: branch_world defaults to NextReactionMethod() as its sampler, because that method caches its next reservation. Peeking twice must return the same answer, and a sampler that redraws at every next would fail the certification honestly. See Choosing a sampler.\n\nThe clone-based derivative estimators — branching and smoothed perturbation analysis (SPA) — drive these same ten verbs. They are the subject of Gradient estimation. Splitting also composes with ordinary statistics; see the common-random-numbers section of Statistical analysis for a variance-reduction example built on branch_world.","title":"Certifying a world"},{"category":"section","location":"manual/rounds/#Round-based-token-service","page":"Round-based token service","text":"An LLM inference server does not serve requests one at a time: at every iteration it recomposes a batch, spends a token budget across the requests in it — a prefill chunk here, one decode token each there — and every request's remaining work shrinks by exactly what it was allocated. Requests persist across many iterations; the \"batch\" is a per-iteration allocation, not a group of jobs that enters and leaves together. Queueing theory calls this iteration-level batching, and Concourse expresses it with the rounds keyword of station!: a station that serves in synchronous rounds, with per-job integer work counters, one station-level clock per round, and a policy hook at every round boundary. Everything else — sources, routing, reneging, fork/join, records — keeps its usual meaning.\n\nstation!(net, :gpu;\n    rounds = Rounds(\n        policy   = Sarathi(budget = 512),\n        duration = Law(:Dirac, value = Const(11.28) + Const(35.47) * ceil(Mark(:tokens) / 128)),\n        work     = (:v_p, :v_d),\n    ))\n\nwork names the marks (produced upstream, by populate!, or by the station's own remark) that give a job's integer work profile, in phase order; they are snapshotted into counters at admission. The last phase is the decode phase of the shipped policies; every earlier phase is chunkable prefill.","title":"Round-based token service"},{"category":"section","location":"manual/rounds/#The-round-lifecycle","page":"Round-based token service","text":"A deposit files the job into the station's FCFS buffer like any other deposit. Then, whenever the station has no round running, the settle step asks the policy to plan one: plan_round(policy, view, draws) sees the waiting line and the active set through a read-only RoundView and returns a RoundPlan — or nothing to idle. A plan commits in three moves: evictions (active jobs return to the waiting line), admissions (waiting jobs move into the active set, their work marks snapshotted into counters), and per-active-job integer allocations, which the engine checks against 0 le textalloc le textremaining per phase. The plan's aggregates — tokens (the total allocation b), requests (the batch size k), and one sum per work phase — are frozen, and one station-level :round clock is enabled under the duration law, which reads those aggregates as pseudo-marks (and nothing else — check C13). The round then runs closed and non-preemptive: deposits during a round simply queue, and the next boundary is the clock's firing.\n\nAt the firing the allocations are credited — each active job's counters drop by exactly its allocation — and every job whose last phase hit zero routes onward on its own marks, like any departure. Then the station re-plans immediately, at the same instant, so back-to-back rounds chain with no gap. Work conservation is the policy's business, not the engine's: a policy that returns a plan with no useful allocation gets exactly the round it asked for.\n\nWhen the policy returns nothing the station idles with no clock at all, and the next deposit triggers the next planning attempt. One convention follows: the round grid is not a global slotted clock but re-anchors at the first deposit after an idle period. Both papers below analyze loaded regimes where the server never idles, so the convention is invisible there; it is documented because a lightly loaded round station genuinely has no \"slot 17 of the simulation\" — only rounds it actually ran.","title":"The round lifecycle"},{"category":"section","location":"manual/rounds/#The-station-shape-(check-C12)","page":"Round-based token service","text":"Round service composes with exactly the plain FCFS station shape: the non-preemptive :back-insert FCFS discipline, no batching (the policy owns batch formation), servers = 1 (the round clock is the server), and no service law (the duration lives in the Rounds config). The work marks must be produced upstream (the census checks it) and must be integers ge 0 at admission, checked at runtime. As with every static check, compile names the problem:\n\nusing Concourse\n\nnet = QueueNetwork(param_names = (:lambda,))\nsource!(net, :arrive;\n        interarrival = Law(:Exponential, scale = inv(Param(:lambda))),\n        mark = MarkLaw(w = Law(:Dirac, value = Const(1.0))))\nstation!(net, :gpu; discipline = LCFS(),\n         rounds = Rounds(policy = Sarathi(budget = 8),\n                         duration = Law(:Dirac, value = Const(1.0)),\n                         work = (:w,)))\nsink!(net, :done)\nroute!(net, :arrive, Always(:gpu))\nroute!(net, :gpu, Always(:done))\ntry\n    compile(net)\ncatch err\n    println(err.msg)\nend","title":"The station shape (check C12)"},{"category":"section","location":"manual/rounds/#The-policy-contract","page":"Round-based token service","text":"RoundPolicy is the first user-code hook inside the fire path, so its contract is stated bluntly: a policy is a pure function of (view, draws). All randomness flows through the draw source (via Concourse.draw!, recorded in the firing's draw list and replayed from it); no clocks, no wall time, no theta, no state beyond the view. The RoundView is built as a real barrier — copies and values, never the QueueState — so a policy can rank and count jobs but cannot reach the simulation. The contract is documented and tested rather than compile-checked, the Opaque law precedent: Julia cannot prove an arbitrary function pure, but replay equality and the debug membership oracle catch violations — the test suite includes a rigged impure policy caught exactly this way.\n\nWhat the policy sees and returns:\n\nRoundView: phases (the work mark names, in order), waiting (the buffer in FCFS order), active (the jobs in service, in admission order) — each entry a RoundJob with id, marks, and work, where work is remaining counters for an active job and the full profile for a waiting one.\nRoundPlan(admit, evict, alloc): waiting ids to activate, active ids to return to the line, and id => tokens-per-phase pairs over the post-admission active set (an absent job gets zero).","title":"The policy contract"},{"category":"section","location":"manual/rounds/#Waiting,-eviction,-cancellation","page":"Round-based token service","text":"Waiting jobs are ordinary waiting jobs: they keep patience clocks and renege as usual, racing the next round boundary exactly as reneging races batch formation on the batch service page. Active jobs have no clocks of their own — the one round clock is the station's.\n\nEviction always resets work counters — Dong & Cao's refresh semantics: an evicted request's partial progress is discarded, and if it is later re-admitted it regenerates everything (the recomputation is visible in the record). The evicted job re-files at the back of the FCFS line with a fresh patience clock; a keep-progress, keep-position eviction is a different capability, out of scope until a target model needs it.\n\nCancellation (a join! sibling losing its race) may reach a job that is active mid-round: the job is removed from the active set, its counters and its entry in the running plan are deleted, and the round runs on — the clock's aggregates were frozen at enabling, like every mark, so the duration does not change under it. This is the canceled batch-member rule applied to rounds.\n\nDepartures meeting a full :block destination are dropped, exactly as batch members are and for the same reason: by the time a round's finished jobs are deposited, the round has fired — there is no per-job server left to hold them. The batching page records the rationale in full.","title":"Waiting, eviction, cancellation"},{"category":"section","location":"manual/rounds/#The-shipped-policies","page":"Round-based token service","text":"Seven policies ship with Concourse, each a few dozen lines over the view and each usable as a template for writing your own. The first five are the stylized scheduling rules of Dai, Deng, Li & Peng, \"Throughput-Optimal Scheduling Algorithms for LLM Inference and AI Agents\" (arXiv:2504.07347); the last two are the flow-control algorithms of Dong & Cao, \"Flow-Controlled Scheduling for LLM Inference with Provable Stability Guarantees\" (arXiv:2604.11001).\n\nFasterTransformerRule(budget, kmax) — decode-prioritized, no mixed batching: decode-only rounds while any decode is pending, whole-prefill rounds otherwise. Not work-conserving.\nVanillaVLLM(budget, kmax) — prefill-prioritized, no mixing, no chunking: whole-prefill rounds while any prefill is pending. Not work-conserving.\nOrca(budget, kmax) — prefill-first mixed batching: fill the budget with prefill tokens (chunked), pack decode tokens into the remainder. Work-conserving, throughput-optimal in the paper's Theorem 2 region.\nSarathi(budget, kmax) — decode-first mixed batching: one token per pending decode, then prefill chunks from a minimal number of requests. Work-conserving, throughput-optimal.\nClassPriority(inner; by, order) — strict non-preemptive class priority wrapped around any of the above, class by class on the leftover budget. This is the wrapper the paper's Rybko–Stolyar two-server instability experiment (§5.4) needs.\nClassBudgets(b; class) — Dong & Cao's Algorithm 1: per-slot per-class activation budgets, FIFO, one decode token per active request per round, no memory check (budgets satisfying the paper's inequality keep the KV cache safe by construction).\nFlowControl(Bdist, M; prompt) — Dong & Cao's Algorithm 2: a drawn global activation budget each round (through the draw source, so it replays), KV accounting U = sum (l + textgenerated + 1), and LIFO eviction with full progress reset while U  M.","title":"The shipped policies"},{"category":"section","location":"manual/rounds/#An-M/D/1-in-disguise","page":"Round-based token service","text":"The sharpest cheap check of the engine is the degenerate case: one work phase, every job carrying one unit of work, Sarathi(budget = 1), and a Dirac duration. Each round serves exactly one job for a fixed time — the round station is M/D/1, and the mean queue must match Pollaczek–Khinchine:\n\nnet = QueueNetwork(param_names = (:lambda,))\nsource!(net, :arrive;\n        interarrival = Law(:Exponential, scale = inv(Param(:lambda))),\n        mark = MarkLaw(w = Law(:Dirac, value = Const(1.0))))\nstation!(net, :gpu;\n         rounds = Rounds(policy = Sarathi(budget = 1),\n                         duration = Law(:Dirac, value = Const(0.5)),\n                         work = (:w,)))\nsink!(net, :done)\nroute!(net, :arrive, Always(:gpu))\nroute!(net, :gpu, Always(:done))\nm = compile(net)\n\nλ, s = 1.2, 0.5\nrec = simulate(m, [λ], 8000.0; seed = 11)\nL = time_average(number_in_system, m, rec)\nρ = λ * s\nprintln(\"simulated L = \", round(L, digits = 3),\n        \"   Pollaczek–Khinchine: \", round(ρ + ρ^2 / (2 * (1 - ρ)), digits = 3))\n\nThe test suite runs this as a replicated 4-standard-error oracle test, and sharper still: the round record's firing sequence must match a plain FCFS M/D/1 model's firings one for one at the same seed — the engine adds nothing when the policy degenerates.","title":"An M/D/1 in disguise"},{"category":"section","location":"manual/rounds/#Notes-from-the-paper-reproductions","page":"Round-based token service","text":"The test suite (test/test_rounds_papers.jl) reproduces both papers end to end — Dai's Fig. 8 worked example, Theorem 2 stability boundary, vertex-C discriminator, and Rybko–Stolyar network; Dong & Cao's Algorithm 1 decoupling identity and Algorithm 2 eviction dynamics. Two behavioral findings from that work are worth knowing when you build on the shipped policies:\n\nDai's Fig. 8 is schematic about its token budget. The figure fixes only the five requests' decode lengths, and no single b_max reproduces all four of its policy rows: Orca's second batch carries five tokens where vanilla vLLM's third stops at four. The tests reproduce the vLLM, FasterTransformer, and Sarathi rows exactly at b_max = 4 and the Orca row at b_max = 5 — the figure is schematic about the budget, not about the compositions.\nFasterTransformerRule and VanillaVLLM freeze totally when a whole prefill exceeds b_max. Neither chunks prefill, faithful to the paper's stylization, so a request whose entire prefill does not fit the budget blocks the head of the line forever: no round ever runs and the backlog is exactly the arrival count. The deterministic vertex-C experiment shows this failure mode at b_max = 128 against a 290-token prefill — reproduced by the tests as the paper's own discriminator, not papered over.","title":"Notes from the paper reproductions"},{"category":"section","location":"manual/rounds/#Estimators","page":"Round-based token service","text":"The honest position, recorded in the estimator-validity table:\n\ntheta in the arrival, mark, or remark laws feeding a round station: the score estimator is valid. Round dynamics are a deterministic map of the recorded draws — the policy consumes no theta (its contract forbids it) — so the likelihood lives entirely in the laws that produced the draws, exactly as the record states it.\ntheta in a density-bearing duration law: score valid as usual; the aggregates are frozen pseudo-marks, so nothing state-dependent enters the law mid-flight.\ntheta in a Dirac duration — Dai's staircase constants a and c are the common case: no score channel exists (a point mass bears no density, so the score cannot see those parameters at all), and pathwise IPA is not certified (a perturbation that reorders a deposit against a round boundary changes plans by whole tokens). This is SPA territory, documented rather than silently wrong.\nbranch_world refuses models containing round stations in v1, the state-reading-law refusal precedent; score gradients over recorded trajectories remain available for theta in the feeding laws.\n\nThe ceil/floor expressions the staircase uses are flat almost everywhere, so a dual number entering them leaves as a constant — which is exact, not an approximation: the staircase genuinely has zero slope between its jumps.","title":"Estimators"},{"category":"section","location":"reference/surface/#Surface-language","page":"Surface language","text":"The surface language is the top layer of Concourse's three-layer design. Its calls build a QueueNetwork value; nothing executes while you build. compile freezes that value into the intermediate representation (IR) described in IR and contract, which the interpreter, the checker, and the estimators all read. Laws and ordering keys are written in the expression algebra.","title":"Surface language"},{"category":"section","location":"reference/surface/#Building-a-network","page":"Surface language","text":"","title":"Building a network"},{"category":"section","location":"reference/surface/#Disciplines","page":"Surface language","text":"A discipline says how a station keeps its waiting line, which job a free server takes, whether a better waiting job may evict one in service, and what an evicted clock remembers. A discipline is a value passed to station!, not a station subtype.","title":"Disciplines"},{"category":"section","location":"reference/surface/#Routing-kernels","page":"Surface language","text":"A kernel says where a job goes when it leaves a station. Pass one to route!.","title":"Routing kernels"},{"category":"section","location":"reference/surface/#Unblock-policies","page":"Surface language","text":"","title":"Unblock policies"},{"category":"section","location":"reference/surface/#Batch-service","page":"Surface language","text":"A batching policy is a value passed to station!, like a discipline; the batch service manual page walks through the semantics.","title":"Batch service"},{"category":"section","location":"reference/surface/#Round-service","page":"Surface language","text":"A round configuration is a value passed to station!; the round service manual page walks through the lifecycle, the policy purity contract, and the estimator position.","title":"Round service"},{"category":"section","location":"reference/surface/#Shipped-round-policies","page":"Surface language","text":"The stylized batching rules of Dai et al. (arXiv:2504.07347) and the flow-control algorithms of Dong & Cao (arXiv:2604.11001).","title":"Shipped round policies"},{"category":"module","location":"reference/surface/#Concourse.Concourse","page":"Surface language","text":"Concourse.jl — a queueing-theory simulation framework.\n\nA queueing network compiles to a generalized semi-Markov process written as plain data (QueueGSMP), interpreted against the CompetingClocks.jl sampler and satisfying the ClockGradients.jl model contract for derivative estimation. The normative design lives in notes/model_definition.tex and notes/event_loop.tex, which supersede notes/queue_layers.tex where they disagree; the code discharges the falsification charters in those notes' §7 tables.\n\n\n\n\n\n","title":"Concourse.Concourse"},{"category":"type","location":"reference/surface/#Concourse.QueueNetwork","page":"Surface language","text":"QueueNetwork(; param_names)\n\nA queueing network under construction. param_names lists the model's parameter names; their order is the layout of the parameter vector θ that simulate and the estimators take. Add stations with source!, station!, sink!, fork!, and join!, connect them with route!, then freeze the network with compile. Building a network executes nothing.\n\nExample\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :counter; service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:counter))\nroute!(net, :counter, Always(:done))\nm = compile(net)                # an M/M/1 queue\n\n\n\n\n\n","title":"Concourse.QueueNetwork"},{"category":"function","location":"reference/surface/#Concourse.source!","page":"Surface language","text":"source!(net, name::Symbol; interarrival, mark=nothing)\n\nAdd an arrival stream called name to the network.\n\ninterarrival: a law (Law or Opaque) for the time between arrivals. It may read parameters and the enabling time (Enab); it cannot read marks, because the next job does not exist yet.\nmark: an optional MarkLaw. Each arriving job draws its marks at creation; the draws are recorded.\n\nA source needs a route! saying where its arrivals go.\n\n\n\n\n\n","title":"Concourse.source!"},{"category":"function","location":"reference/surface/#Concourse.station!","page":"Surface language","text":"station!(net, name::Symbol; service=nothing, discipline=FCFS(), servers=1,\n         capacity=typemax(Int), overflow=:drop, unblock=FCFSUnblock(),\n         patience=nothing, renege_to=nothing, batching=nothing,\n         remark=nothing, rounds=nothing)\n\nAdd a service station called name to the network.\n\nservice: the service-time law, required unless rounds is given (a round station's duration lives in its Rounds config, check C12). It may read parameters, the job's marks, and the enabling time. Alone among laws it may also read live station occupancy through InService/InBuffer (check C5); such a law is re-evaluated whenever a watched count changes, with the job's accrued service effort carried over and te kept at the original enabling — the same segment convention ProcessorSharing uses.\ndiscipline: how the waiting line is kept and served — FCFS (the default), LCFS, SIRO, Priority, ProcessorSharing, or SRPT.\nservers: the number of parallel servers. Under ProcessorSharing it is the shared capacity in the speed min(1, servers/n), not a slot count.\ncapacity: the size of the waiting room, counted apart from the servers. The default is unbounded.\noverflow: what happens to a job that finds the waiting room full. :drop discards it. :block holds the job in the upstream server that finished it — that server stays occupied — until room frees here. An arrival straight from a source is dropped even under :block, because a source has no server to hold it.\nunblock: which blocked upstream transfer is admitted when room frees; FCFSUnblock is the only policy so far.\npatience and renege_to come together. patience is a law for how long a job will wait before abandoning the line, and renege_to names the station the abandoning job goes to. Jobs in service do not renege.\nbatching: a Batching policy, or nothing (the default) for one job per server. Under batching, a free server gathers between min and max waiting jobs (in discipline order) into one batch served by a single clock; the batch carries the mark batchsize for the service law to read, and on completion the members route individually. Batching requires the FCFS discipline (check C6).\nremark: a NamedTuple of laws (or a MarkLaw), or nothing (the default). The laws are drawn when a job is deposited into the station from outside and merged over the job's marks — same names replace, new names extend — before the discipline or the service law reads them, so an :ordered insert files the job by its new values. Every remark law is evaluated against the job's pre-redraw marks, then the drawn values merge, so a pair of laws reading each other's names swaps them. Re-files within the station — an evicted job's return, an unblocked transfer's admission (the redraw happened at the deposit that blocked it) — redraw nothing. Remark draws flow through the depositing firing's draw source and land in the record like any mark draw; a remark law may not read station state (check C11), and a remark-only mark is readable at this station and downstream of it, never upstream.\nrounds: a Rounds config, or nothing (the default). Under rounds the station serves in synchronous rounds: at each boundary the config's RoundPolicy admits and evicts jobs and allocates integer tokens to active ones, one station-level clock runs the round under the config's duration law, and per-job work counters persist across rounds. Check C12 fixes the shape — FCFS, no batching, servers = 1, no service law — and the config's work marks must be produced upstream and be integers ≥ 0 at admission.\n\nA station needs a route! saying where finished jobs go.\n\n\n\n\n\n","title":"Concourse.station!"},{"category":"function","location":"reference/surface/#Concourse.sink!","page":"Surface language","text":"sink!(net, name::Symbol)\n\nAdd an absorbing exit called name to the network. A job routed to a sink leaves the system and is deleted from the state; its history stays visible in the record. A sink cannot have a route!.\n\n\n\n\n\n","title":"Concourse.sink!"},{"category":"function","location":"reference/surface/#Concourse.fork!","page":"Surface language","text":"fork!(net, name::Symbol; branches)\n\nAdd a fork called name to the network. A job deposited at a fork splits instantly into one sibling per branch; the siblings share a group id and the parent job disappears. branches names at least two destination stations. Forks take no time and hold no clock. A fork routes through its branches, so it cannot have a route!. Pair it with a join! to synchronize the siblings again.\n\nA fork whose branches reach a canceling join (cancel != :none) is tracked: compile finds such forks by reachability, and at every split the full sibling roster is recorded so the join can later cancel each sibling wherever it sits. Checks C8 and C9 keep a tracked fork's branches unambiguous — one canceling join per fork, sibling traffic only on the branch stations.\n\n\n\n\n\n","title":"Concourse.fork!"},{"category":"function","location":"reference/surface/#Concourse.join!","page":"Surface language","text":"join!(net, name::Symbol; parts::Int, need::Int = parts, cancel::Symbol = :none)\n\nAdd a join called name to the network. Siblings of the same fork group are stashed as they arrive. When need of the group's parts siblings have arrived, they merge into one job, which leaves through the join's route!. parts must be at least 2 and usually equals the branch count of the matching fork!. Joins take no time and hold no clock; stashed siblings still count as jobs in the system.\n\nneed < parts is (n, k) redundancy: the group's siblings race, and cancel says what happens to the rest.\n\ncancel = :on_completion: when the need-th sibling arrives, the merge happens and every other sibling of the group is canceled wherever it is — waiting, in service, held blocked, or stashed — freeing its server or waiting-room slot. A sibling that has itself forked cancels its descendants recursively.\ncancel = :on_start: when the need-th sibling of a group enters service, siblings still waiting in buffers are canceled; jobs already in service run to completion. The join merges the first need arrivals and silently absorbs any later ones.\ncancel = :none (the default) requires need == parts: a join that merges at need but never cancels would strand the remaining siblings (check C7).\n\nCancellation is deterministic given the state — it consumes no draws, and the canceled work stays visible in the record. compile keeps each race coherent: a fork's siblings may reach at most one canceling join (check C8), and the stations between a tracked fork and its canceling join receive sibling traffic only (check C9).\n\n\n\n\n\n","title":"Concourse.join!"},{"category":"function","location":"reference/surface/#Concourse.route!","page":"Surface language","text":"route!(net, origin::Symbol, kernel)\n\nDeclare where jobs leaving the station named origin go. kernel is one of Always, ByMark, Probabilistic, RoundRobin, or ShortestQueue. Every source, station, and join needs exactly one route; sinks and forks take none. compile checks this.\n\n\n\n\n\n","title":"Concourse.route!"},{"category":"function","location":"reference/surface/#Concourse.populate!","page":"Surface language","text":"populate!(net, station::Symbol, count::Int; mark = nothing)\n\nSeed count jobs at station before time zero. The jobs are filed into the station's buffer in declaration order and dispatched into service by one settle pass at t = 0, honoring the discipline and the capacity; check C10 requires the station to have room for them. Multiple calls compose (multiclass populations). A network with at least one populate! needs no source — that is a closed network — and sources and populations may also coexist.\n\nmark: an optional MarkLaw. Each seeded job draws its marks at time zero; the draws are recorded in the record's init list (the \"firing 0\" slot), so replay reproduces them without an RNG.\n\nOnly a service station can hold a population — populating a source, sink, fork, or join is an error.\n\n\n\n\n\n","title":"Concourse.populate!"},{"category":"function","location":"reference/surface/#Concourse.compile","page":"Surface language","text":"compile(net::QueueNetwork; allow_blocking_cycles=false) -> QueueGSMP\n\nFreeze a network into a QueueGSMP, the intermediate representation the interpreter and the estimators read. Station names become integer indices and parameter names become positions in θ.\n\nallow_blocking_cycles = true skips the blocking-cycle check (C3 below) and marks the model cyclic-permitted: the simulation runs until a cycle of full :block buffers actually wedges, at which point it raises a BlockingDeadlock naming the cycle. Deadlock resolution is unsupported.\n\ncompile also runs the static checks and throws ArgumentError at the first violation:\n\nevery route target and renege destination is a declared station;\nthe network has at least one source or one populate! entry — otherwise it can never contain a job;\nevery populated station has room for its population: the counts placed at it sum to at most servers + capacity when the capacity is finite (check C10);\nevery source, station, and join has a route, and no sink or fork has one;\ndeterministic kernels (ByMark) read marks only;\nevery ShortestQueue destination is a service station, and under by = :tokens a round station (Rounds), since only round stations keep the work counters token occupancy sums;\nevery law reads only declared parameters and marks some source produces;\nno cycle of finite :block buffers exists, because a full cycle would deadlock (check C3; skipped under allow_blocking_cycles);\nstation occupancy (InService/InBuffer) is read only by the service law of a station, and every occupancy read names a declared station (check C5);\nevery station that declares Batching uses the non-preemptive FCFS discipline (check C6);\nevery station that declares Rounds has the round-station shape — non-preemptive :back-insert FCFS, no batching, servers = 1, no service law — and its work marks are produced upstream (check C12);\nevery rounds duration law reads only the plan's frozen aggregates — tokens, requests, and the per-phase sums named by the work marks — and no station state (check C13);\nremark laws obey source-mark-law scope — no station-state reads (check C11) — and read only marks the job carries before the redraw; a remark-only mark is readable at the remark station and downstream of it, never upstream;\na fork's siblings can reach at most one canceling join, so which join triggers a group's cancellation is unambiguous (check C8);\nevery station on a tracked branch — strictly between a fork and its canceling join — receives sibling traffic only (check C9).\n\n\n\n\n\n","title":"Concourse.compile"},{"category":"function","location":"reference/surface/#Concourse.FCFS","page":"Surface language","text":"FCFS()\n\nFirst come, first served. Jobs join the back of the waiting line; a free server takes the front. Non-preemptive. This is the default discipline of station!.\n\n\n\n\n\n","title":"Concourse.FCFS"},{"category":"function","location":"reference/surface/#Concourse.LCFS","page":"Surface language","text":"LCFS()\n\nLast come, first served, non-preemptive. A new job joins the front of the waiting line, so a free server takes the most recent arrival. A job already in service is never interrupted; the preemptive variant is not expressible yet.\n\n\n\n\n\n","title":"Concourse.LCFS"},{"category":"function","location":"reference/surface/#Concourse.SIRO","page":"Surface language","text":"SIRO()\n\nService in random order. A free server picks a waiting job uniformly at random. The pick consumes one auxiliary draw, which is recorded, so replay reproduces the same choices.\n\n\n\n\n\n","title":"Concourse.SIRO"},{"category":"function","location":"reference/surface/#Concourse.Priority","page":"Surface language","text":"Priority(by::ScalarExpr; preempt=false, memory=:resume)\n\nPriority service. The waiting line is ordered by the value of by evaluated on each job's marks; lower values are served first, and ties keep first-come, first-served order. With preempt=true, a strictly better waiting job evicts the worst job in service when no server is free. memory says what an evicted job's service clock remembers: :resume banks the service already received, :fresh forgets it and redraws.\n\nExample\n\nstation!(net, :cpu;\n         discipline = Priority(Mark(:class); preempt = true, memory = :resume),\n         service = Law(:Exponential, scale = inv(Param(:mu))))\n\n\n\n\n\n","title":"Concourse.Priority"},{"category":"function","location":"reference/surface/#Concourse.ProcessorSharing","page":"Surface language","text":"ProcessorSharing()\n\nProcessor sharing. Every job at the station is in service at once, each at speed min(1, servers/n) where n is the number of resident jobs. There is no waiting line. Here servers is the shared service capacity, not a count of slots.\n\n\n\n\n\n","title":"Concourse.ProcessorSharing"},{"category":"function","location":"reference/surface/#Concourse.SRPT","page":"Surface language","text":"SRPT(by::ScalarExpr = Mark(:size))\n\nShortest remaining processing time. Preemptive: the job with the least work left is served, where remaining work is the value of by (a mark holding the job's total size) minus the service the job has already received. An interrupted job resumes where it stopped. The service law should be consistent with the size mark, typically Law(:Dirac, value = Mark(:size)).\n\n\n\n\n\n","title":"Concourse.SRPT"},{"category":"type","location":"reference/surface/#Concourse.Always","page":"Surface language","text":"Always(dest::Symbol)\n\nThe routing kernel that sends every departing job to the station named dest.\n\n\n\n\n\n","title":"Concourse.Always"},{"category":"type","location":"reference/surface/#Concourse.ByMark","page":"Surface language","text":"ByMark(expr::ScalarExpr, cutoffs::Vector{Float64}, dests::Vector{Symbol})\n\nDeterministic split by a mark value. Evaluate expr on the job's marks and route by where the value falls among the sorted cutoffs: a value at or below cutoffs[1] goes to dests[1], a value above the last cutoff goes to dests[end]. dests has one more entry than cutoffs. The expression may read marks only; compile rejects a kernel that reads parameters or time, because a deterministic route must not put an unrecorded factor into the likelihood.\n\nExample\n\n# Jobs with size <= 2.0 go to :short, larger jobs to :long.\nroute!(net, :arrive, ByMark(Mark(:size), [2.0], [:short, :long]))\n\n\n\n\n\n","title":"Concourse.ByMark"},{"category":"type","location":"reference/surface/#Concourse.Probabilistic","page":"Surface language","text":"Probabilistic(dests::Vector{Symbol}, probs::Vector{Float64})\nProbabilistic(pairs::Pair{Symbol,<:Real}...)\n\nRandom routing: each departing job goes to dests[i] with probability probs[i], independently. The probabilities must sum to 1. Each decision consumes one auxiliary uniform draw, which is recorded, so replay reproduces the same choices.\n\nExample\n\nroute!(net, :arrive, Probabilistic(:a => 0.3, :b => 0.7))\n\n\n\n\n\n","title":"Concourse.Probabilistic"},{"category":"type","location":"reference/surface/#Concourse.RoundRobin","page":"Surface language","text":"RoundRobin(dests::Vector{Symbol})\n\nCyclic routing: successive departing jobs go to dests[1], dests[2], …, wrapping around. The cursor is part of the simulation state (QueueState), so replay reproduces it.\n\n\n\n\n\n","title":"Concourse.RoundRobin"},{"category":"type","location":"reference/surface/#Concourse.ShortestQueue","page":"Surface language","text":"ShortestQueue(dests::Symbol...; by = :requests)\n\nJoin the shortest queue: each departing job goes to the candidate station with the smallest occupancy, deterministically — ties go to the station with the lowest index (declaration order), so the decision is a pure function of the state. Nothing is drawn and nothing is recorded; replay reproduces every decision from the folded state. by picks the occupancy notion:\n\n:requests (the default): the total number of jobs at the destination — waiting, in service, and held blocked.\n:tokens: the total remaining work at the destination — the active jobs' work counters plus the waiting jobs' full profiles. Valid only when every destination is a round station (Rounds), because only round stations keep work counters; compile rejects the rest.\n\nAmendment A4 reserves likelihood-bearing decisions for recorded draws; ShortestQueue is admitted because a deterministic function of state bears no likelihood. What it costs is estimator smoothness: a θ perturbation that reorders events can flip a routing decision discontinuously, so score gradients remain valid (the decision contributes no likelihood factor) but pathwise IPA is not certified, and branch_world refuses models containing ShortestQueue in v1. Randomized load balancing (power-of-d choices) would be likelihood-bearing and is out of scope. Concourse.stability treats the kernel as an equal split across its destinations — exact under symmetry, an approximation otherwise.\n\nExample\n\nroute!(net, :arrive, ShortestQueue(:a, :b))                 # by job count\nroute!(net, :arrive, ShortestQueue(:gpu1, :gpu2; by = :tokens))  # by remaining work\n\n\n\n\n\n","title":"Concourse.ShortestQueue"},{"category":"type","location":"reference/surface/#Concourse.FCFSUnblock","page":"Surface language","text":"FCFSUnblock()\n\nThe unblock policy for stations with overflow = :block: when waiting room frees, the transfer that has been blocked longest is admitted first. Which blocked upstream job gets a freed slot is observable model behavior, so it is declared per station (the unblock keyword of station!), not fixed by the interpreter. This is the only policy so far.\n\n\n\n\n\n","title":"Concourse.FCFSUnblock"},{"category":"type","location":"reference/surface/#Concourse.Batching","page":"Surface language","text":"Batching(; min = 1, max = typemax(Int))\n\nBatch-service policy for station!: a free server waits until at least min jobs are in the buffer, then gathers up to max of them (in discipline order) into one batch served by a single clock. The batch is a synthetic job carrying the mark batchsize, frozen at enabling like any mark, so the service law may read Mark(:batchsize). Members waiting for a batch to form keep their patience clocks; the batch job itself never gets one. On completion each member routes individually on its own marks and draws; a member that meets a full :block destination is dropped like a source arrival, because the batch's firing already freed the server — nothing is left to hold the member. Requires the FCFS discipline (check C6).\n\nThe defaults min = 1, max = typemax(Int) are the gather-everything rule (dynamic batching); min = max = K is classical fixed-size batch service (M/M^K/1).\n\n\n\n\n\n","title":"Concourse.Batching"},{"category":"type","location":"reference/surface/#Concourse.Rounds","page":"Surface language","text":"Rounds(; policy, duration, work)\n\nThe round-service configuration of station!. A round station serves in synchronous rounds: at each boundary policy (a RoundPolicy) plans the round — admissions, evictions, and per-active-job integer token allocations — and one station-level clock runs it under duration, a law over the plan's frozen aggregates. At the firing the allocations are credited against per-job work counters, jobs whose work is exhausted route onward, and the next round is planned at the same instant, so back-to-back rounds chain with no gap. A station with no plan idles clock-free; the first deposit after an idle period starts the round grid at that deposit's time.\n\npolicy: the RoundPolicy consulted at every boundary.\nduration: the round-length law. It may read parameters, Enab, and the plan's aggregates as pseudo-marks — Mark(:tokens) (the token load b), Mark(:requests) (the batch size k), and one per-phase sum named by each work mark — frozen at enabling like any mark (check C13). Dai's staircase is Const(c) + Const(a) * ceil(Mark(:tokens) / b0).\nwork: the names of the marks (produced upstream or by remark) that give a job's integer work profile, in phase order; the LAST phase is the decode phase of the shipped policies. The marks are snapshotted into integer counters at admission and must be integers ≥ 0 at runtime.\n\nCheck C12 fixes the station shape: FCFS (:back-insert, non-preemptive), no batching, servers = 1 (the round clock is the server), and no service law — the duration lives here. Waiting jobs keep patience clocks; active jobs have none, and an evicted job re-files with a fresh patience clock and RESET work counters (Dong's refresh semantics). Round-station departures meeting a full :block destination are dropped, the batch-member rule. A Dirac duration bears no density, so θ inside it has no score channel and pathwise IPA is not certified; θ in the arrival, mark, or remark laws feeding the station keeps a valid score. branch_world refuses round stations in v1.\n\nExample\n\nstation!(net, :gpu;\n    rounds = Rounds(\n        policy   = Sarathi(budget = 512),\n        duration = Law(:Dirac; value = Const(11.28) + Const(35.47) * ceil(Mark(:tokens) / 128)),\n        work     = (:v_p, :v_d),\n    ))\n\n\n\n\n\n","title":"Concourse.Rounds"},{"category":"type","location":"reference/surface/#Concourse.RoundPolicy","page":"Surface language","text":"RoundPolicy\n\nAbstract supertype of round-boundary batch policies. A concrete policy implements one method, plan_round, a pure function of a RoundView and a draw source that returns the next round's RoundPlan (or nothing to idle). The shipped policies are Dai et al.'s four (FasterTransformerRule, VanillaVLLM, Orca, Sarathi) with the ClassPriority wrapper, and Dong & Cao's two (ClassBudgets, FlowControl).\n\n\n\n\n\n","title":"Concourse.RoundPolicy"},{"category":"function","location":"reference/surface/#Concourse.plan_round","page":"Surface language","text":"plan_round(policy, v::RoundView, draws) -> Union{RoundPlan,Nothing}\n\nThe one required method of a RoundPolicy: plan the next round from the view, or return nothing to idle (no round runs; the station re-plans at the next deposit). The contract, documented and tested but not compile-checkable (the Opaque precedent): the policy is a pure function of (v, draws) — all randomness through draws (via draw!, recorded and replayed), no clocks, no wall time, no θ, no state beyond the view. Replay equality and the debug = true membership oracle catch violations.\n\n\n\n\n\n","title":"Concourse.plan_round"},{"category":"type","location":"reference/surface/#Concourse.RoundView","page":"Surface language","text":"RoundView\n\nThe read-only window a RoundPolicy plans through: phases (the station's work mark names, in phase order), waiting (the RoundJobs in the buffer, FCFS order), and active (the jobs in service, admission order). It is a barrier — copies and values, never the QueueState — so a policy can rank and count jobs but cannot reach the simulation state.\n\n\n\n\n\n","title":"Concourse.RoundView"},{"category":"type","location":"reference/surface/#Concourse.RoundJob","page":"Surface language","text":"RoundJob\n\nOne job as a RoundView shows it to a policy: id (the job id), marks (the job's marks), and work — the remaining integer work per phase for an active job, or the full profile the marks give (what the counters would be at admission) for a waiting one. The vectors are copies; mutating them changes nothing.\n\n\n\n\n\n","title":"Concourse.RoundJob"},{"category":"type","location":"reference/surface/#Concourse.RoundPlan","page":"Surface language","text":"RoundPlan(; admit = [], evict = [], alloc = [])\n\nWhat a RoundPolicy commits at a round boundary: admit (waiting job ids to move into the active set, in order), evict (active job ids returned to the waiting line, work counters RESET — Dong's refresh semantics), and alloc (id => tokens-per-phase pairs over active jobs, newly admitted included; an active job absent from alloc gets zero). The engine applies evictions, then admissions, then checks every allocation against 0 ≤ alloc ≤ remaining per phase, and freezes the plan's aggregates — tokens, requests, and the per-phase sums — as the pseudo-marks the duration law reads. A job whose every phase reaches zero at the round's firing routes onward.\n\n\n\n\n\n","title":"Concourse.RoundPlan"},{"category":"type","location":"reference/surface/#Concourse.FasterTransformerRule","page":"Surface language","text":"FasterTransformerRule(; budget, kmax = typemax(Int))\n\nDai et al.'s stylized FasterTransformer (arXiv:2504.07347 §4): decode-prioritized, no mixed batching. Whenever any request is in the decode phase, the round is a decode-only batch — one token per decode request, FCFS, up to the token budget and the batch-size cap kmax — and prefill waits. Only with no decodes pending does it form a prefill-only batch of whole prefills (no chunking), FCFS while they fit. Not work-conserving; unstable inside the Theorem 2 region.\n\n\n\n\n\n","title":"Concourse.FasterTransformerRule"},{"category":"type","location":"reference/surface/#Concourse.VanillaVLLM","page":"Surface language","text":"VanillaVLLM(; budget, kmax = typemax(Int))\n\nDai et al.'s vanilla vLLM (arXiv:2504.07347 §4): prefill-prioritized, no mixed batching, no chunked prefill. Whenever any request is in the prefill phase, the round is a prefill-only batch of whole prefills, FCFS while they fit the token budget (and kmax), and decodes wait. Only with no prefill pending does it batch decode tokens, one per request, FCFS. Not work-conserving; unstable inside the Theorem 2 region.\n\n\n\n\n\n","title":"Concourse.VanillaVLLM"},{"category":"type","location":"reference/surface/#Concourse.Orca","page":"Surface language","text":"Orca(; budget, kmax = typemax(Int))\n\nDai et al.'s stylized Orca (arXiv:2504.07347 §4): prefill-prioritized with mixed batching. Each round first fills the token budget with prefill tokens, FCFS (chunked as needed to stay work-conserving), then packs decode tokens — one per decode request, FCFS — into the remainder, up to the batch-size cap kmax. Work-conserving, hence throughput-optimal in the Theorem 2 region.\n\n\n\n\n\n","title":"Concourse.Orca"},{"category":"type","location":"reference/surface/#Concourse.Sarathi","page":"Surface language","text":"Sarathi(; budget, kmax = typemax(Int))\n\nDai et al.'s stylized Sarathi-Serve (arXiv:2504.07347 §4): decode-prioritized with chunked prefill. Each round first takes one decode token per decode-phase request, FCFS, then fills the remaining token budget with prefill chunks from a minimal number of requests — as much as possible from the FCFS-first prefill request before touching the next — up to the batch-size cap kmax. Work-conserving, hence throughput-optimal in the Theorem 2 region.\n\n\n\n\n\n","title":"Concourse.Sarathi"},{"category":"type","location":"reference/surface/#Concourse.ClassPriority","page":"Surface language","text":"ClassPriority(inner; by, order)\n\nNon-preemptive class priority over any Dai-style policy: jobs are partitioned by the value of mark by, classes are served in the order order lists their values (unlisted values come last, together), and inner's composition rule runs class by class on the leftover token budget and batch-size cap — so the whole budget goes to the highest class's tokens before any lower class is touched, with inner's own prefill/decode rule inside each class. FCFS is preserved within a class. This is the wrapper Dai et al.'s Rybko–Stolyar experiment (§5.4) needs: ClassPriority(Sarathi(budget = 768); by = :class, order = [2.0, 1.0]) gives class 2 strict priority. Admissions delegate to the Dai rule (every waiting job is admitted); wrappers nest.\n\n\n\n\n\n","title":"Concourse.ClassPriority"},{"category":"type","location":"reference/surface/#Concourse.ClassBudgets","page":"Surface language","text":"ClassBudgets(b; class = :class)\n\nDong & Cao's Algorithm 1 (arXiv:2604.11001): flow control with known output lengths. Each round (one decode slot), for each class k = 1..m in order, up to b[k] waiting requests whose integer-valued mark class equals k are activated, FIFO; every active request then decodes exactly one token. There is NO memory check — budgets satisfying the paper's inequality (2) guarantee the KV cache never overflows — and no eviction. b is anything indexable by the class index (a Tuple, Vector, or NamedTuple in class order). Use a single work phase naming the decode length; the prompt length is an ordinary mark.\n\n\n\n\n\n","title":"Concourse.ClassBudgets"},{"category":"type","location":"reference/surface/#Concourse.FlowControl","page":"Surface language","text":"FlowControl(Bdist, M; prompt = :l)\n\nDong & Cao's Algorithm 2 (arXiv:2604.11001): flow control with unknown output lengths. Each planned round (one decode slot) draws a global activation budget B ~ Bdist through the round's draw source (recorded, so replay reproduces it), activates up to B waiting requests FIFO, and then checks the KV cache: with l the value of the prompt mark and g the tokens a request has generated, the slot's memory usage is U = Σ (l + g + 1) over the active set, the slot's own token included (the paper's l + (t - s + 1) accounting). While U > M, active requests are evicted last-in first-out — the most recently activated first, just-activated ones before older ones — with FULL progress reset (the engine clears the work counters; a re-activated request regenerates everything). Survivors each decode one token. Use a single work phase naming the decode length. The station idles (and draws nothing) only when it holds no jobs at all; each round with jobs present draws a fresh budget, one draw per slot as in the paper.\n\n\n\n\n\n","title":"Concourse.FlowControl"},{"category":"section","location":"manual/closed_networks/#Closed-networks","page":"Closed networks","text":"A fixed population of jobs circulating forever — N machines cycling between working and being repaired, N interactive users thinking and then submitting a request, a central-server model with a set number of processes — is a closed network: nothing arrives from outside and nothing leaves. In Concourse a population is declared with populate!, and everything else — disciplines, capacities, routing, records, replay, gradients — keeps its usual meaning. The one genuinely new mechanism is a \"firing 0\" slot in the record, init, carrying the draws that seeding consumed, so that a populated network replays and differentiates exactly like an open one.","title":"Closed networks"},{"category":"section","location":"manual/closed_networks/#Seeding-a-population","page":"Closed networks","text":"populate!(net, station, count; mark = nothing) seeds count jobs at a service station — only a service station; populating a source, sink, fork, or join is an error. Multiple calls compose, which is how a multiclass population is written: each call may carry its own MarkLaw, and each seeded job draws its marks at time zero.\n\nA network with at least one populate! needs no source — that is a closed network — and sources and populations may also coexist, an open network that starts nonempty. In the mixed case the census is exactly N + textarrivals - textdepartures at every recorded state, which the test suite folds over a whole record. A network with neither a source nor a population is refused at compile: \"the network has no source and no populate! entry, so it can never contain a job\".\n\nPlacement is deliberately unmagical. Each entry's jobs are filed into their station's buffer in declaration order, and then one settle cascade runs at t = 0 — the same dispatch step every firing runs — honoring the discipline and the capacity, so the run starts exactly as if the jobs had always been there. Every clock that ends up enabled gets enabling time 0.0 through the ordinary enable loop; nothing downstream knows a job was seeded rather than routed. Because seeded jobs are ordinary residents, they must fit — check C10 requires each populated station's counts to sum to at most servers + capacity:\n\nusing Concourse\n\nnet = QueueNetwork(param_names = (:mu,))\nstation!(net, :a; servers = 1, capacity = 2,\n         service = Law(:Exponential, scale = inv(Param(:mu))))\nroute!(net, :a, Always(:a))\npopulate!(net, :a, 4)\ntry\n    compile(net)\ncatch err\n    println(err.msg)\nend\n\nOne check goes quiet rather than loud: Concourse.stability (check C4) returns an empty report for a sourceless network. With a fixed population there is no arrival rate to outrun, so a closed network cannot be unstable; mixed networks keep the source-driven part of the traffic equations.","title":"Seeding a population"},{"category":"section","location":"manual/closed_networks/#A-closed-cycle-against-Buzen's-algorithm","page":"Closed networks","text":"The two-station cycle is the smallest closed network with something to predict: N jobs alternate between exponential single-server stations :a and :b, and the question is how the population splits between them in the long run.\n\nnet = QueueNetwork(param_names = (:mu1, :mu2))\nstation!(net, :a; service = Law(:Exponential, scale = inv(Param(:mu1))))\nstation!(net, :b; service = Law(:Exponential, scale = inv(Param(:mu2))))\nroute!(net, :a, Always(:b))\nroute!(net, :b, Always(:a))\npopulate!(net, :a, 4)\nm = compile(net)\n\nθ = [1.0, 1.5]\nrec = simulate(m, θ, 4000.0; seed = 5)\nat(name) = (q = m.names[name]; st -> Float64(length(st.buf[q]) + length(st.srv[q])))\nLa = time_average(at(:a), m, rec)\nLb = time_average(at(:b), m, rec)\nLa + Lb    # the population is closed: every job is at :a or :b\n\nThe exact answer comes from the product form for closed networks (Gordon–Newell): the stationary probability of an occupancy vector is prod_i rho_i^n_i divided by a normalizing constant G(N), where rho_i is visit ratio × mean service time. G(N) looks like a sum over all ways to place N jobs on the stations, but Buzen's convolution computes it in O(MN): fold stations in one at a time with g_m(n) = g_m-1(n) + rho_m g_m(n-1), which one ascending in-place pass realizes. The mean occupancy of station i follows from the same table, L_i = sum_k=1^N rho_i^k G(N-k)G(N):\n\nN = 4\nρ = [1 / θ[1], 1 / θ[2]]    # visit ratio × mean service time; both visits are 1\nG = zeros(N + 1)\nG[1] = 1.0\nfor r in ρ, n in 1:N\n    G[n + 1] += r * G[n]\nend\nexact_La = sum(ρ[1]^k * G[N - k + 1] / G[N + 1] for k in 1:N)\nprintln(\"simulated L at :a = \", round(La, digits = 3),\n        \"   Buzen exact = \", round(exact_La, digits = 3))\n\nThis is the validation pattern for closed models: an exact normalizing-constant (or mean value analysis — MVA walks the same recursion population by population) computation priced against the simulated time averages. The test suite runs this comparison as a replicated 4-standard-error oracle test, and checks that the two station means sum to exactly N.","title":"A closed cycle against Buzen's algorithm"},{"category":"section","location":"manual/closed_networks/#Machine-repairman","page":"Closed networks","text":"The classical availability model is two lines away from the cycle: N machines fail independently — a delay station :up with servers = N, so every machine ages on its own clock — and queue for a single repairman. The number in repair is a finite birth–death chain with pi_k propto fracN(N-k) (numu)^k, which prices the fraction of machines up:\n\nnetmr = QueueNetwork(param_names = (:nu, :mu))\nstation!(netmr, :up; servers = N, service = Law(:Exponential, scale = inv(Param(:nu))))\nstation!(netmr, :repair; service = Law(:Exponential, scale = inv(Param(:mu))))\nroute!(netmr, :up, Always(:repair))\nroute!(netmr, :repair, Always(:up))\npopulate!(netmr, :up, N)\nmr = compile(netmr)\n\nθmr = [0.5, 2.0]    # failure rate ν, repair rate μ\nmrec = simulate(mr, θmr, 4000.0; seed = 9)\nup = mr.names[:up]\navail = time_average(st -> Float64(length(st.srv[up])), mr, mrec) / N\n\nr = θmr[1] / θmr[2]\nπk = [factorial(N) / factorial(N - k) * r^k for k in 0:N]\nπk ./= sum(πk)\nexact = sum((N - k) * πk[k + 1] for k in 0:N) / N\nprintln(\"simulated availability = \", round(avail, digits = 3),\n        \"   birth–death exact = \", round(exact, digits = 3))","title":"Machine repairman"},{"category":"section","location":"manual/closed_networks/#Firing-0:-the-record's-init-slot","page":"Closed networks","text":"Seeding may consume randomness — each populated job draws its marks at t = 0, and the t = 0 dispatch may draw too (a SIRO pick, say). Under amendment A4 new randomness means new record content, never a second channel, so these draws flow through the same draw conduit as every mark and routing draw, keyed by the reserved pseudo-clock (:init, 0, 0). :init is not a clock family — this key can never be enabled or fired — it exists to key the seeding streams and to label the record's MarkedRecord init list, the \"firing 0\" slot. A two-class population makes the contents visible:\n\nnetc = QueueNetwork(param_names = (:mu1, :mu2))\nstation!(netc, :a; service = Law(:Exponential, scale = Mark(:speed) * inv(Param(:mu1))))\nstation!(netc, :b; service = Law(:Exponential, scale = inv(Param(:mu2))))\nroute!(netc, :a, Always(:b))\nroute!(netc, :b, Always(:a))\npopulate!(netc, :a, 2; mark = MarkLaw(speed = Law(:Dirac, value = Const(0.5))))\npopulate!(netc, :b, 2; mark = MarkLaw(speed = Law(:Dirac, value = Const(2.0))))\nmc = compile(netc)\n\ncrec = simulate(mc, [1.0, 1.5], 100.0; seed = 21)\ncrec.init\n\nFour initial mark draws, in declaration order — the order initial_state seeds jobs, which is the order replay reads them back. replay and time_average seed the initial state from rec.init before folding the firings, so a populated record replays without an RNG or a θ, like every record. The conduit's discipline also holds at firing 0: a truncated init list errors loudly instead of desynchronizing quietly.\n\nshort = MarkedRecord(crec.key, crec.time, crec.draws, crec.init[1:3], crec.horizon)\ntry\n    replay(mc, short)\ncatch err\n    println(err.msg)\nend\n\nThe zero-argument initial_state(m) still works for draw-free populations (no mark laws, deterministic dispatch). For a draw-bearing population it refuses to invent draws and points at the record instead:\n\ntry\n    initial_state(mc)\ncatch err\n    println(err.msg)\nend","title":"Firing 0: the record's init slot"},{"category":"section","location":"manual/closed_networks/#Branching-and-gradients","page":"Closed networks","text":"The score estimator works through the record, population included: replay_model binds rec.init, so the ClockGradients contract's initial_state reproduces the seeded population — and its t = 0 clocks enter the likelihood like any others. The test suite checks the score gradient of the closed cycle's occupancy with respect to both service rates against common-random-number finite differences, drift alarm included.\n\nThe live, branchable side is stricter, and loud about it. A drawn initial mark cannot branch: live_model's init binding is empty, and branch_world seeds from the zero-argument initial_state, so both fail at seeding rather than replay stale values — only draw-free populations branch. A θ-reading initial mark law is refused by branch_world at construction, exactly as a θ-reading source mark law is: an initial mark law is a mark law, and a θ-dependent one would add an unimplemented derivative term to every estimator (see the branching rules).\n\nBeyond those existing rules, closed populations add no estimator caveat: initial marks are ordinary recorded draws, consumed once at t = 0, so seeding introduces no new event-order discontinuity and no new likelihood term. The estimator-validity table gains no row.","title":"Branching and gradients"},{"category":"section","location":"queues/mg1/#M/G/1-and-why-distributions-matter","page":"M/G/1 and why distributions matter","text":"The M/M/1 formulas used memorylessness twice. Real service times are rarely exponential: a checkout basket has a fairly predictable size, a database query is usually tiny but occasionally enormous. This page keeps Poisson arrivals and lets the service distribution be anything — M/G/1 — and shows that what matters about it is its variability, through one formula.","title":"M/G/1 and why distributions matter"},{"category":"section","location":"queues/mg1/#The-Pollaczek–Khinchine-formula","page":"M/G/1 and why distributions matter","text":"Write ES for the mean service time and define the squared coefficient of variation,\n\nCV^2 = fracmathrmVar(S)ES^2\n\na scale-free measure of variability: CV^2 = 0 for a constant, 1 for the exponential,  1 for distributions more erratic than exponential. For an M/G/1 queue under FCFS (first come, first served), the mean number in system is\n\nL = rho + fracrho^2(1 + CV^2)2(1-rho)\n\nThis is the Pollaczek–Khinchine (P–K) formula. Only the mean and variance of S enter. Two readings:\n\nWith CV^2 = 1 it reduces to rho(1-rho) — M/M/1 again.\nL grows linearly in the variance, at fixed load. Waiting is caused not just by how much work arrives but by how lumpily it arrives.\n\nThe intuition: your wait is dominated by the chance of getting stuck behind one huge job. Halving the variance of service times halves the queueing part of the delay without buying a faster server.","title":"The Pollaczek–Khinchine formula"},{"category":"section","location":"queues/mg1/#Three-service-laws,-one-load","page":"M/G/1 and why distributions matter","text":"Fix lambda = 1 and mean service ES = 05, so rho = 05 for all three of:\n\nmodel service law CV^2 P–K L\nM/D/1 constant 05 (Dirac) 0 0.75\nM/M/1 exponential, mean 05 1 1.0\nM/G/1, heavy Gamma, shape 14, mean 05 4 1.75\n\nSame arrival stream, same mean work, and the theory says the queues differ by more than a factor of two. Simulate all three (16 replications each, the four-standard-error convention):\n\nusing Concourse, Statistics\n\nfunction mg1_model(service)\n    net = QueueNetwork(param_names = (:lambda,))\n    source!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\n    station!(net, :desk; discipline = FCFS(), servers = 1, service = service)\n    sink!(net, :done)\n    route!(net, :arrive, Always(:desk))\n    route!(net, :desk, Always(:done))\n    compile(net)\nend\n\nfunction replicate(f, m, θ, horizon, nreps; seed0 = 1000)\n    vals = [f(simulate(m, θ, horizon; seed = seed0 + r)) for r in 1:nreps]\n    mean(vals), std(vals) / sqrt(nreps)\nend\n\npk(ρ, cv2) = ρ + ρ^2 * (1 + cv2) / (2 * (1 - ρ))\n\ncases = [(\"M/D/1\",        Law(:Dirac, value = Const(0.5)),                       0.0),\n         (\"M/M/1\",        Law(:Exponential, scale = Const(0.5)),                 1.0),\n         (\"M/G/1 heavy\",  Law(:Gamma, shape = Const(0.25), scale = Const(2.0)),  4.0)]\n\nresults = [] # hide\nprintln(rpad(\"model\", 14), rpad(\"measured L\", 18), \"P-K exact\")\nfor (name, law, cv2) in cases\n    m = mg1_model(law)\n    est, se = replicate(r -> time_average(number_in_system, m, r), m, [1.0], 2000.0, 16)\n    push!(results, (name, est, se, pk(0.5, cv2))) # hide\n    println(rpad(name, 14),\n            rpad(string(round(est, digits = 3), \" ± \", round(se, digits = 3)), 18),\n            pk(0.5, cv2))\nend\n\nusing Luxor, QueueDiagrams, LaTeXStrings # hide\nvmax = 2.0 # hide\npy(v) = 90.0 - 170.0 * v / vmax # hide\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nsethue(\"gray60\"); setline(1) # hide\nLuxor.line(Point(-240, 90), Point(240, 90), action = :stroke) # hide\nfor (i, (name, est, se, exact)) in enumerate(results) # hide\n    x = -150.0 + (i - 1) * 150.0 # hide\n    sethue(CLASS_COLORS[1]) # hide\n    box(Point(x - 30, py(est)), Point(x + 30, 90), action = :fill) # hide\n    sethue(\"black\"); setline(2) # hide\n    setdash(\"dot\") # hide\n    Luxor.line(Point(x - 42, py(exact)), Point(x + 42, py(exact)), action = :stroke) # hide\n    setdash(\"solid\") # hide\n    fontsize(12) # hide\n    text(string(round(est, digits = 2)), # hide\n         Point(x, min(py(est), py(exact)) - 8), halign = :center) # hide\n    text(name, Point(x, 108), halign = :center) # hide\nend # hide\nsethue(\"gray40\"); fontsize(11) # hide\ntext(\"mean number in system; bar = simulated, dotted line = Pollaczek–Khinchine\", # hide\n     Point(0, 128), halign = :center) # hide\nend 560 260 # hide\n\nEqual means, equal loads — and the measured queues fan out exactly as the variance says they should, each within noise of its dotted line.","title":"Three service laws, one load"},{"category":"section","location":"queues/mg1/#The-escape-hatch:-processor-sharing","page":"M/G/1 and why distributions matter","text":"The P–K penalty is a property of FCFS, not of the workload. Run the same heavy-tailed law under processor sharing (PS, from the disciplines page):\n\nfunction mg1_ps(service)\n    net = QueueNetwork(param_names = (:lambda,))\n    source!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\n    station!(net, :cpu; discipline = ProcessorSharing(), servers = 1, service = service)\n    sink!(net, :done)\n    route!(net, :arrive, Always(:cpu))\n    route!(net, :cpu, Always(:done))\n    compile(net)\nend\n\nmps = mg1_ps(Law(:Gamma, shape = Const(0.25), scale = Const(2.0)))\nest, se = replicate(r -> time_average(number_in_system, mps, r), mps, [1.0], 2000.0, 16)\nprintln(\"PS with the heavy-tailed law: L = \", round(est, digits = 3),\n        \" ± \", round(se, digits = 3), \"   (M/M/1 value: 1.0)\")\n\nThe variance penalty is gone: under PS the mean occupancy depends on the service law only through its mean, so L = rho(1-rho) for any service distribution. This property is called insensitivity, and it is the reason the disciplines page found PS mediocre on low-variance work but promised it would shine here: PS never wins big, but it never pays the variance penalty either.\n\nInsensitivity returns as a star player in the BCMP case study, where it holds for whole networks. Before that, we need more than one station: networks.","title":"The escape hatch: processor sharing"},{"category":"section","location":"reference/ir/#IR-and-contract","page":"IR and contract","text":"The intermediate representation (IR) is the middle layer: a generalized semi-Markov process (GSMP) written as plain data (QueueGSMP) together with a state (QueueState). Five contract functions define the model's dynamics, and everything above drives the model only through them: the interpreter folds fire_changes forward, and the gradient layers implement the ClockGradients.jl contracts with these same functions. The surface language produces a QueueGSMP with compile.","title":"IR and contract"},{"category":"section","location":"reference/ir/#Data","page":"IR and contract","text":"","title":"Data"},{"category":"section","location":"reference/ir/#The-five-contract-functions","page":"IR and contract","text":"","title":"The five contract functions"},{"category":"type","location":"reference/ir/#Concourse.QueueGSMP","page":"IR and contract","text":"QueueGSMP\n\nThe compiled model: a generalized semi-Markov process (GSMP) written as plain data. Produced by compile; consumed by the interpreter (simulate), the record fold (replay), and the gradient estimators. Station references are integer indices. names maps a station name to its index, and params maps a parameter name to its position in the parameter vector θ.\n\nsrv_readers and buf_readers are the state-dependence map: srv_readers[s] lists the stations whose service law reads InService of station s (a ProcessorSharing station is an srv-reader of itself, since every resident's speed reads the resident count), and buf_readers[s] likewise for InBuffer. fire_changes re-evaluates a reader's surviving service clocks whenever a watched count changes.\n\npopulation holds the network's populate! entries in declaration order, with stations as indices; initial_state seeds them.\n\nallow_blocking_cycles records that compile was asked to skip check C3: the topology may contain a cycle of finite :block buffers, and the interpreter's deposit step watches for a realized deadlock at runtime, raising BlockingDeadlock.\n\n\n\n\n\n","title":"Concourse.QueueGSMP"},{"category":"type","location":"reference/ir/#Concourse.QueueState","page":"IR and contract","text":"QueueState\n\nThe complete state of a queueing simulation between firings. It holds the wall-clock time, the count nfired of firings so far, the next job id, and per-station vectors of job ids: buf (waiting, in discipline order), srv (in service), and hold (finished but blocked from transferring). jobs maps each live job id to its marks. pending and group track fork–join siblings; members maps each unresolved fork parent to the sibling ids it minted (the roster sibling cancellation walks, recursively for nested forks), and started counts, per group racing toward a cancel = :on_start join, how many siblings have entered service. batchmembers maps a synthetic batch job to the member jobs it gathered (members stay in jobs but appear in no station vector), and cursor holds each RoundRobin kernel's position.\n\nRound service (Rounds) adds two replay-owned fields: work maps each job active at a round station to its remaining integer work per phase (created at admission, deleted at departure or eviction), and roundplan holds, per station, the running round's committed RoundPlan — its frozen allocation and aggregates — or nothing when the station is between rounds. A RoundPlan in a slot is immutable: state transitions replace the slot, never the plan's vectors, so copies of the state may share it.\n\nThe state also carries the clock bookkeeping that determines future behavior, so the sampler can be a pure consumer of it: te holds each enabled clock's enabling time, bank holds the age banked by a :resume-disabled clock, and anchor marks the last speed change of a processor-sharing clock. Fields are documented here because measurement functions, such as those passed to time_average, read them directly.\n\n\n\n\n\n","title":"Concourse.QueueState"},{"category":"type","location":"reference/ir/#Concourse.ClockKey","page":"IR and contract","text":"ClockKey\n\nThe identity of a clock: a tuple (family, station, job). family is the clock family — :arrival, :service, :patience, or :round. station is the station index in the compiled model. job is the job id, or 0 for an arrival clock, which belongs to the source rather than to a job, and for a round clock, which belongs to the station's running round. Clock keys appear in MarkedRecords, in the deltas of fire_changes, and as sampler keys.\n\n\n\n\n\n","title":"Concourse.ClockKey"},{"category":"function","location":"reference/ir/#Concourse.initial_state","page":"IR and contract","text":"initial_state(m::QueueGSMP) -> QueueState\ninitial_state(m::QueueGSMP, ds::DrawSource) -> QueueState\n\nThe state at time zero, with one arrival clock enabled per source (enabling time 0.0) and the network's populate! population seeded: each entry's jobs are filed into their station's buffer in declaration order, one settle pass at t = 0 dispatches them into service, and every enabled clock gets enabling time 0.0. One of the five contract functions (initial_state, enabled, clock_distribution, fire_changes, states_equal).\n\nSeeding may consume draws — initial marks, and any draw the t = 0 dispatch makes (a SIRO pick, say) — which flow through ds, keyed by the reserved pseudo-clock (:init, 0, 0); simulate records them as the record's init list and replay reads them back. The one-argument form works when no draw is needed; a model whose population carries mark laws must be seeded through a draw source and errors here, pointing at the record's init draws.\n\n\n\n\n\n","title":"Concourse.initial_state"},{"category":"function","location":"reference/ir/#Concourse.enabled","page":"IR and contract","text":"enabled(m::QueueGSMP, st::QueueState) -> Vector{ClockKey}\n\nThe keys of every clock enabled in state st, derived from the state alone: one :arrival clock per source, one :service clock per job in service, one :patience clock per waiting job at a station with a patience law, and one :round clock per round station with a committed plan. One of the five contract functions. The interpreter's debug oracle recomputes this set after every firing and compares it against the sampler.\n\n\n\n\n\n","title":"Concourse.enabled"},{"category":"function","location":"reference/ir/#Concourse.clock_distribution","page":"IR and contract","text":"clock_distribution(m::QueueGSMP, θ, key::ClockKey, st::QueueState) -> Distribution\n\nThe firing-time distribution of clock key in state st, measured from the clock's enabling time st.te[key]. The clock's law is evaluated with the parameter vector θ, the owning job's marks, and the enabling time. Under ProcessorSharing the returned distribution also folds in the banked age, the current speed, and the anchor of the last speed change. One of the five contract functions.\n\nThe result's element type follows eltype(θ), so a dual-number-valued θ flows through untouched — the seam ClockGradients.jl differentiates through.\n\n\n\n\n\n","title":"Concourse.clock_distribution"},{"category":"function","location":"reference/ir/#Concourse.fire_changes","page":"IR and contract","text":"fire_changes(m, st, key, t, draws; worklist=:fifo) -> (new_state, deltas)\n\nApply the firing of clock key at time t to state st and return the new state together with the list of clock deltas — (:enable, k), (:disable, k), or (:reenable, k) pairs the sampler must apply. st is not mutated. draws is the draw source for auxiliary randomness (marks, routing, random selection); worklist (:fifo or :lifo) orders the settle cascade, and both orders reach the same fixed point. One of the five contract functions; most users reach it through simulate and replay.\n\nPure given the draw source: live it records, in replay it reads back. Deltas are DERIVED, not emitted: for every station the firing's cascade touched, each family's enabled keys are diffed between the old and new state, so a clock's lifecycle follows from membership alone and a new family needs no plumbing. A clock enabled and disabled within one firing never reaches the sampler — the GSMP has no zero-duration clocks. Enabling-time and banked-age bookkeeping (amendment A1) happens in the same pass, in one place.\n\n\n\n\n\n","title":"Concourse.fire_changes"},{"category":"function","location":"reference/ir/#Concourse.states_equal","page":"IR and contract","text":"states_equal(a::QueueState, b::QueueState) -> Bool\n\nField-by-field equality of two states, including the clock bookkeeping (te, bank, anchor). This is the equality the replay tests use to show that a fold of fire_changes over a record reproduces the live trajectory exactly. One of the five contract functions.\n\n\n\n\n\n","title":"Concourse.states_equal"},{"category":"section","location":"manual/state_dependent/#State-dependent-service-laws","page":"State-dependent service laws","text":"A service rate that depends on how busy the station is — helpers who join as the line grows, a machine that slows near saturation, a birth–death chain with an arbitrary rate function — is bread-and-butter queueing. In Concourse a station's service law may read live occupancy through two expression atoms, and the interpreter re-evaluates the law mid-flight whenever a count it watches changes. Processor sharing, which always worked this way, is now one special case of the same machinery.","title":"State-dependent service laws"},{"category":"section","location":"manual/state_dependent/#The-atoms,-and-where-they-may-appear","page":"State-dependent service laws","text":"InService(:s) is the number of jobs in service at station :s, and InBuffer(:s) the number waiting, both as Float64 counts at the moment the law is evaluated. They compose with the rest of the expression algebra, so Param(:mu) * min(InService(:q) + InBuffer(:q), Const(3.0)) is a rate that grows with the crowd and saturates at three.\n\nOccupancy is live state, not a value frozen at enabling, so where the atoms may appear is restricted: only the service law of a station (compile check C5). A mark law reading state would push station state into the record's mark draws and break replay; a state-reading interarrival law is balking, a separate feature; patience laws, routing kernels, and discipline ordering keys are refused for their own reasons. As with every static check, compile names the problem:\n\nusing Concourse\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive;\n        interarrival = Law(:Exponential, scale = inv(Param(:lambda) + InService(:q))))\nstation!(net, :q; service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:q))\nroute!(net, :q, Always(:done))\ntry\n    compile(net)\ncatch err\n    println(err.msg)\nend\n\nThe reads_state census reports which stations a law watches. compile turns the census into a dependency map on the QueueGSMP (srv_readers and buf_readers), so the interpreter knows exactly which stations' service clocks to revisit when an in-service or buffer count changes — and revisits no others.","title":"The atoms, and where they may appear"},{"category":"section","location":"manual/state_dependent/#The-segment-convention","page":"State-dependent service laws","text":"A clock enabled under one occupancy may see that occupancy change before it fires. Each change closes one segment of the clock's history and opens another. Four quantities describe a clock mid-flight:\n\nte — the wall-clock enabling time. It is set once, when the clock enters the enabled set, and never rewritten while the clock lives.\naccrued effort — the service the job has received so far: wall time weighted by speed. Speed is 1 everywhere except processor sharing, where it is min(1, servers/n).\nbank — the effort accumulated over the closed segments (st.bank[k]).\nanchor — the wall time at which the current segment opened (st.anchor[k]).\n\nWhen a watched count changes at time t, every surviving in-service clock of a reader station banks the effort of the segment that just closed (at the old speed), moves its anchor to t, keeps te where it was, and is re-declared to the sampler with the delta (:reenable, k). The new law is the fresh base — the station's law rebuilt from the occupancy now — conditioned on the total draw exceeding the accrued effort: age carryover. Internally the wrapper is SharedRemaining(newbase, age, speed, shift), whose support starts at the anchor's offset from te.\n\nOne clock, walked through its life at speed 1:\n\nwall time what happens te bank anchor law declared to the sampler\n2.0 enabled; occupancy gives base F_1 2.0 — — F_1, measured from te\n5.0 a watched count changes: (:reenable, k) 2.0 3.0 5.0 F_2 conditioned on X  30, anchored at 5.0\n6.5 it changes again: (:reenable, k) 2.0 4.5 6.5 F_3 conditioned on X  45, anchored at 6.5\n8.0 the clock fires — — — the drawn X was 45 + (80 - 65) = 60\n\nAn alternative convention was considered and rejected: the quantile-equivalent (hazard-matching) mapping, which would carry the old law's survival quantile into the new law so that the clock's survival probability is continuous across the change. Age carryover won because it is what processor sharing already implements — a PS speed change is now literally this path, with the station registered as an srv-reader of itself, the base law unchanged, and speed min(1, servers/n) — and because it is what the ClockGradients fixed-anchor contract expects: te anchors every likelihood segment, and the score replay rebuilds the current segment's law at every state it visits. The convention is recorded as amendment A5 in notes/model_definition.tex.","title":"The segment convention"},{"category":"section","location":"manual/state_dependent/#M/M/c-as-one-state-dependent-server","page":"State-dependent service laws","text":"The classic birth–death identity: c exponential servers at rate mu each are indistinguishable from one server whose rate is mu cdot min(n c), with n the number in the system. The second formulation exercises the whole re-evaluation path — every arrival and departure below n = c changes the law of the clock in flight.\n\nc = 3.0\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nn = InService(:q) + InBuffer(:q)\nstation!(net, :q; servers = 1,\n         service = Law(:Exponential, scale = inv(Param(:mu) * min(n, Const(c)))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:q))\nroute!(net, :q, Always(:done))\nm = compile(net)\n\nrec = simulate(m, [2.1, 1.0], 20000.0; seed = 3)\ntime_average(number_in_system, m, rec)\n\nErlang C for M/M/3 at lambda = 21, mu = 1 puts the exact long-run mean at about 3.25. The test suite runs this comparison as a replicated 4-standard-error oracle test, both ways: the plain three-server station and this one-server disguise agree with the closed form and with each other.","title":"M/M/c as one state-dependent server"},{"category":"section","location":"manual/state_dependent/#A-cross-station-read","page":"State-dependent service laws","text":"The atoms are not confined to a station's own counts. Here station :b slows down while station :a is busy — :b is a reader of :a's in-service count and of nothing else:\n\nnet2 = QueueNetwork(param_names = (:la, :ma, :lb, :mb))\nsource!(net2, :src_a; interarrival = Law(:Exponential, scale = inv(Param(:la))))\nsource!(net2, :src_b; interarrival = Law(:Exponential, scale = inv(Param(:lb))))\nstation!(net2, :a; service = Law(:Exponential, scale = inv(Param(:ma))))\nstation!(net2, :b; service = Law(:Exponential,\n         scale = inv(Param(:mb) * (Const(1.0) - Const(0.5) * InService(:a)))))\nsink!(net2, :done)\nroute!(net2, :src_a, Always(:a))\nroute!(net2, :src_b, Always(:b))\nroute!(net2, :a, Always(:done))\nroute!(net2, :b, Always(:done))\nmx = compile(net2)\n\nFolding the record through fire_changes shows the mechanism: (:reenable, k) deltas appear exactly at firings that change :a's in-service count while :b has a survivor in service, and nowhere else.\n\nθ = [1.0, 2.0, 0.8, 2.0]\nrec2 = simulate(mx, θ, 300.0; seed = 41, debug = true)\nst = Concourse.initial_state(mx)\nnreen = 0\nfor i in eachindex(rec2.key)\n    ds = Concourse.replaydraws(rec2.key[i], rec2.draws[i], mx.params)\n    global st, deltas = fire_changes(mx, st, rec2.key[i], rec2.time[i], ds)\n    global nreen += count(d -> d[1] == :reenable, deltas)\nend\nnreen\n\nThe bank and anchor tables are part of QueueState, so replay reproduces state-dependent trajectories exactly, states_equal compares the segment bookkeeping field by field, and the debug oracle runs unchanged.\n\nTwo tools opt out, deliberately. Concourse.stability skips state-reading stations — their service law has no static mean, and the census principle is to report what the combinator values expose, never to invent. And branch_world refuses state-reading service laws with an ArgumentError, for the estimator reason the next section makes precise.","title":"A cross-station read"},{"category":"section","location":"manual/state_dependent/#Estimator-validity","page":"State-dependent service laws","text":"This table records which derivative estimators are guaranteed for which model features; later capabilities will extend it.\n\nModel feature Pathwise IPA Score SPA\ntheta-dependent mark laws not guaranteed not guaranteed not guaranteed\nState-dependent service laws not guaranteed valid valid\nBatch service (Batching) not guaranteed valid valid\nSibling cancellation (join! with cancel) not guaranteed valid valid\nRound service (Rounds) not guaranteed valid, except θ in a Dirac duration valid\nShortestQueue routing not guaranteed valid valid\n\ntheta-dependent mark laws (the existing branching restriction): a mark law that reads a parameter adds a derivative term of its own to every estimator, and that term is not implemented — branch_world refuses such models at construction rather than return a silently incomplete number.\nState-dependent laws, pathwise IPA: not guaranteed unbiased. IPA holds the event order fixed while perturbing times, but reordering events changes an occupancy, hence the law itself, discontinuously — the interchange argument behind IPA does not cover that coupling. This is why branch_world refuses state-reading service laws.\nState-dependent laws, score: valid. The likelihood of each inter-event interval is evaluated under the current segment's law, which the record and the te/bank/anchor bookkeeping pin down exactly. The test suite verifies E[score] = 0 and agreement with finite differences on birth–death models.\nState-dependent laws, SPA: the estimator remains valid — SPA's correction conditions on the realized order at each swap, so a law change caused by the swap sits inside the conditioning. The current spa_gradient, however, refuses any record containing re-declared clocks rather than handle multi-segment histories, so this validity has no code path yet; use the score estimator today.\nBatch service, score: valid. The batch clock's law reads only the batchsize mark, frozen at enabling like every mark, so each interval's likelihood is exactly what the record pins down — nothing state-dependent enters the law mid-flight.\nBatch service, pathwise IPA: not guaranteed unbiased. The batch size is integer-valued: a perturbation that reorders an arrival against a batch formation changes the composition by whole jobs, and the path jumps. Unlike state-reading laws, branch_world does not refuse batching models — the fragility is documented here, not enforced — because statistics insensitive to composition may still differentiate cleanly; the claim is the user's to make.\nBatch service, SPA: valid — a swap's correction conditions on the realized order, so the recomposition a swap causes sits inside the conditioning, as with a law change. Batch clocks are never re-declared, so these records do not trip spa_gradient's multi-segment refusal.\nSibling cancellation, score: valid. Cancellation consumes no draws and changes no law mid-flight: canceled clocks simply leave the enabled set at a firing the record pins down, so each inter-event interval's likelihood — survival terms of the canceled clocks included — is exactly what the record says it is.\nSibling cancellation, pathwise IPA: not guaranteed unbiased — this is the canonical order discontinuity. Which sibling wins a race is the event order: a perturbation that swaps two finish times swaps the winner's identity, and different work is canceled. IPA is valid only for statistics insensitive to that identity switch; as with batching, the fragility is documented, not enforced — branch_world does not refuse racing models, and the insensitivity claim is the user's to make.\nSibling cancellation, SPA: valid — a swap's correction conditions on the realized order, so the identity switch and the work it cancels sit inside the conditioning; SPA is the road to latency derivatives for racing models. Canceled clocks are removed, never re-declared, so these records do not trip spa_gradient's multi-segment refusal.\nRound service, score: valid for θ in the arrival, mark, or remark laws feeding a round station — round dynamics are a deterministic map of the recorded draws (the policy contract forbids θ), so the likelihood lives entirely in the laws that produced them — and valid as usual for θ in a density-bearing duration law, whose aggregates are frozen pseudo-marks. But θ inside a Dirac duration (Dai's staircase constants are the common case) has no score channel at all: a point mass bears no density, so the score estimator cannot see those parameters — a documented gap, not a silently wrong number.\nRound service, pathwise IPA: not guaranteed unbiased — the batching discontinuity in its sharpest form. Allocations are integer tokens: a perturbation that reorders a deposit against a round boundary changes the plan by whole tokens and whole jobs. This is why branch_world refuses models containing round stations in v1; score gradients over their records remain available for θ in the feeding laws.\nRound service, SPA: valid in the same sense as batching — a swap's correction conditions on the realized order, so the replanning a swap causes sits inside the conditioning, and SPA is where a derivative in a Dirac duration's constants would live (between reorderings the staircase moves event times smoothly). No analysis or code path exists for round records yet; the claim is the direction of travel, not a shipped estimator.\nShortestQueue, score: valid. The routing decision is a deterministic function of state — it draws nothing, records nothing, and bears no likelihood, so the record's likelihood is exactly the product of the terms the laws contribute, unchanged by the kernel.\nShortestQueue, pathwise IPA: not certified — a perturbation that reorders two events can flip a routing decision discontinuously, and a flipped destination is a different path by a whole job. branch_world refuses models containing ShortestQueue in v1, the state-reading refusal precedent.\nShortestQueue, SPA: valid — the flip a swap causes is a deterministic consequence of the realized order, so it sits inside the swap's conditioning like a cancellation's identity switch. As with the other SPA rows, spa_gradient has no code path for these records yet.\n\nClosed networks (populate!) earn no row: initial marks are ordinary recorded draws, consumed once at t = 0 and replayed from the record's init slot, so seeding introduces no new event-order discontinuity and no new likelihood term. A θ-reading initial mark law falls under the θ-dependent-mark row above — branch_world refuses it exactly as it refuses a source's.\n\nMark redraw on deposit (station!'s remark) earns no row for the same reason: remark draws are ordinary recorded mark draws, consumed at the depositing firing and replayed from its draw list, so they add no new likelihood term and no event-order discontinuity beyond what the marks they replace already carry. A θ-reading remark law falls under the θ-dependent-mark row above — branch_world refuses it exactly as it refuses a source's.\n\nSee Gradient estimation for the estimators themselves and for the processor-sharing experiment that first demonstrated the IPA failure on multi-segment records.","title":"Estimator validity"},{"category":"section","location":"queues/disciplines/#Line-disciplines","page":"Line disciplines","text":"When the server frees up and several jobs are waiting, someone has to decide who goes next. That rule is the discipline. Concourse ships six, and this page runs the same stream of work through all of them.\n\nThe experiment at the bottom of the page needs jobs whose service demands are known when they arrive. So each arriving job is stamped with a size — the exact amount of service it will need — drawn from a uniform distribution. A number stamped on a job is called a mark; marks get their own page. For now: every discipline below sees the identical arrival stream and the identical job sizes, and only the order of service changes.\n\nIn the diagrams, jobs are colored by arrival order: blue arrived first, then orange, then green. The head of the line (next to be served) is drawn nearest the server.","title":"Line disciplines"},{"category":"section","location":"queues/disciplines/#FCFS-—-first-come,-first-served","page":"Line disciplines","text":"The default in shops, banks, and fair-minded software: jobs are served in arrival order. The earliest arrival stands at the head.\n\nusing Luxor, QueueDiagrams, LaTeXStrings # hide\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nb = buffer(Point(-60, 0), 150, 40; slots = 5, jobs = [1, 2, 3]) # hide\ndiscipline(Point(-60, 0), 150, 40, \"FCFS\") # hide\ns = server(Point(75, 0), 22; inservice = 0) # hide\nflow(Point(-220, 0), b.entry) # hide\nflow(b.exit, s.entry) # hide\nflow(s.exit, Point(190, 0)) # hide\nend 460 120 # hide","title":"FCFS — first come, first served"},{"category":"section","location":"queues/disciplines/#LCFS-—-last-come,-first-served","page":"Line disciplines","text":"The newest arrival is served first, like plates in a stack. The blue job that arrived first now waits at the back — and every later arrival cuts ahead of it.\n\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nb = buffer(Point(-60, 0), 150, 40; slots = 5, jobs = [3, 2, 1]) # hide\ndiscipline(Point(-60, 0), 150, 40, \"LCFS\") # hide\ns = server(Point(75, 0), 22; inservice = 0) # hide\nflow(Point(-220, 0), b.entry) # hide\nflow(b.exit, s.entry) # hide\nflow(s.exit, Point(190, 0)) # hide\nend 460 120 # hide","title":"LCFS — last come, first served"},{"category":"section","location":"queues/disciplines/#SIRO-—-service-in-random-order","page":"Line disciplines","text":"When the server frees up, it picks a waiting job uniformly at random. Arrival order carries no weight at all.\n\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nb = buffer(Point(-60, 0), 150, 40; slots = 5, jobs = [2, 3, 1]) # hide\ndiscipline(Point(-60, 0), 150, 40, \"SIRO\") # hide\nsethue(\"black\"); fontsize(15) # hide\ntext(\"?\", Point(-60, -32), halign = :center) # hide\ns = server(Point(75, 0), 22; inservice = 0) # hide\nflow(Point(-220, 0), b.entry) # hide\nflow(b.exit, s.entry) # hide\nflow(s.exit, Point(190, 0)) # hide\nend 460 120 # hide","title":"SIRO — service in random order"},{"category":"section","location":"queues/disciplines/#Priority","page":"Line disciplines","text":"Jobs carry a priority key, and the buffer stays sorted by it: lowest key at the head, ties served in arrival order. In the diagram the blue class outranks the orange class, so blues wait at the head regardless of when they arrived. In the experiment below the key is the job's size, which makes this shortest job first: Priority(Mark(:size)). (A preempt = true variant can also evict the job currently in service, with a choice of whether the victim keeps its progress.)\n\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nb = buffer(Point(-60, 0), 150, 40; slots = 5, jobs = [1, 1, 2, 2]) # hide\ndiscipline(Point(-60, 0), 150, 40, \"Priority\") # hide\ns = server(Point(75, 0), 22; inservice = 1) # hide\nflow(Point(-220, 0), b.entry) # hide\nflow(b.exit, s.entry) # hide\nflow(s.exit, Point(190, 0)) # hide\nend 460 120 # hide","title":"Priority"},{"category":"section","location":"queues/disciplines/#PS-—-processor-sharing","page":"Line disciplines","text":"No line at all: every job present is served at once, each at an equal share of the server's speed. With n jobs present, each proceeds at speed 1n. This is the classical model of a time-sliced CPU (central processing unit).\n\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\ns = sharedserver(Point(0, 0), 40; jobs = [1, 2, 3, 4], label = \"PS\") # hide\nflow(Point(-180, 0), s.entry) # hide\nflow(s.exit, Point(180, 0)) # hide\nend 460 140 # hide","title":"PS — processor sharing"},{"category":"section","location":"queues/disciplines/#SRPT-—-shortest-remaining-processing-time","page":"Line disciplines","text":"The job with the least work left is served first, and a newly arrived shorter job preempts the one in service (the interrupted job keeps its progress and resumes later). SRPT provably minimizes the average number of jobs in the system. In the diagram the smallest job stands at the head.\n\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nb = buffer(Point(-60, 0), 150, 40; slots = 5) # hide\nfor (i, r) in enumerate((6, 9, 12)) # hide\n    job(Point(-60 + 75 - (i - 0.5) * 25, 0), r) # hide\nend # hide\ndiscipline(Point(-60, 0), 150, 40, \"SRPT\") # hide\ns = server(Point(75, 0), 22; inservice = 0) # hide\nflow(Point(-220, 0), b.entry) # hide\nflow(b.exit, s.entry) # hide\nflow(s.exit, Point(190, 0)) # hide\nend 460 120 # hide","title":"SRPT — shortest remaining processing time"},{"category":"section","location":"queues/disciplines/#Same-load,-six-disciplines","page":"Line disciplines","text":"Now the experiment. One station, arrival rate lambda = 08, job sizes uniform on 04 16 (mean 1), so the utilization is rho = 08 in every case. The service law Law(:Dirac, value = Mark(:size)) says \"this job's service time is exactly its stamped size\". Swapping the discipline is a one-argument change:\n\nusing Concourse\n\nfunction line_model(disc)\n    net = QueueNetwork(param_names = (:lambda,))\n    source!(net, :arrive;\n            interarrival = Law(:Exponential, scale = inv(Param(:lambda))),\n            mark = MarkLaw(size = Law(:Uniform, a = Const(0.4), b = Const(1.6))))\n    station!(net, :desk; discipline = disc, servers = 1,\n             service = Law(:Dirac, value = Mark(:size)))\n    sink!(net, :done)\n    route!(net, :arrive, Always(:desk))\n    route!(net, :desk, Always(:done))\n    compile(net)\nend\nnothing # hide\n\nFor each discipline we run eight independent simulations and measure each job's time in system (also called the sojourn or response time: waiting plus service). A job's clock starts at its arrival event and stops at its service-completion event, both read straight from the record:\n\nusing Statistics\n\nfunction sojourns(rec)\n    arr = Float64[]                    # arrival time of job i, in arrival order\n    dep = Dict{Int,Float64}()          # job id => departure time\n    for (k, t) in zip(rec.key, rec.time)\n        k[1] == :arrival && push!(arr, t)\n        k[1] == :service && (dep[Int(k[3])] = t)\n    end\n    [dep[j] - arr[j] for j in eachindex(arr) if haskey(dep, j)]\nend\n\ndisciplines = [\"FCFS\"     => FCFS(),\n               \"LCFS\"     => LCFS(),\n               \"SIRO\"     => SIRO(),\n               \"Priority\" => Priority(Mark(:size)),\n               \"PS\"       => ProcessorSharing(),\n               \"SRPT\"     => SRPT()]\n\nprintln(rpad(\"discipline\", 11), rpad(\"mean N\", 8), rpad(\"mean T\", 8),\n        rpad(\"std T\", 8), rpad(\"T90\", 7), \"T99\")\nfor (name, disc) in disciplines\n    md = line_model(disc)\n    Ts = Float64[]; Ns = Float64[]\n    for r in 1:8\n        rec = simulate(md, [0.8], 2000.0; seed = 20 + r)\n        append!(Ts, sojourns(rec))\n        push!(Ns, time_average(number_in_system, md, rec))\n    end\n    println(rpad(name, 11), rpad(round(mean(Ns), digits = 2), 8),\n            rpad(round(mean(Ts), digits = 2), 8),\n            rpad(round(std(Ts), digits = 2), 8),\n            rpad(round(quantile(Ts, 0.9), digits = 2), 7),\n            round(quantile(Ts, 0.99), digits = 2))\nend","title":"Same load, six disciplines"},{"category":"section","location":"queues/disciplines/#How-to-read-the-table","page":"Line disciplines","text":"FCFS, LCFS, and SIRO agree on the averages. Same mean number in system (mean N), same mean time in system (mean T). That is not luck: none of the three looks at job sizes, all keep the server busy whenever work is waiting, so they do the same total work and — by a law we meet on the next page — must share a mean. For the record, the Pollaczek–Khinchine formula of the M/G/1 page predicts textmean  T = 324 for this load; the measured means sit a few percent under it because a finite run starts empty and never counts the jobs still inside at the end.\n\nBut the spread is completely different. Look at std T and the percentile columns (T90 and T99: 90% and 99% of jobs finish within those times). Under FCFS your wait is bounded by the work ahead of you at arrival, so its tail is the shortest. LCFS is sneaky: its T90 is actually better than FCFS — most jobs arrive near the head and are served fast — but its T99 is three times worse, because a job that loses its turn keeps losing it to every later arrival. SIRO sits in between. Same averages, different victims: the number in the system may not care about the discipline, but who waits absolutely does.\n\nSize-aware disciplines beat the average. Priority-by-size and SRPT push small jobs ahead of big ones and cut both the mean and the tail. SRPT is the provable optimum for mean number in system.\n\nPS is not a free lunch. Processor sharing has the worst mean here. Sharing helps short jobs escape behind long ones, but these sizes are nearly uniform — there are no monsters to escape from — and slowing everyone down just makes everyone late. On the M/G/1 page, where service times get a heavy tail, PS will look much better. Which discipline wins depends on the service-time distribution — that is the next two pages' subject.","title":"How to read the table"},{"category":"section","location":"queues/networks/#Networks:-stations-wired-together","page":"Networks: stations wired together","text":"Real systems are rarely one station. A request hits a load balancer, then an application server, then a database, sometimes loops back for a retry. In Concourse a network is just more stations and more route! calls, and each route! takes a routing kernel that decides where a finished job goes:\n\nAlways(dest) — every job goes to dest.\nProbabilistic(a => 0.3, b => 0.7) — an independent coin flip per job.\nRoundRobin([a, b]) — alternate destinations in strict rotation.\nByMark(...) — route by a value stamped on the job (the marks page).\n\nThis page builds three little networks — a tandem line, a feedback loop, and a split — and checks each against exact theory.","title":"Networks: stations wired together"},{"category":"section","location":"queues/networks/#Tandem:-queues-in-series","page":"Networks: stations wired together","text":"Two stations in a row; every job passes through both.\n\nusing Luxor, QueueDiagrams, LaTeXStrings # hide\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nb1 = buffer(Point(-160, 0), 110, 38; slots = 4, jobs = [0, 0]) # hide\ns1 = server(Point(-70, 0), 20; inservice = 0, label = L\"\\mu_1\") # hide\nb2 = buffer(Point(60, 0), 110, 38; slots = 4, jobs = [0]) # hide\ns2 = server(Point(150, 0), 20; inservice = 0, label = L\"\\mu_2\") # hide\nflow(Point(-300, 0), b1.entry; label = L\"\\lambda\") # hide\nflow(b1.exit, s1.entry) # hide\nflow(s1.exit, b2.entry) # hide\nflow(b2.exit, s2.entry) # hide\nflow(s2.exit, Point(250, 0)) # hide\nend 640 150 # hide\n\nA classical result (Burke's theorem) says the departure stream of a stable M/M/1 queue is itself a Poisson process with rate lambda. So station 2 is also an M/M/1 queue, and the mean number in the whole system is just a sum of two single-queue formulas:\n\nL = fracrho_11-rho_1 + fracrho_21-rho_2\nqquad rho_i = lambdamu_i \n\nWith lambda = 1, mu_1 = 2, mu_2 = 16: L = 1 + 53 approx 2667.\n\nusing Concourse, Statistics\n\nfunction replicate(f, m, θ, horizon, nreps; seed0 = 1000)\n    vals = [f(simulate(m, θ, horizon; seed = seed0 + r)) for r in 1:nreps]\n    mean(vals), std(vals) / sqrt(nreps)\nend\n\nnet = QueueNetwork(param_names = (:lambda, :mu1, :mu2))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :first;  service = Law(:Exponential, scale = inv(Param(:mu1))))\nstation!(net, :second; service = Law(:Exponential, scale = inv(Param(:mu2))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:first))\nroute!(net, :first,  Always(:second))\nroute!(net, :second, Always(:done))\ntandem = compile(net)\n\nest, se = replicate(r -> time_average(number_in_system, tandem, r),\n                    tandem, [1.0, 2.0, 1.6], 2000.0, 16)\nprintln(\"measured L: \", round(est, digits = 3), \" ± \", round(se, digits = 3),\n        \"   theory: \", round(1.0 + 5 / 3, digits = 3))","title":"Tandem: queues in series"},{"category":"section","location":"queues/networks/#Feedback:-jobs-that-come-back","page":"Networks: stations wired together","text":"Now let a finished job need rework: after station 2, with probability q = 025 it is sent back to station 1, otherwise it leaves.\n\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\nb1 = buffer(Point(-150, -20), 110, 38; slots = 4, jobs = [0, 0]) # hide\ns1 = server(Point(-60, -20), 20; inservice = 0, label = L\"\\mu_1\") # hide\nb2 = buffer(Point(70, -20), 110, 38; slots = 4, jobs = [0]) # hide\ns2 = server(Point(160, -20), 20; inservice = 0, label = L\"\\mu_2\") # hide\nflow(Point(-290, -20), b1.entry; label = L\"\\lambda\") # hide\nflow(b1.exit, s1.entry) # hide\nflow(s1.exit, b2.entry) # hide\nflow(b2.exit, s2.entry) # hide\nflow(s2.exit, Point(270, -20); label = L\"1-q\") # hide\nfeedback(Point(160, 0), Point(-205, -14); drop = 60, label = L\"q\") # hide\nend 640 220 # hide\n\nThe routing now has a cycle, so how much traffic does each station really see? Balance the flow: station 1 receives the fresh stream lambda plus the fed-back fraction q of everything that completes, and in the long run what completes is what enters. Solving the traffic equations gives the effective arrival rate\n\nlambda_texteff = fraclambda1-q\n\nat both stations — each job makes a geometric number of passes, 1(1-q) on average. Jackson's theorem says each station then behaves as M/M/1 at rate lambda_texteff (remarkably, even though the merged stream-plus-feedback is not Poisson). With lambda = 1, q = 14: lambda_texteff = 43, and with mu_1 = 2 mu_2 = 4 the loads are 23 and 13:\n\nL = frac2313 + frac1323 = 25 \n\nnet = QueueNetwork(param_names = (:lambda, :mu1, :mu2))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :first;  service = Law(:Exponential, scale = inv(Param(:mu1))))\nstation!(net, :second; service = Law(:Exponential, scale = inv(Param(:mu2))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:first))\nroute!(net, :first,  Always(:second))\nroute!(net, :second, Probabilistic(:first => 0.25, :done => 0.75))\nloop = compile(net)\n\nest, se = replicate(r -> time_average(number_in_system, loop, r),\n                    loop, [1.0, 2.0, 4.0], 2000.0, 16)\nprintln(\"measured L: \", round(est, digits = 3), \" ± \", round(se, digits = 3),\n        \"   theory: 2.5\")\n\nEach probabilistic routing decision is one recorded coin flip in the record's draw list, which is why a run through this network still replays exactly.","title":"Feedback: jobs that come back"},{"category":"section","location":"queues/networks/#Splitting-a-stream:-coin-flips-versus-turn-taking","page":"Networks: stations wired together","text":"Send a rate-2 Poisson stream to two identical servers, first with Probabilistic (each job flips a fair coin), then with RoundRobin (strict alternation). The rates are identical — each server gets rate 1 — so are the queues the same?\n\nTheory says no, and the difference is instructive. A random split of a Poisson process thins it: each branch is again Poisson, so each server is an honest M/M/1 with rho = 05 and L = 1. But alternation is not thinning. Each server sees every second arrival, and the gap between its arrivals is a sum of two exponentials — more regular than Poisson. Smoother arrivals mean less queueing. The exact value comes from GI/M/1 theory (GI = general independent interarrivals): the mean number in system is rho(1-sigma), where sigma solves a fixed-point equation in the interarrival distribution's Laplace transform. Three lines compute it:\n\nσ = 0.5\nfor _ in 1:100\n    global σ = (1 / (2 - σ))^2     # Erlang-2 interarrivals, μ = 2\nend\nL_rr = 0.5 / (1 - σ)\nprintln(\"sigma = \", round(σ, digits = 4), \",  GI/M/1 prediction L = \",\n        round(L_rr, digits = 4))\n\nfunction split_model(kernel)\n    net = QueueNetwork(param_names = (:lambda, :mu))\n    source!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\n    station!(net, :a; service = Law(:Exponential, scale = inv(Param(:mu))))\n    station!(net, :b; service = Law(:Exponential, scale = inv(Param(:mu))))\n    sink!(net, :done)\n    route!(net, :arrive, kernel)\n    route!(net, :a, Always(:done))\n    route!(net, :b, Always(:done))\n    compile(net)\nend\n\ncoin = split_model(Probabilistic(:a => 0.5, :b => 0.5))\nturn = split_model(RoundRobin([:a, :b]))\n\nat_a(m) = (q = m.names[:a]; st -> Float64(length(st.buf[q]) + length(st.srv[q])))\n\nest_c, se_c = replicate(r -> time_average(at_a(coin), coin, r), coin, [2.0, 2.0], 2000.0, 16)\nest_t, se_t = replicate(r -> time_average(at_a(turn), turn, r), turn, [2.0, 2.0], 2000.0, 16)\nprintln(\"coin flips:  L at server a = \", round(est_c, digits = 3), \" ± \",\n        round(se_c, digits = 3), \"   theory: 1.0\")\nprintln(\"round robin: L at server a = \", round(est_t, digits = 3), \" ± \",\n        round(se_t, digits = 3), \"   theory: \", round(L_rr, digits = 3))\n\nSame arrival rate, same server, one-fifth less queue — purely from smoothing the arrival stream. (For the curious: sigma here is exactly (3-sqrt 5)2, so the golden ratio is hiding in the round-robin queue.)","title":"Splitting a stream: coin flips versus turn-taking"},{"category":"section","location":"queues/networks/#Joining-the-shortest-queue","page":"Networks: stations wired together","text":"Round robin smooths the arrival stream blindly. A real load balancer can do better by looking: send each job to whichever server currently holds the fewest. That is ShortestQueue, the one routing kernel that reads station state:\n\njsq = split_model(ShortestQueue(:a, :b))\nest_j, se_j = replicate(r -> time_average(at_a(jsq), jsq, r), jsq, [2.0, 2.0], 2000.0, 16)\nprintln(\"shortest queue: L at server a = \", round(est_j, digits = 3), \" ± \",\n        round(se_j, digits = 3))\n\nBetter than both the coin flips and the turn-taking — the join-the-shortest- queue (JSQ) system behaves nearly like a single pooled queue, because a server never idles while the other holds a waiting job. (There is no one-line formula this time; the exact value comes from solving the two-dimensional Markov chain numerically, which is how the test suite validates this kernel.)\n\nShortestQueue is deterministic: the smallest occupancy wins, and a tie goes to the destination with the lowest station index (declaration order). Occupancy means the total number of jobs at the destination — waiting, in service, and held blocked. For round stations (token-serving stations declared with a Rounds config) there is a second notion, ShortestQueue(dests...; by = :tokens), which compares total remaining work — the active jobs' token counters plus the waiting jobs' full profiles; it requires every destination to be a round station, since only round stations keep work counters.\n\nWhy is a state-reading route allowed when a state-reading ByMark expression is rejected? Because nothing random happens: Concourse's design rule (amendment A4) is that every likelihood-bearing decision must flow through a recorded draw, and a deterministic function of the state bears no likelihood. A ShortestQueue decision draws nothing and records nothing — replay reproduces it by recomputing the occupancies from the folded state.\n\nTwo caveats, both consequences of reading state:\n\nGradients. Score gradients over records remain valid — the decision contributes no likelihood factor — but pathwise IPA is not certified: a parameter perturbation that reorders two events can flip a routing decision discontinuously. branch_world therefore refuses models containing ShortestQueue in v1; use the score estimator over records.\nStability reports. Concourse.stability's traffic equations treat ShortestQueue as an equal split across its destinations. That is exact under symmetry (identical destinations share the flow equally) and an approximation otherwise, since JSQ shifts flow toward faster-draining stations.","title":"Joining the shortest queue"},{"category":"section","location":"queues/networks/#Open-versus-closed-networks","page":"Networks: stations wired together","text":"Every network on this page is open: jobs enter from outside, circulate, and leave. The other classical family is closed: a fixed population of N jobs circulates forever, never entering or leaving — the standard model for \"N users at terminals, each thinking, then submitting a job\" — and its theory (mean value analysis) prices interactive systems.\n\npopulate! declares exactly that: seed N jobs inside the network at time zero, and the network needs no source! at all. Four users at terminals — a station with four servers, so everyone thinks in parallel — submit jobs to one CPU and wait for the answer:\n\nnet = QueueNetwork(param_names = (:think, :serve))\nstation!(net, :terminals; servers = 4,\n         service = Law(:Exponential, scale = inv(Param(:think))))\nstation!(net, :cpu; service = Law(:Exponential, scale = inv(Param(:serve))))\nroute!(net, :terminals, Always(:cpu))\nroute!(net, :cpu, Always(:terminals))\npopulate!(net, :terminals, 4)\ninteractive = compile(net)\n\nrec = simulate(interactive, [0.5, 3.0], 2000.0; seed = 9)\ncpu = interactive.names[:cpu]\ntime_average(st -> Float64(length(st.buf[cpu]) + length(st.srv[cpu])),\n             interactive, rec)\n\nNo job ever enters or leaves — the four circulate forever between thinking and computing. The closed networks manual page develops the machinery: multiclass populations with drawn initial marks, the record's \"firing 0\" slot that keeps replay exact, and validation of simulated occupancies against the exact product-form answer via Buzen's algorithm.\n\nRouting kernels so far treat all jobs alike. The next page gives jobs identity — classes, sizes, service demands they carry with them.","title":"Open versus closed networks"},{"category":"section","location":"manual/batching/#Batch-service","page":"Batch service","text":"A server that takes several jobs at once — a diffusion furnace loading a tube of wafers, a GPU running inference over a padded batch of requests, a shuttle that leaves when enough passengers have boarded — serves a batch: one clock times the whole group, and the group's size may set that clock's law. In Concourse a station declares this with the batching keyword of station!; everything else — routing, reneging, blocking, records — keeps its usual meaning at the level of the individual jobs.","title":"Batch service"},{"category":"section","location":"manual/batching/#The-gathering-rule","page":"Batch service","text":"Batching(min, max) says: while a server slot is free and at least min jobs are waiting, gather up to max of them, in discipline order, into one batch. With fewer than min waiting the server idles — no clock counts down toward formation, because every arrival re-runs the dispatch step, so the batch forms at the instant the threshold is met. Two settings bracket the family:\n\nBatching() — the defaults min = 1, max = typemax(Int) — is the gather-everything rule: an idle server takes the entire line. This is the dynamic batching of Inoue (2021, arXiv:1912.06322), the worked model below.\nBatching(min = K, max = K) is classical fixed-size batch service, the M/M^K/1 of the textbooks. The test suite pins it against a truncated-CTMC solution of the batch chain.\n\nGathering must consume no randomness, and a batch in service must never be preempted or reordered — guarantees only the non-preemptive :back-insert FCFS discipline gives. Declaring batching under any other discipline is compile check C6:\n\nusing Concourse\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :furnace; discipline = LCFS(),\n         service = Law(:Exponential, scale = inv(Param(:mu))),\n         batching = Batching(min = 2, max = 2))\nsink!(net, :done)\nroute!(net, :arrive, Always(:furnace))\nroute!(net, :furnace, Always(:done))\ntry\n    compile(net)\ncatch err\n    println(err.msg)\nend","title":"The gathering rule"},{"category":"section","location":"manual/batching/#One-synthetic-job-per-batch","page":"Batch service","text":"When a batch forms, the gathered jobs leave the buffer and a fresh job enters service in their place: a new id whose only marks are (batchsize = Float64(k),), with k the number gathered. The service clock belongs to this synthetic job, so the service law reads the batch size the way any law reads any mark — Mark(:batchsize), no new expression atom — and the size is frozen at enabling, like every mark. A processing time affine in the batch size is one line:\n\nservice = Law(:Dirac, value = Const(τ0) + Const(α) * Mark(:batchsize))\n\nThe members themselves stay in st.jobs, keyed to their batch through st.batchmembers — like stashed join siblings, they are still in the system, just not in any line. Only the batch station's own service law may read batchsize (check C2): the mark exists on the synthetic job alone, and a batch job never gets a patience clock — it is in service by construction, and jobs in service do not renege.","title":"One synthetic job per batch"},{"category":"section","location":"manual/batching/#Members-before,-during,-and-after-the-batch","page":"Batch service","text":"Before. A waiting member is an ordinary waiting job. In particular it keeps its patience clock until the moment it is gathered, so reneging races batch formation exactly as it should: a job whose patience fires while the line is still below min abandons like any other, and the census identity arrivals = served + reneged + in system holds exactly.\n\nDuring. Members in a live batch have no clocks of their own; the one service clock is the batch's.\n\nAfter. When the batch's clock fires, the synthetic job dissolves and each member routes individually, in gather order, on its own marks — each member's routing kernel may consume its own recorded draw, so a Probabilistic route downstream of a batch station behaves per-member, not per-batch.\n\nOne decision deserves its rationale. :block overflow holds a finished job in the upstream server that produced it, and that server stays occupied until room frees downstream. But by the time a batch member is deposited, its batch has already fired — the server is free, and there is nothing left to hold the member. So a member that meets a full :block destination is dropped, exactly like an arrival straight from a source (which has no server behind it either). This is a documented modeling decision, to be revisited only if a target model needs member-blocking; the alternative — re-occupying the freed server — would silently reintroduce the batch after its completion event.","title":"Members before, during, and after the batch"},{"category":"section","location":"manual/batching/#Counting-members,-not-bookkeeping","page":"Batch service","text":"Two occupancy conventions follow from the synthetic-job mechanics:\n\nnumber_in_system counts members. A live batch contributes its k members, not k + 1 jobs — the batch id is bookkeeping, and the count subtracts one per entry in st.batchmembers.\nConcourse.number_at(q) counts residents of the station — buffer, servers, and held jobs — and there a live batch counts as one, because the srv vector holds the synthetic job.\n\nThe distinction is real: in M/M^K/1 the system count sees K members in service where the station count sees one batch. The test suite checks both against the same truncated-CTMC oracle.","title":"Counting members, not bookkeeping"},{"category":"section","location":"manual/batching/#Nothing-new-in-the-record","page":"Batch service","text":"Batch composition is a deterministic function of state — min, max, and discipline order decide everything, no randomness — so the record gains nothing: no new event kind, no new draw. Replay folds the same fire_changes and reforms every batch identically; states_equal compares st.batchmembers field by field, and the debug oracle runs unchanged. The sharpest form of \"the batch path adds nothing when inactive\" is also tested: under Batching(min = 1, max = 1) with a deterministic service law, the batch station reproduces the plain FCFS station's record — same times, same draws, same event kinds — differing only in the job-id slot, because batch ids are minted alongside member ids.","title":"Nothing new in the record"},{"category":"section","location":"manual/batching/#Inoue's-gather-everything-model","page":"Batch service","text":"Inoue (2021) analyzes exactly the defaults: an idle server takes the whole line, and the batch processing time is deterministic and affine in the batch size, tau(b) = tau_0 + alpha b — the observed behavior of GPU inference serving, where tau_0 is kernel launch overhead and alpha the per-request cost. The paper's Theorem 2 gives closed-form upper bounds on the mean latency. Here is the model at the paper's own parameter point (the Tesla V100 fit, normalized load rho = lambdaalpha = 05), with mean latency measured through Little's law:\n\nα, τ0 = 0.1438, 1.8874          # Inoue's Tesla V100 fit (§3.3)\nλ = 0.5 / α                     # normalized load ρ = λα = 0.5\n\nnet = QueueNetwork(param_names = (:lambda,))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :gpu;\n         service = Law(:Dirac, value = Const(τ0) + Const(α) * Mark(:batchsize)),\n         batching = Batching())         # min = 1, max = typemax(Int)\nsink!(net, :done)\nroute!(net, :arrive, Always(:gpu))\nroute!(net, :gpu, Always(:done))\nm = compile(net)\n\nrec = simulate(m, [λ], 4000.0; seed = 7)\nL = time_average(number_in_system, m, rec)\nW = L / λ                                # Little's law: mean latency\n\nφ0 = (α + τ0) / (2 * (1 - λ * α)) * (1 + 2 * λ * τ0 + (1 - λ * τ0) / (1 + λ * α))\nφ1 = 1.5 * τ0 / (1 - λ * α) + (α / 2) * (λ * α + 2) / (1 - λ^2 * α^2)\nprintln(\"mean latency: \", round(W, digits = 3),\n        \"   Theorem 2 bound: \", round(min(φ0, φ1), digits = 3))\n\nThe simulated latency sits below the bound, as Theorem 2 requires — at this load, just below varphi_1, matching the paper's Figure 4a. The test suite runs this comparison as a replicated 4-standard-error oracle test against both the bound and the exact stationary solution of the paper's batch-size Markov chain.","title":"Inoue's gather-everything model"},{"category":"section","location":"manual/batching/#Estimators","page":"Batch service","text":"The batch clock's law reads only the batchsize mark, frozen at enabling, so the score-function estimator's likelihood is exactly right as recorded — score is valid. Pathwise IPA is fragile: the batch size is integer-valued, and a perturbation that reorders an arrival against a batch formation changes the composition by whole jobs, a discontinuous jump the IPA interchange argument does not cover. The estimator-validity table records the full row.","title":"Estimators"},{"category":"section","location":"developer/design_questions/#Design-questions-and-outcomes","page":"Design questions and outcomes","text":"In July 2026, with every target queueing model simulating and most of them differentiating, four questions remained open. They were written up in plain English (notes/design_questions.tex), discussed, and resolved (notes/reading_design_questions.md records the decisions). This page states each question and what happened to it.\n\nThe centerpiece of this page is a refutation. Question 3 asked whether the pathwise derivative estimator works on processor-sharing records. It does not, and the experiment that showed it also located why. Recording what did not work — precisely, with the evidence pinned in tests — is the whole value of this section.\n\nBackground terms used throughout: IPA is infinitesimal perturbation analysis, the pathwise estimator that freezes the order of firings and differentiates their times. SPA is smoothed perturbation analysis, IPA plus a correction for order changes. PS is processor sharing, the discipline that serves all jobs at a station simultaneously. A segment is one stretch of a clock's life under one declared distribution; PS re-declares clocks whenever the number of sharing jobs changes, so PS clocks have many segments. The four estimators are described for users in the Manual.","title":"Design questions and outcomes"},{"category":"section","location":"developer/design_questions/#Question-1:-cloning-worlds-that-use-auxiliary-draws","page":"Design questions and outcomes","text":"The question. The branchable world (needed by the branching and SPA estimators) refused any model that uses marks or random routing. Replay of a record is safe for those models, because recorded values are read back. But a clone runs forward, past the end of any record: its future arrivals need new sizes, its future routing needs new coin flips. Where should those live random streams live, what should re-seeding cover, and — hardest — what should the draws be keyed by, so that two same-seed clones see matching randomness even after their event orders diverge?\n\nThe outcome: decided and implemented. Three rules:\n\nThe auxiliary streams live in the world object, beside the sampler. They are not the sampler's business.\nRe-seeding covers both families. A rekey re-seeds the auxiliary streams along with the clock streams; re-seeding only the clocks would leave the clone replaying the original's future sizes.\nDraws are keyed by a stable identity, never by request order. A mark draw is keyed by the source's own clock and consumed once per arrival, so the fifth arrival of clone A gets the same size as the fifth arrival of clone B, whatever else fired in between — the \"position in the source's own sequence\" rule.\n\nThe separable sub-question — marks whose distributions read theta — was deferred: such a mark adds its own derivative term to every estimator, and that term is future work. theta-dependent mark laws are refused at world construction.\n\nEvidence. test/test_branch.jl: a coupled clone reproduces firings and marks exactly; a rekeyed clone draws fresh sizes; same-seed clones agree on corresponding jobs' marks after forced divergence; and branching_gradient matches finite differences on a Bernoulli routing split and on a SITA mark split.","title":"Question 1: cloning worlds that use auxiliary draws"},{"category":"section","location":"developer/design_questions/#Question-2:-SPA's-correction-for-re-declared-clocks","page":"Design questions and outcomes","text":"The question. SPA works for clocks that keep one distribution for life and refuses records containing multi-segment clocks. The refusal is not an implementation gap: the mathematics of the correction term had not been derived for the multi-segment case. The correction weighs each near-tie by the runner-up's hazard and by the jump the swap would cause; when the forced firing itself re-declares the survivor — which under PS it always does — the existing correctness argument no longer covers the construction.\n\nThe outcome: the derivation was written. Following the project's practice, the answer to \"this needs to be derived\" was a report: notes/spa_derivation.tex rebuilds SPA from first principles, then works the multi-segment extension. Two findings, summarized on The SPA estimator page:\n\nThe boundary weight generalizes without new mathematics: it is the hazard of the latest segment's declared law, and the commuting gate (the zero-jump shortcut) survives re-declaration whenever the two forced orders re-coalesce.\nThe genuinely new mathematics is a chain derivative showing that the velocity term — which SPA takes from the pathwise replay — is exactly what the current replay gets wrong on multi-segment records. So the multi-segment problem is not, first, an SPA problem; it is a problem in the object SPA stands on.\n\nThe decision on refusals: they stay whole-record. If any part of a record is beyond the derivation, the user hears a clear no rather than a quietly wrong number. Implementation is gated by the falsification ladder in the derivation; until its first steps pass, SPA's multi-segment refusal stands.","title":"Question 2: SPA's correction for re-declared clocks"},{"category":"section","location":"developer/design_questions/#Question-3:-IPA-over-processor-sharing-records","page":"Design questions and outcomes","text":"The question. The IPA machinery was written with multi-segment records in mind, and the score estimator — which shares the record structure — passes its tests on PS. But IPA itself had never been tested on a PS record. \"Untested\" and \"working\" are different claims. The proposed move was an experiment: run IPA on M/M/1-PS against finite differences, the same comparison every other estimator passed.\n\nThe outcome: refuted. The experiment (lambda = 1, mu = 2, horizon 50, 400 records, common-random-number central finite differences on the same seeds, occupancy integral int_0^H Ndt) answers no, under both couplings:\n\n partial_lambda partial_mu\nfinite differences 934 pm 39 -477 pm 18\nIPA, redraw coupling 577 pm 34 -100 pm 01\nIPA, carry coupling 2060 pm 118 -362 pm 12\n\nThree facts sharpen the failure into something usable.\n\nFirst, the machinery runs clean. The dual numbers flow without errors, and the replay reproduces the recorded firing times at the sampling parameter. The failure is a wrong number, not a crash — the worst failure mode, because nothing warns the user.\n\nSecond, a degenerate control localizes the bias. A PS station with 64 servers re-declares every survivor at every count change, yet at the test load the speed is always 1, so its dynamics are identical to FCFS — but the PS records carry multi-segment chains while the FCFS records are segment-free. On the segment-free records, both couplings match finite differences. On the multi-segment records of the same law, both are badly biased. Identical dynamics, opposite verdicts: the bias lives entirely in the record's segment representation.\n\nThird, the mechanism is located, and it is structural. The replay rebuilds each segment's law from states folded at the recorded floating-point times, so the law's bank, anchor, and speed are frozen constants with no derivative. The sensitivity of a re-declaration boundary time — itself a theta-dependent event time of the race being differentiated — has no channel into the replayed times. A candidate one-line repair was tried and refuted: it changed nothing, because the frozen anchors remain frozen either way. The complete repair needs the segment parameters to be dual-valued functions of the replayed times — which is Question 4's territory — and what the correct derivative even is, once that channel exists, is Question 2's derivation.\n\nStatus. Pinned as charter item F14 in test/test_ps.jl, with fixed seeds, including the degenerate-segment control. Consequence for users today: on PS records the valid estimators are score and branching, both tested against finite differences. IPA silently returns a confident wrong number — which is the failure mode the score pairing exists to detect.","title":"Question 3: IPA over processor-sharing records"},{"category":"section","location":"developer/design_questions/#Question-4:-should-the-record-store-segment-boundaries?","page":"Design questions and outcomes","text":"The question. The record stores only firings: clock, time, auxiliary values. Enabling times and re-declarations are reconstructed by re-running the model's rules over the firing list. Reconstruction keeps the record minimal and forces the model's rules to be the single source of truth, with a real audit benefit: every run cross-checks the rules against the simulation. But reconstruction walks the whole record, rebuilds every active clock's distribution after every firing, and rests on the assumption that a re-declaration always visibly changes the distribution. Should the record store segment boundaries instead?\n\nThe outcome: held open, with a stated preference. The decision was to keep the record minimal — it is the filtration, in the statistical sense — and use reconstruction while developing, treating storage as a possible later optimization. The Question 3 refutation then gave the question a derivative-driven edge: the repair for IPA-on-PS needs segment parameters that carry derivatives, which means either the reconstruction learns to fold at dual-valued times (the preferred route, keeping the record minimal), or the record stores boundary indices so boundary ages can be rebuilt as differences of replayed times. The recommendation in notes/spa_derivation.tex is to make the reconstruction dual-capable and revisit storing boundaries only if that proves too costly.","title":"Question 4: should the record store segment boundaries?"},{"category":"section","location":"developer/design_questions/#Summary","page":"Design questions and outcomes","text":"Question Kind Outcome\n1. Auxiliary draws in clones engineering decided and implemented; tested in test/test_branch.jl\n2. SPA with re-declaration research derivation written (notes/spa_derivation.tex); implementation gated by its falsification ladder\n3. IPA on PS records experiment refuted (charter F14, test/test_ps.jl); score and branching are the valid estimators on PS\n4. Segments in the record architecture held open; reconstruction preferred, now with a plan to make it dual-capable","title":"Summary"},{"category":"section","location":"developer/bibliography/#Bibliography","page":"Bibliography","text":"The literature that shaped Concourse's design lives in notes/queuepapers/, as full-text extractions selected to support implementing the three-layer architecture. This page is the annotated index; notes/queuepapers/INDEX.md is its source and carries extraction details (two OCR pipelines fed the directory, so in the noisier files, trust the math over the surrounding prose).\n\nThe selection criteria, in the order they mattered: how to map a queueing model onto a generalized semi-Markov process (GSMP) you can execute with a next-event rule; how to specify a queueing model in the first place; and works the design notes cite by name.","title":"Bibliography"},{"category":"section","location":"developer/bibliography/#The-central-one","page":"Bibliography","text":"**Zimmermann (2007). *Stochastic Discrete Event Systems.*** The closest published relative of Concourse's design. Zimmermann's thesis is the same thesis as notes/queue_layers.tex: many surface model classes share one underlying abstract model, so define the abstraction once and translate each surface class into it. His Chapter 2 (a unified description for stochastic discrete event systems) maps to Concourse's intermediate representation, Chapter 4 (queuing models) to the surface language and its compilation, Chapters 5–6 (Petri nets) to the :fresh/:resume preemption vocabulary, and Chapter 9 (efficient simulation) to the sampler layer. Also the source for the immediate-transition / vanishing-marking treatment the cascade worklist restates for queues.","title":"The central one"},{"category":"section","location":"developer/bibliography/#Mapping-a-queueing-model-to-a-GSMP","page":"Bibliography","text":"**Glasserman (1991). *Gradient Estimation via Perturbation Analysis.*** Chapter 2 defines the GSMP with a detailed construction of its sample paths — the canonical statement of the clock-speed formalism that Concourse compiles away, and the text the time-change proposition is checked against. Also the standard reference for infinitesimal perturbation analysis (IPA), the pathwise estimator.\n\n**Bogliolo et al. (2004). Specification and Analysis of Power-Managed Systems.*** The most concrete GSMP-with-clock-speeds implementation in the collection: an explicit array of clock readings and a literal state-by-event speed matrix, with an execution semantics. Useful precisely *because it does the thing Concourse argues against — it is the baseline the truncate-and-rescale compilation replaces, and the reference implementation named in charter item F8.\n\n**Dolgert (2016). *Continuous-Time Discrete-Event Simulation from Counting Processes.*** The tightest statement of the \"which event fires next\" rule: hazard rates to counting processes to a transition kernel, with four ways to sample it. Argues that GSMP and generalized stochastic Petri nets (absent immediate and simultaneous events) are computationally the same class. The bridge from the intermediate representation to CompetingClocks.\n\n**Reinhardt et al. (2022). *ML3: A Language for Agent-Based Discrete-Event Simulation.*** The one prior art for the whole stack: a domain-specific surface language whose semantics are defined in terms of GSMPs, with abstract syntax, formal semantics, and the simulation algorithms the semantics imply. Evidence that compile-surface-syntax-to-GSMP is a known-good architecture rather than a bet; also bears on keeping routing-kernel state declarative instead of hidden in closures.\n\n**Lindemann and Shedler (1999). *Numerical Analysis of Generalized Semi-Markov Processes.*** Short. Finite-state GSMPs with concurrent deterministic events, worked on a small non-Markovian queue with an exact numerical answer — candidate test oracle for the cases the product-form (BCMP) detector cannot reach.\n\n**Carnevali et al. *ORIS: A Tool for Non-Markovian Evaluation.*** Tool paper: stochastic time Petri nets over an underlying GSMP, with a passenger-queue case study. Read for its engine/analysis split — a working instance of the architecture diagram in which a checker and several analyzers attach to one intermediate representation.","title":"Mapping a queueing model to a GSMP"},{"category":"section","location":"developer/bibliography/#Specifying-queueing-models","page":"Bibliography","text":"**Shedler (1987). Regeneration and Networks of Queues.*** Cited twice by the design notes: the job stack (the buffer order *is the discipline's data, so there is no per-discipline container type) and passage times (the two-predicate fold over the record reimplements his marked-job specification, with the record replacing the state augmentation his 1987 memory budget forced him to compress). Also feeds the regenerative estimation material in the Manual's statistics page.\n\nLipsky (2009). Queueing Theory: A Linear Algebraic Approach, 2nd ed. Matrix-analytic closed forms, a semi-Markov process chapter, and a section on networks of non-exponential servers — the case the sampler layer exists to handle. Oracle material beyond the product-form reach of BCMP.","title":"Specifying queueing models"},{"category":"section","location":"developer/bibliography/#Quarton-documents","page":"Bibliography","text":"notes/queuepapers/quarton_docs/ preserves design documents from Quarton.jl, the predecessor project the architecture note argues against throughout. definition.md is the source of the sub-event ordering worry (\"the danger of separating these steps is that we might exclude a useful model\") that the fixed deposit!/settle! cascade order resolves, and redesign_ordering.md is the ordering problem itself.","title":"Quarton documents"},{"category":"section","location":"developer/bibliography/#Cited-but-not-in-the-collection","page":"Bibliography","text":"Harchol-Balter, Performance Modeling and Design of Computer Systems. The claim \"every statistic in Harchol-Balter's book is a fold over the record\" is made against a book the collection does not carry.\nBaskett, Chandy, Muntz, and Palacios (1975) — the BCMP theorem, which the product-form oracle detector (check C5) rests on. The notable gap; Zimmermann Chapter 4 and Lipsky carry enough of the product-form conditions to reconstruct the check.\nThe GSMP clock-speed literature (Glynn; Haas) that the compile-away decision leans on; Glasserman Chapter 2 is the closest substitute and is included.\nGong and Ho; Fu and Hu — the origin and the book-length treatment of smoothed perturbation analysis, followed closely by notes/spa_derivation.tex (see The SPA estimator).","title":"Cited but not in the collection"},{"category":"section","location":"reference/expressions/#Expression-algebra","page":"Expression algebra","text":"Every random quantity in a model — interarrival times, service times, patience, marks — is declared as a law written in this small algebra. A law is data, not a closure, so compile can see exactly which parameters, marks, and times each law reads and check the model statically. The surface language consumes these values, and the IR evaluates them when clocks are enabled.","title":"Expression algebra"},{"category":"section","location":"reference/expressions/#Expressions","page":"Expression algebra","text":"","title":"Expressions"},{"category":"section","location":"reference/expressions/#Laws","page":"Expression algebra","text":"","title":"Laws"},{"category":"section","location":"reference/expressions/#Inspection","page":"Expression algebra","text":"These predicates report what an expression or law reads. The compile-time checks are built on them.","title":"Inspection"},{"category":"type","location":"reference/expressions/#Concourse.ScalarExpr","page":"Expression algebra","text":"ScalarExpr\n\nAbstract supertype of the expression algebra that law arguments are written in. A ScalarExpr is a value, not a closure, so the compile-time checker can see what an expression reads: parameters (reads_params), marks (reads_marks), time (reads_time), and station occupancy (reads_state).\n\nThe leaves are Param, Mark, Enab, Const, InService, and InBuffer. Expressions combine with +, -, *, /, inv, log, exp, sqrt, min, max, ceil, and floor. A bare number in arithmetic with an expression is promoted to a Const. The staircase functions ceil and floor are flat almost everywhere, so a parameter inside their argument contributes no pathwise derivative — genuinely, not as an approximation (a dual number entering them leaves as a constant).\n\nExample\n\njulia> rate = Param(:lambda) * 2;\n\njulia> reads_params(rate)\nSet{Symbol} with 1 element:\n  :lambda\n\n\n\n\n\n","title":"Concourse.ScalarExpr"},{"category":"type","location":"reference/expressions/#Concourse.Param","page":"Expression algebra","text":"Param(name::Symbol)\n\nThe model parameter called name. Evaluation reads the entry of the parameter vector θ at the position compile assigned to name from the network's param_names. A law must reach θ through Param; that is how the checker and the gradient estimators know which clocks depend on which parameters.\n\n\n\n\n\n","title":"Concourse.Param"},{"category":"type","location":"reference/expressions/#Concourse.Mark","page":"Expression algebra","text":"Mark(name::Symbol)\n\nThe mark called name on the job the law applies to. Marks are per-job values drawn once at job creation (see MarkLaw). A service law that reads Mark(:size) gives each job its own service distribution.\n\n\n\n\n\n","title":"Concourse.Mark"},{"category":"type","location":"reference/expressions/#Concourse.Enab","page":"Expression algebra","text":"Enab()\n\nThe wall-clock time at which the clock being built was enabled. This is the only time a law may read. In a generalized semi-Markov process (GSMP) a clock's distribution is frozen at its enabling, so \"current time\" has no meaning inside a law. Use Enab for time-varying rates, for example a nonhomogeneous arrival process:\n\nLaw(:Exponential,\n    scale = inv(Param(:lambda) * (Const(1.0) + Const(0.5) * exp(Const(-0.02) * Enab()))))\n\n\n\n\n\n","title":"Concourse.Enab"},{"category":"type","location":"reference/expressions/#Concourse.Const","page":"Expression algebra","text":"Const(value)\n\nA literal number as a ScalarExpr. Bare numbers in arithmetic with expressions are promoted to Const automatically, so Param(:mu) * 2 and Param(:mu) * Const(2.0) are the same expression.\n\n\n\n\n\n","title":"Concourse.Const"},{"category":"type","location":"reference/expressions/#Concourse.InService","page":"Expression algebra","text":"InService(station::Symbol)\n\nThe number of jobs currently in service at the station named station, as a Float64 count. Occupancy is live state, not a value frozen at enabling: only the service law of a station may read it (compile check C5), and whenever the count changes the law is re-evaluated mid-flight, with the job's accrued service effort carried over and te left at the original enabling — the same segment convention processor sharing uses.\n\n# A server that speeds up with the crowd, saturating at three helpers.\nLaw(:Exponential, scale = inv(Param(:mu) * min(InService(:cpu), Const(3.0))))\n\n\n\n\n\n","title":"Concourse.InService"},{"category":"type","location":"reference/expressions/#Concourse.InBuffer","page":"Expression algebra","text":"InBuffer(station::Symbol)\n\nThe number of jobs waiting in the buffer at the station named station, as a Float64 count. The rules of InService apply: only the service law of a station may read occupancy (compile check C5), and the law is re-evaluated whenever the watched count changes.\n\n\n\n\n\n","title":"Concourse.InBuffer"},{"category":"type","location":"reference/expressions/#Concourse.Law","page":"Expression algebra","text":"Law(family::Symbol; kwargs...)\n\nA probability distribution written as data. family names a Distributions.jl type, and each keyword argument is one constructor argument, given as a ScalarExpr or a number. The keyword names are labels for the reader; the values are passed to the constructor positionally, in the order written. So write them in the constructor's positional order.\n\nBecause a Law is inspectable data, reads_params, reads_marks, and reads_time report exactly what it depends on, and compile can check the model statically.\n\nExamples\n\nLaw(:Exponential, scale = inv(Param(:mu)))            # Exponential with mean 1/μ\nLaw(:Gamma, shape = Const(2.0), scale = Const(0.5) * inv(Param(:mu)))\nLaw(:Dirac, value = Mark(:size))                      # deterministic, per job\n\n\n\n\n\n","title":"Concourse.Law"},{"category":"type","location":"reference/expressions/#Concourse.Opaque","page":"Expression algebra","text":"Opaque(f; params=Symbol[], marks=Symbol[], time=false)\n\nThe escape hatch for laws the expression algebra cannot write: an arbitrary function made legal by declaring what it reads. f has the signature (θnamed, marks, te) -> distribution, where θnamed is a NamedTuple of the declared params, marks is the job's mark NamedTuple, and te is the enabling time. The declarations params, marks, and time tell the checker what f reads; the checker trusts them, so its conclusions about an Opaque law are unverified.\n\nExample\n\nOpaque((θ, marks, te) -> Exponential(1 / (θ.mu * marks.weight));\n       params = [:mu], marks = [:weight])\n\n\n\n\n\n","title":"Concourse.Opaque"},{"category":"type","location":"reference/expressions/#Concourse.MarkLaw","page":"Expression algebra","text":"MarkLaw(; name = law, ...)\n\nA named collection of laws, one per mark. Marks are drawn once at job creation, in the written order, and recorded, so replay reproduces them. A later law in the list may read an earlier mark of the same job. Attach a MarkLaw to a source with the mark keyword of source!.\n\nExample\n\nMarkLaw(size = Law(:Exponential, scale = Const(1.0)),\n        class = Law(:Uniform, a = Const(0.0), b = Const(2.0)))\n\n\n\n\n\n","title":"Concourse.MarkLaw"},{"category":"function","location":"reference/expressions/#Concourse.reads_params","page":"Expression algebra","text":"reads_params(x) -> Set{Symbol}\n\nThe parameter names that a ScalarExpr or a law reads through Param. For an Opaque law it returns the declared names. compile checks every name against the network's param_names.\n\nExample\n\njulia> reads_params(Law(:Exponential, scale = inv(Param(:mu))))\nSet{Symbol} with 1 element:\n  :mu\n\n\n\n\n\n","title":"Concourse.reads_params"},{"category":"function","location":"reference/expressions/#Concourse.reads_marks","page":"Expression algebra","text":"reads_marks(x) -> Set{Symbol}\n\nThe mark names that a ScalarExpr or a law reads through Mark. For an Opaque law it returns the declared names. compile checks that every read mark is produced by some source.\n\nExample\n\njulia> reads_marks(Mark(:size) - Const(1.0))\nSet{Symbol} with 1 element:\n  :size\n\n\n\n\n\n","title":"Concourse.reads_marks"},{"category":"function","location":"reference/expressions/#Concourse.reads_time","page":"Expression algebra","text":"reads_time(x) -> Bool\n\nWhether a ScalarExpr or a law reads the enabling time through Enab. For an Opaque law it returns the declared time flag.\n\nExample\n\njulia> reads_time(exp(Const(-0.02) * Enab()))\ntrue\n\njulia> reads_time(Param(:mu))\nfalse\n\n\n\n\n\n","title":"Concourse.reads_time"},{"category":"function","location":"reference/expressions/#Concourse.reads_state","page":"Expression algebra","text":"reads_state(x) -> Set{Symbol}\n\nThe stations whose occupancy a ScalarExpr or a law reads through InService or InBuffer. An Opaque law cannot read state — its callback never receives a state view — so it reads none. compile permits a nonempty set only in the service law of a station (check C5).\n\nExample\n\njulia> reads_state(Param(:mu) * min(InService(:cpu), Const(3.0)))\nSet{Symbol} with 1 element:\n  :cpu\n\n\n\n\n\n","title":"Concourse.reads_state"},{"category":"section","location":"reference/interpreter/#Interpreter-and-record","page":"Interpreter and record","text":"The interpreter connects the IR to the bottom layer, CompetingClocks.jl, which samples the clocks. simulate runs the event loop and writes a MarkedRecord — the primary observable. Every statistic is a fold over the record, and replay reproduces the exact state trajectory from it without touching a random number generator. The record also feeds the gradient estimators.","title":"Interpreter and record"},{"category":"section","location":"reference/interpreter/#Running-and-measuring","page":"Interpreter and record","text":"","title":"Running and measuring"},{"category":"section","location":"reference/interpreter/#Auxiliary-draws","page":"Interpreter and record","text":"Every draw that is not a clock — marks, probabilistic routing, SIRO's pick, a round policy's randomness — flows through one conduit, the draw source (amendment A4). Live sources record every value into the firing's draw list; replay sources answer from the record and never touch a random number generator. A custom RoundPolicy is the one place user code receives a draw source: draw through Concourse.draw! and the policy replays for free.","title":"Auxiliary draws"},{"category":"section","location":"reference/interpreter/#Runtime-errors","page":"Interpreter and record","text":"A model compiled with allow_blocking_cycles = true (see compile) raises one typed exception when a cycle of full :block buffers wedges; it arrives carrying the partial record, so the deadlock is replayable evidence. The Cycles section of the blocking tutorial shows it in action.","title":"Runtime errors"},{"category":"section","location":"reference/interpreter/#Samplers","page":"Interpreter and record","text":"FirstToFireMethod, NextReactionMethod, and FirstReactionMethod are re-exported from CompetingClocks.jl. Pass one as the method keyword of simulate or branch_world. The manual page Choosing a sampler discusses when each is the right choice.","title":"Samplers"},{"category":"function","location":"reference/interpreter/#Concourse.simulate","page":"Interpreter and record","text":"simulate(m, θ, horizon; seed=0, method=nothing, worklist=:fifo,\n         debug=false, keep_states=false) -> MarkedRecord\n\nRun the compiled model m at parameter vector θ until the next firing would pass horizon, and return the MarkedRecord of what fired.\n\nθ: parameter values, ordered as the network's param_names.\nseed: seeds the master random number generator. The same seed gives the same record.\nmethod: nothing lets CompetingClocks.jl's builder choose the sampler; pass FirstToFireMethod(), NextReactionMethod(), or FirstReactionMethod() to override.\nworklist: order of the settle cascade, :fifo or :lifo. Both reach the same fixed point.\ndebug: after every firing, recompute the enabled clock set from the state and compare it against the sampler; error on drift.\nkeep_states: also return the live state trajectory, as (record, states), for comparison against replay.\n\nExample\n\nm = compile(net)                          # an M/M/1 with θ = (λ, μ)\nrec = simulate(m, [1.0, 2.0], 2000.0; seed = 7)\ntime_average(number_in_system, m, rec)    # near ρ/(1-ρ) = 1\n\n\n\n\n\n","title":"Concourse.simulate"},{"category":"type","location":"reference/interpreter/#Concourse.MarkedRecord","page":"Interpreter and record","text":"MarkedRecord\n\nWhat a simulation produces: for each firing, the clock key (key), the firing time (time), and the auxiliary draws that firing consumed in consumption order (draws, a vector of purpose => value pairs per firing), plus the horizon the run covered. length(rec) is the number of firings.\n\ninit is the \"firing 0\" slot: the draws that seeding the network's populate! population consumed at t = 0 — initial marks in declaration order, plus any draw the t = 0 dispatch made. It is empty for a network without a population.\n\nThe record is the primary observable. Every statistic is a fold of fire_changes over it, and because the draws are recorded, replay reproduces the exact state trajectory without touching a random number generator or the parameter vector.\n\n\n\n\n\n","title":"Concourse.MarkedRecord"},{"category":"function","location":"reference/interpreter/#Concourse.replay","page":"Interpreter and record","text":"replay(m, rec; worklist=:fifo) -> Vector{QueueState}\n\nThe state trajectory as a fold of fire_changes over the record, including the initial state; index i+1 is the state just after firing i.\n\n\n\n\n\n","title":"Concourse.replay"},{"category":"function","location":"reference/interpreter/#Concourse.time_average","page":"Interpreter and record","text":"time_average(g, m, rec) -> Float64\n\nThe time average of g(state) over [0, horizon], exact between events because the state is constant there. g maps a QueueState to a number, for example number_in_system.\n\nExample\n\nrec = simulate(m, θ, 2000.0; seed = 1)\nL = time_average(number_in_system, m, rec)   # mean number in system\n\n\n\n\n\n","title":"Concourse.time_average"},{"category":"function","location":"reference/interpreter/#Concourse.number_in_system","page":"Interpreter and record","text":"number_in_system(st::QueueState) -> Int\n\nThe number of jobs in the system: every live job, whether waiting, in service, held blocked, or stashed at a join — and the members of a batch, never the synthetic batch job itself. Pass it to time_average to estimate L, the time-average number in system.\n\n\n\n\n\n","title":"Concourse.number_in_system"},{"category":"type","location":"reference/interpreter/#Concourse.DrawSource","page":"Interpreter and record","text":"DrawSource\n\nThe single conduit for auxiliary draws (Amendment A4). A live source (built by livedraws) draws from a keyed RNG stream and appends each value to consumed, which becomes the firing's draw record. A replay source (built by replaydraws) returns recorded values in order and never touches an RNG — replay needs neither streams nor θ, which is what makes the contract's fire θ-free and deterministic given the record.\n\nFields: streams/θ are nothing in replay mode; replay is nothing in live mode; firing is the clock whose fire is consuming draws; ri is the replay cursor into replay.\n\n\n\n\n\n","title":"Concourse.DrawSource"},{"category":"function","location":"reference/interpreter/#Concourse.livedraws","page":"Interpreter and record","text":"livedraws(streams, firing, params, θ) -> DrawSource\n\nA live draw source for the firing of clock firing: draws come from streams keyed by (firing, purpose), and every value is appended to the source's consumed list, which the interpreter stores as the firing's draw record.\n\n\n\n\n\n","title":"Concourse.livedraws"},{"category":"function","location":"reference/interpreter/#Concourse.replaydraws","page":"Interpreter and record","text":"replaydraws(firing, recorded, params) -> DrawSource\n\nA replay draw source: draw and mark requests during the fire of firing are answered from recorded in order, checking that each request's purpose matches what was recorded. No RNG, no θ.\n\n\n\n\n\n","title":"Concourse.replaydraws"},{"category":"function","location":"reference/interpreter/#Concourse.draw!","page":"Interpreter and record","text":"draw!(ds, purpose, dist) -> Float64\n\nA raw distribution draw (routing uniforms, SIRO's pick). Keying the stream by (firing clock, purpose) makes the draw belong to the clock rather than to a position in a global call sequence — the coupling property the sampler's own streams already have. In replay mode the recorded value is returned and dist is ignored.\n\n\n\n\n\n","title":"Concourse.draw!"},{"category":"type","location":"reference/interpreter/#Concourse.BlockingDeadlock","page":"Interpreter and record","text":"BlockingDeadlock\n\nThe runtime error a model compiled with allow_blocking_cycles = true raises when a cycle of full :block buffers actually wedges (see compile). Fields:\n\ncycle: the station names in routing order, starting from the station whose blocked transfer closed the cycle; each holds a job routed to the next (the last back to the first).\ntime: the wall time of the firing whose settle cascade formed the cycle.\njobs: the held job ids, one per cycle station, aligned with cycle.\nrecord: the MarkedRecord up to and including the firing that deadlocked, when thrown from simulate; nothing when thrown from a bare fire_changes. A deadlock is a state property, so replay of the record reproduces the trajectory and re-raises the same error at the same firing.\n\n\n\n\n\n","title":"Concourse.BlockingDeadlock"},{"category":"section","location":"manual/record_replay/#The-record-and-replay","page":"The record and replay","text":"Every research workflow in this manual builds on one property: a simulation run produces a record, and the record replays deterministically. Statistics, trajectory splitting, and derivative estimation are all built as operations over records or over live worlds that could have produced them. This page shows what the record holds and why replay is exact.","title":"The record and replay"},{"category":"section","location":"manual/record_replay/#What-a-run-produces","page":"The record and replay","text":"simulate returns a MarkedRecord. The record is a list with one entry per firing. An entry holds three things: which clock fired, the time it fired, and any auxiliary random values the firing consumed.\n\nusing Concourse\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :counter; service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:counter))\nroute!(net, :counter, Always(:done))\nm = compile(net)\n\nrec = simulate(m, [1.0, 2.0], 100.0; seed = 42)\nlength(rec)\n\nEach entry names a clock by its ClockKey, a tuple (family, station, jobid). The :arrival family is a source's next-arrival clock. The :service family is one clock per job in service. The first few firings of this run:\n\n[(rec.key[i], round(rec.time[i], digits = 3)) for i in 1:5]\n\nThe record also stores its horizon, so time averages know the length of the observation window:\n\nrec.horizon","title":"What a run produces"},{"category":"section","location":"manual/record_replay/#Replay-is-a-fold","page":"The record and replay","text":"replay starts from the initial state and applies the firings one at a time. It returns the whole state trajectory: index i + 1 is the state just after firing i.\n\nstates = replay(m, rec)\nlength(states) == length(rec) + 1\n\nReplay is exact, not approximate. The state change caused by a firing is a deterministic function once the firing time and the recorded draws are given. simulate can keep its own live states so you can check this claim directly. states_equal compares two states field by field, including the clock bookkeeping:\n\nrec2, live = simulate(m, [1.0, 2.0], 100.0; seed = 42, keep_states = true)\nall(states_equal(a, b) for (a, b) in zip(live, replay(m, rec2)))\n\nThe same seed also reproduces the record itself, entry for entry:\n\nrec.key == rec2.key && rec.time == rec2.time","title":"Replay is a fold"},{"category":"section","location":"manual/record_replay/#Statistics-are-folds-over-the-record","page":"The record and replay","text":"Because the state between two firings is constant, a time average is exact: it is a sum of state values weighted by the gaps between event times. time_average runs that fold for any function of the state. number_in_system counts the jobs currently anywhere in the network.\n\nlong = simulate(m, [1.0, 2.0], 5000.0; seed = 7)\ntime_average(number_in_system, m, long)\n\nTheory for this M/M/1 queue says the long-run mean is rho(1-rho) = 1 at utilization rho = lambdamu = 05.\n\nAny function of the state works. Utilization is the fraction of time the server is busy:\n\ncounter = m.names[:counter]\ntime_average(st -> Float64(!isempty(st.srv[counter])), m, long)\n\nYou can also write your own fold directly. Counting departures needs only the keys:\n\nndepart = count(k -> k[1] == :service, long.key)\nndepart / long.horizon   # departure rate, close to λ = 1 in steady state","title":"Statistics are folds over the record"},{"category":"section","location":"manual/record_replay/#Auxiliary-draws-are-recorded-too","page":"The record and replay","text":"Clocks carry most of the randomness. Two kinds of randomness are not clocks: marks (a job's random size or class, drawn at arrival) and random routing (a weighted coin flip). These auxiliary draws are taken from named random streams during the live run and written into the record next to the firing that consumed them. Replay never draws again — it reads the recorded values back. That rule is what keeps replay exact for models with marks and coin flips, and it means replay never needs a random number generator or a parameter vector.\n\nnet2 = QueueNetwork(param_names = (:lambda, :mu_short, :mu_long))\nsource!(net2, :arrive;\n        interarrival = Law(:Exponential, scale = inv(Param(:lambda))),\n        mark = MarkLaw(size = Law(:Exponential, scale = Const(1.0))))\nstation!(net2, :short; service = Law(:Exponential, scale = inv(Param(:mu_short))))\nstation!(net2, :long;  service = Law(:Exponential, scale = inv(Param(:mu_long))))\nsink!(net2, :done)\nroute!(net2, :arrive, ByMark(Mark(:size), [2.0], [:short, :long]))\nroute!(net2, :short, Always(:done))\nroute!(net2, :long, Always(:done))\nmsplit = compile(net2)\n\nsrec = simulate(msplit, [1.0, 2.0, 2.0], 100.0; seed = 11)\n# The first arrival's entry carries the size it drew.\nsrec.key[1], srec.draws[1]\n\nThe replay fold reproduces the live trajectory for this model too:\n\nsrec2, slive = simulate(msplit, [1.0, 2.0, 2.0], 100.0; seed = 11, keep_states = true)\nall(states_equal(a, b) for (a, b) in zip(slive, replay(msplit, srec2)))","title":"Auxiliary draws are recorded too"},{"category":"section","location":"manual/record_replay/#Firing-0:-the-init-slot","page":"The record and replay","text":"One kind of randomness happens before any clock fires. A network with a populate! population may draw the seeded jobs' initial marks at time zero, and those draws follow the same rule as every other auxiliary draw: recorded live, read back on replay. They live in the record's init list — the \"firing 0\" slot — keyed by the reserved pseudo-clock (:init, 0, 0), in population declaration order.\n\nnet3 = QueueNetwork(param_names = (:mu,))\nstation!(net3, :ring; service = Law(:Exponential, scale = Mark(:size) * inv(Param(:mu))))\nroute!(net3, :ring, Always(:ring))\npopulate!(net3, :ring, 3; mark = MarkLaw(size = Law(:Exponential, scale = Const(1.0))))\nmring = compile(net3)\n\nrrec = simulate(mring, [2.0], 100.0; seed = 5)\nrrec.init\n\nreplay and time_average seed the initial state from rec.init before folding the firings, so the determinism claim covers populated networks too; for a network without a population the slot is empty and seeding consumes nothing. The conduit's discipline also holds at firing 0 — a truncated init list errors loudly rather than desynchronizing quietly. The closed networks page develops the populations that put this slot to work.","title":"Firing 0: the init slot"},{"category":"section","location":"manual/record_replay/#Why-this-matters","page":"The record and replay","text":"Determinism given the record is the foundation for everything that follows in this manual.\n\nStatistical analysis computes batch means and regeneration cycles by folding over one record, without re-running the simulation.\nGradient estimation replays a record while tracking how firing times would shift under a parameter change. That only makes sense because replay is exact.\nBranching worlds extend the same discipline to live simulations that run past the end of any record.\n\nThe record is deliberately minimal — it is the filtration of the run, in the statistical sense. Anything you did not record can be reconstructed by replaying, because replay is cheap and exact.","title":"Why this matters"},{"category":"section","location":"manual/samplers/#Choosing-a-sampler","page":"Choosing a sampler","text":"The interpreter does not draw firing times itself. It tells CompetingClocks.jl which clocks are enabled, with which distributions, and the sampler answers one question: which clock fires next, and when. Concourse re-exports three sampler choices and passes your pick through the method keyword of simulate:\n\nsimulate(m, θ, horizon; seed = 1, method = NextReactionMethod())\n\nFirstToFireMethod() draws a clock's firing time once, when the clock is enabled, and keeps it in a queue. It is the simplest and usually the fastest choice, and it is what the builder picks when you pass no method.\nNextReactionMethod() is Anderson's modified next-reaction method for exponential-family clocks and the classic next-reaction method for everything else. It reuses draws across re-declarations, which makes it the best choice for variance reduction with common random numbers (CRN).\nFirstReactionMethod() redraws every enabled clock at every step. That is wasteful for large models but very fast for small ones, and redrawing at each call is occasionally what you want when resampling paths.","title":"Choosing a sampler"},{"category":"section","location":"manual/samplers/#No-sampler-is-load-bearing","page":"Choosing a sampler","text":"The design claim (decision D4 in The event loop) is that sampler choice is a performance knob, never a correctness knob. Every sampler valid for the model's distributions must produce statistically identical results, because the interpreter speaks only the sampler-facing surface (enable!, disable!, reenable!, next, fire!) and no model code reaches around it. The M/M/1 mean under all three methods:\n\nusing Concourse\nusing Statistics\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :counter; service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:counter))\nroute!(net, :counter, Always(:done))\nm = compile(net)\nθ = [1.0, 2.0]\n\nfor method in (FirstToFireMethod(), NextReactionMethod(), FirstReactionMethod())\n    vals = [time_average(number_in_system, m,\n                         simulate(m, θ, 1000.0; seed = r, method))\n            for r in 1:12]\n    println(nameof(typeof(method)), \": \",\n            round(mean(vals), digits = 3), \" ± \",\n            round(std(vals) / sqrt(12), digits = 3))\nend\n\nAll three straddle the true value 1. The guarantee is statistical, not pathwise: two methods may or may not reproduce the same trajectory from the same seed. (Here FirstToFireMethod and NextReactionMethod happen to produce identical records for these exponential clocks, while FirstReactionMethod walks a different path to the same distribution.)\n\nftf = simulate(m, θ, 100.0; seed = 3, method = FirstToFireMethod())\nfrm = simulate(m, θ, 100.0; seed = 3, method = FirstReactionMethod())\nlength(ftf), length(frm), ftf.time == frm.time","title":"No sampler is load-bearing"},{"category":"section","location":"manual/samplers/#Replay-does-not-care-either","page":"Choosing a sampler","text":"Whatever sampler produced a record, replay is a fold over the record and touches no sampler at all. The replayed trajectory matches the live one for every method — here on a harder model, preemptive priority with resumed service, with the debug oracle on:\n\nnet2 = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net2, :arrive;\n        interarrival = Law(:Exponential, scale = inv(Param(:lambda))),\n        mark = MarkLaw(class = Law(:Uniform, a = Const(0.0), b = Const(2.0))))\nstation!(net2, :cpu;\n         discipline = Priority(Mark(:class); preempt = true, memory = :resume),\n         servers = 1, service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net2, :done)\nroute!(net2, :arrive, Always(:cpu))\nroute!(net2, :cpu, Always(:done))\nmp = compile(net2)\n\nfor method in (FirstToFireMethod(), NextReactionMethod())\n    rec, live = simulate(mp, [1.0, 1.5], 300.0; seed = 21, method,\n                         keep_states = true, debug = true)\n    ok = all(states_equal(a, b) for (a, b) in zip(live, replay(mp, rec)))\n    println(nameof(typeof(method)), \" replay exact: \", ok)\nend","title":"Replay does not care either"},{"category":"section","location":"manual/samplers/#Practical-guidance","page":"Choosing a sampler","text":"Plain forward simulation: pass nothing. The builder chooses, and the default (FirstToFireMethod) is the fastest for ordinary runs.\nPaired runs and finite differences: NextReactionMethod. Reusing draws per clock gives the tightest CRN coupling across parameter changes; see the common-random-numbers section of Statistical analysis.\nBranchable worlds: branch_world defaults to NextReactionMethod and you should leave it. The branching protocol requires that peeking at the next firing twice gives the same answer, so the sampler must cache its reservation. A redraw-at-next method would fail ClockGradients.check_branchable — honestly, at certification time, rather than silently in an estimator.\nTiny models, many restarts: FirstReactionMethod avoids the queue bookkeeping entirely and redraws each call.\n\nIf a sampler choice ever changes a statistical result, that is a bug in Concourse or CompetingClocks, not a configuration subtlety — the test suite (test/test_samplers.jl) runs the same oracles under every method to enforce exactly that.","title":"Practical guidance"},{"category":"section","location":"#Concourse.jl","page":"Home","text":"Concourse builds queueing-network models. It compiles each network to a generalized semi-Markov process (GSMP), simulates it with CompetingClocks.jl, and estimates derivatives of simulation output with ClockGradients.jl. The same model value drives the simulator and every derivative estimator.","title":"Concourse.jl"},{"category":"section","location":"#Quickstart","page":"Home","text":"An M/M/1 queue: Poisson arrivals at rate λ = 1, one server at rate μ = 2. The long-run number of jobs in the system should be ρ/(1−ρ) = 1 at utilization ρ = λ/μ = 0.5.\n\nusing Concourse\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :counter; discipline = FCFS(), servers = 1,\n         service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:counter))\nroute!(net, :counter, Always(:done))\nmodel = compile(net)\nrecord = simulate(model, [1.0, 2.0], 2000.0; seed = 42)\ntime_average(number_in_system, model, record)\n\nThe simulation agrees with the formula. That check — simulation against closed-form theory, on the page — is the pattern this documentation repeats wherever theory exists.","title":"Quickstart"},{"category":"section","location":"#Where-to-start","page":"Home","text":"New to queueing theory? Start with Queues, a tutorial that teaches queueing through pictures and simulations you can run.\nKnow queueing and want to do research with this tool? Start with the Manual: records and replay, trajectory splitting, gradient estimation, statistical analysis.\nWant to understand or extend the design? Start with the Developer section, which explains the three-layer architecture and the reasoning behind the model contract.\n\nThe Reference section documents every exported name, grouped by architectural layer.","title":"Where to start"},{"category":"section","location":"queues/jobs_servers/#Jobs,-servers,-and-service-times","page":"Jobs, servers, and service times","text":"Queueing theory is the mathematics of waiting in line. It needs only three ideas to start.\n\nA job is a unit of work: a customer at a counter, a print request, a compute task. A server is the resource that does the work. A server works on one job at a time. The service time is how long the work takes. Service times vary from job to job, so we model the service time as a random variable: each job's service time is a fresh draw from a probability distribution.\n\nHere is the whole picture so far — one server, jobs passing through it:\n\nusing Luxor, QueueDiagrams, LaTeXStrings # hide\n@drawsvg begin # hide\nbackground(\"white\") # hide\nfontface(\"Helvetica\") # hide\ns = server(Point(0, 0), 30; inservice = 1, label = L\"\\mu\")\nflow(Point(-170, 0), s.entry; label = \"jobs arrive\")\nflow(s.exit, Point(170, 0); label = \"jobs leave\")\nend 460 140 # hide\n\nThe label mu (mu) is the service rate: the average number of jobs the server can finish per unit of time. If the mean service time is half a minute, then mu = 2 per minute. Rate and mean are reciprocals: ES = 1mu, where S is the service time.","title":"Jobs, servers, and service times"},{"category":"section","location":"queues/jobs_servers/#Building-it-in-Concourse","page":"Jobs, servers, and service times","text":"Concourse describes a queueing system as a small network. Even the lone server needs three pieces: a source that creates jobs, a station that holds the server, and a sink where finished jobs go. route! wires them together, and compile freezes the description into a runnable model.\n\nusing Concourse\n\nnet = QueueNetwork(param_names = (:lambda, :mu))\nsource!(net, :arrive; interarrival = Law(:Exponential, scale = inv(Param(:lambda))))\nstation!(net, :desk; discipline = FCFS(), servers = 1,\n         service = Law(:Exponential, scale = inv(Param(:mu))))\nsink!(net, :done)\nroute!(net, :arrive, Always(:desk))\nroute!(net, :desk, Always(:done))\nm = compile(net)\nnothing # hide\n\nThree things to notice.\n\nA Law is a recipe for a probability distribution. Law(:Exponential, scale = inv(Param(:mu))) means \"an exponential distribution whose scale is 1mu\" — that is, mean service time 1mu. Param(:mu) refers to an entry of a parameter vector theta that we supply at run time, so the same model can be simulated at many rates.\nFCFS() is the queue discipline: first come, first served. Jobs are served in arrival order. Disciplines get their own page.\nThe source draws the times between arrivals from its own law. Arrivals are the subject of the next page; for now they are just a way to feed the server some jobs.\n\ncompile turns the network into a generalized semi-Markov process (GSMP) — plain data that the simulator interprets. You will not need that term again in this tutorial, but it is the object every later feature builds on.","title":"Building it in Concourse"},{"category":"section","location":"queues/jobs_servers/#What-a-simulation-produces","page":"Jobs, servers, and service times","text":"simulate runs the model forward. It takes the parameter vector theta (here lambda = 1 arrival per minute, mu = 2 services per minute), a time horizon, and a random seed. What it returns is a record: the list of every event that fired, with its time.\n\nθ = [1.0, 2.0]\nrec = simulate(m, θ, 6.0; seed = 3)\nfor (key, t) in zip(rec.key, rec.time)\n    println(rpad(round(t, digits = 3), 8), key)\nend\n\nEach line is one event. The key (:arrival, 1, 0) says \"the arrival clock of station 1 (the source) fired\": a new job was born. The key (:service, 2, j) says \"the service clock of job j at station 2 (the desk) fired\": job j finished and left. Jobs are numbered in arrival order. The record is the complete output of a simulation — every statistic on the pages that follow is computed by reading it back.","title":"What a simulation produces"},{"category":"section","location":"queues/jobs_servers/#Checking-the-service-times","page":"Jobs, servers, and service times","text":"The model says service times are exponential with mean 1mu = 05 minutes. Did the simulation obey? A job's service starts when it first appears in the server slot and ends when its service event fires. replay rebuilds the full state trajectory from the record, so we can watch for both moments and measure every realized service duration.\n\nusing Statistics\nrec = simulate(m, θ, 2000.0; seed = 1)\nstates = replay(m, rec)              # states[i+1] is the state just after event i\ndesk = m.names[:desk]\nentered = Dict{Int,Float64}()        # job id => time it entered service\nfor (i, t) in enumerate(rec.time)\n    for j in states[i + 1].srv[desk]\n        haskey(entered, Int(j)) || (entered[Int(j)] = t)\n    end\nend\ndurations = [rec.time[i] - entered[Int(k[3])]\n             for (i, k) in enumerate(rec.key) if k[1] == :service]\nest = mean(durations)\nse = std(durations) / sqrt(length(durations))\nprintln(\"measured mean service time: \", round(est, digits = 4),\n        \" ± \", round(se, digits = 4))\nprintln(\"theory: 1/mu = \", 1 / θ[2])\n\nThe measured mean lands within a couple of standard errors of 05. This pattern — simulate, measure, compare against the formula — repeats on every page of this tutorial.\n\nOne more check costs nothing. Every job that arrived either left or is still inside, so the counts must balance exactly:\n\narrivals   = count(k -> k[1] == :arrival, rec.key)\ndepartures = count(k -> k[1] == :service, rec.key)\nprintln(arrivals, \" arrivals, \", departures, \" departures, \",\n        number_in_system(states[end]), \" still in the system\")\nprintln(\"conserved: \", arrivals - departures == number_in_system(states[end]))\n\nNext: what happens when jobs arrive faster than the server can drain them — arrivals and the waiting line.","title":"Checking the service times"}]
}
